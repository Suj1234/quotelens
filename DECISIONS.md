# Decisions

Format: date · what was specified · what was found/used instead · why · where it matters.

- **2026-09-23 · Doc filenames.** CLAUDE.md refers to `docs/01_PRD.md` / `docs/02_FSD_TRD.md`; the files are `docs/01_PRD_Kill_the_Quote_Spreadsheet.md` / `docs/02_FSD_TRD_Kill_the_Quote_Spreadsheet.md`. Kept the files as delivered. Matters only when locating sections.
- **2026-09-23 · Next.js.** TRD §2 says 15.x (verify). `create-next-app@latest` gave **16.3.6** with React 19.2 and Tailwind **v4** (CSS-first config, no `tailwind.config`). Tokens live in `src/app/globals.css` (`:root` + `@theme inline`), which is how DESIGN.md's "tailwind.config theme extensions" maps to v4. Next 16 ships `AGENTS.md` with the rule to read `node_modules/next/dist/docs/` before writing Next-specific code; kept.
- **2026-09-23 · shadcn/ui.** CLI 4.21.0, style `radix-nova`, base Radix. `toast` is no longer offered for Radix projects → using **`sonner`**. shadcn CSS variables are mapped onto DESIGN.md tokens in `globals.css`; radii capped at 3/5/6px.
- **2026-09-23 · SheetJS.** npm `xlsx@0.18.5` has an unfixed high-severity advisory (prototype pollution / ReDoS) and we parse untrusted vendor files → installed **0.20.3 from `cdn.sheetjs.com`** (SheetJS's official distribution). Remaining `npm audit` item: `uuid@8` via exceljs (buffer path of v3/v5/v6 only; exceljs uses v4) — not reachable.
- **2026-09-23 · Fonts.** DESIGN.md §1.3 says self-host woff2 in `/public/fonts`. Using `next/font/google` (IBM Plex Sans 400/500/600, Mono 400/500), which downloads at build time and serves from our own origin — same outcome, no manual font files.
- **2026-09-23 · Theme.** DESIGN.md's `localStorage["ql-theme"]` + `data-theme` on `<html>` (prototype's mechanism); no stored value = follow OS. Tailwind `dark:` variant bound to `[data-theme="dark"]`.
- **2026-09-23 · `@types/node`.** Scaffold pinned `^20`; vitest 5 peers need ≥22. Runtime is Node 22 → `@types/node@^22`.
- **2026-09-23 · `server-only` in scripts/tests.** `tsx` scripts run with `--conditions=react-server` so `server-only` resolves to its empty module; vitest aliases it to a stub.
- **2026-09-23 · Supabase API keys (P0-T2, checked supabase.com/docs/guides/api/api-keys).** Legacy `anon`/`service_role` JWT keys are deprecated by end of 2026; new keys are `sb_publishable_…` / `sb_secret_…`. A secret key works in `createClient` exactly like service_role and bypasses RLS; Supabase rejects it from browsers (User-Agent check). We keep the TRD env names — `SUPABASE_SERVICE_ROLE_KEY` may hold either the legacy service_role JWT or an `sb_secret_` key.
- **2026-09-23 · Supabase Storage API (P0-T2).** Docs pages for the JS reference moved (404); verified against installed `@supabase/storage-js@2.117.1` typings: `storage.from(bucket).createSignedUrl(path, expiresInSeconds, { download?, transform? })` → `{ data: { signedUrl } }`; `upload(path, body, { contentType, upsert })`; `createBucket(id, { public })`. Signed URLs use 3600 s (TRD §5: 60 min).
- **2026-09-23 · Migrations.** `0001_init.sql` / `0002_views.sql` are extracted **verbatim** from TRD §6.1–6.21 (awk over the code blocks). Additions, each marked in the file: (a) no `create extension pgcrypto` — `gen_random_uuid()` is core since PG13; (b) RLS enabled on every table with no policies + privileges revoked from `anon`/`authenticated`, so a leaked anon/publishable key reads nothing (service role bypasses RLS, so the app is unaffected); (c) views set `security_invoker = true`; (d) `run_readonly_query` gets `search_path = public`, execute revoked from public/anon/authenticated and granted to `service_role` (TRD §6.21 asks for this). (e) `0003_storage.sql` creates the four private buckets via `storage.buckets` (idempotent). Validated locally on PGlite (embedded PG): all apply; read-only function blocks writes even mid-transaction. Applied via **`npm run db:migrate`** (`scripts/migrate.ts`, `postgres` dev dependency, Session pooler `DATABASE_URL`; each file once, tracked in `public._migrations`, RLS on). No Supabase MCP or psql in this session. The script parses the URL by hand because DB passwords may contain `/ ? # @` and it never logs the URL. `npm run db:check` verifies.
- **2026-09-23 · Known risk for P4.** `run_readonly_query` is `security definer` (runs as owner) so it can read any table, incl. `users.password_hash`. The SQL guard (P4-T1) must allow only the `v_*` views. Upgrade path if needed: a dedicated `query_reader` role granted SELECT on views only, and the function owned by it.
- **2026-09-23 · Scripts env.** `tsx --env-file-if-exists=.env.local` (Node 22 flag) instead of dotenv.
- **2026-09-23 · Sign-in route (P0-T3).** TRD §3 lists `(auth)/login/page.tsx`; DESIGN.md §3.1 puts sign-in at `/`. Sign-in lives at `/`; `/login` redirects there. Protection is `requireUser()` in each server page (no Next 16 `proxy`), `requireApiUser()` in route handlers (401/403 JSON).
- **2026-09-23 · Passwords.** `users.password_hash` = `scrypt$salt$hash` via Node `crypto.scrypt` (no bcrypt dependency). Session: iron-session v9, cookie `ql_session`, httpOnly, SameSite=Lax, Secure in production, 7-day TTL. Demo buttons prefill the email and focus the password (CLAUDE.md P0-T3), unlike the prototype which logs in on click.
- **2026-09-23 · Priya's email.** Not in the dataset (`rfx_meta.json` has only Sujit's `sujit.menon@meridianfoods.example`). Used `priya.raghavan@meridianfoods.example`.
- **2026-09-23 · Screen CSS.** Screen-specific layout uses a CSS module copied value-for-value from the prototype (`src/app/signin.module.css`); shared primitives (`.brand`, `.avatar`, `.lead`, `.eyebrow`, `.kbd`) are in `globals.css`; shadcn Button/Input restyled to `.btn`/input (Button default variant = `.btn`, `variant="default"` = `.btn.primary`, `ghost` = `.btn.quiet`).
- **2026-09-23 · MER-0419 status (P0-T4).** TRD §20 step 4 says MER-0419 is a *draft* copy; CLAUDE.md P0-T4 says *issued* (for the seed pipeline). CLAUDE.md wins on scope → MER-0419 is `issued`, frozen v1, like MER-0417; MER-0418 `draft`, v0, vendors attached but not invited. Titles for 0418/0419 follow the prototype RFx list ("… (live run)" / "… (seed run)").
- **2026-09-23 · Seed idempotency.** Users, vendors, lines, questions upsert on natural keys (password re-hashed from `SEED_ADMIN_PASSWORD`). RFx headers, `rfx_vendors` and `settings` are **insert-if-missing** (`ignoreDuplicates`) so re-running the seed never resets a demo in progress (e.g. MER-0417 after P8 award, thresholds changed in Settings).
- **2026-09-23 · Seed master data.** Sujit's role is `buyer` (DESIGN §4 "buyer/admin"; admin-only features are gated to buyer+admin). Vendor emails use `.example` domains taken from the dataset letterheads (Balaji's realistic file shows a real-looking gmail address — not used); gmail-mode plus-aliases live in `settings.vendor_addresses`, seeded `{}` until P6. `default_currency` left INR for all vendors — OrientPack's USD must be read from the document, not from master data. `settings.fx_rates` seeded with USD only (83.15, 2026-09-23 per dataset `core.json`); TRD shows an EUR entry but gives no value, so none invented. Dataset upload skips the generator `.py` files.
- **2026-09-23 · Scripts are ESM.** `scripts/package.json` = `{"type":"module"}` so scripts can use top-level await under tsx.
- **2026-09-23 · RFx list "Annual value".** Shows "—" until a scenario/award exists (P7); the sub-line counts are computed from the rows.
- **2026-09-23 · Gemini models (P0-T6; checked ai.google.dev/gemini-api/docs/models and /structured-output, plus `models.list` for this key).** TRD §2 defaults both to `gemini-3.8-flash` (verify). Chosen: **`GEMINI_MODEL_FAST=gemini-3.5-flash-lite`** ("fastest, most cost-effective 3.5 model", stable) and **`GEMINI_MODEL_STRONG=gemini-3.8-flash`** ("most intelligent Flash model", stable). `scripts/check-models.ts` ran 4 candidates × 3 tests (1-line JSON schema; Kohinoor PDF inline; OrientPack JPG inline), all valid JSON and all read 27 priced rows + the 2.5% footnote (PDF) and USD / 30 rows (photo). Latency: flash-lite 1–5 s; 3.8-flash 3–26 s (thinking; first run slow); `gemini-3.1-pro-preview` 4–10 s but *preview* (lower limits, can be withdrawn) → not used. Pinned exact IDs, not `*-latest` aliases. Both 1,048,576 input / 65,536 output tokens. Revisit STRONG if P1 extraction quality or the 60 s stage budget suffers (options: `thinkingLevel`, or pro-preview).
- **2026-09-23 · Gemini SDK shape (`@google/genai@2.24.0`).** `ai.models.generateContent({ model, contents: [{ role: "user", parts: [{ inlineData: { mimeType, data: base64 } }, { text }] }], config: { temperature, responseMimeType: "application/json", responseJsonSchema: z.toJSONSchema(schema) } })` → `res.text`, `res.usageMetadata.{promptTokenCount,candidatesTokenCount}`. Using `responseJsonSchema` (plain JSON Schema from Zod 4) rather than the OpenAPI-subset `responseSchema`. Docs list supported keywords: type, properties, required, additionalProperties, enum, format, minimum/maximum, items, prefixItems, min/maxItems, title, description, anyOf, `$ref:"#"`; very large/deep schemas may be rejected. `config.thinkingConfig.thinkingLevel` (MINIMAL/LOW/…) is available for latency tuning. The docs also show a newer `client.interactions.create` API — not used; TRD specifies generateContent.
- **2026-09-23 · Vercel (P0-T5; checked vercel.com/docs/functions/limitations and /configuring-functions/duration, updated 2026-08-24).** Hobby + Fluid Compute (on by default for new projects): **300 s default and maximum**, 2 GB / 1 vCPU, **4.5 MB request/response body limit**, single region, default `iad1`. For Next.js ≥ 13.5 App Router, duration is set per route with `export const maxDuration`; the TRD §21 `vercel.json` `functions` globs are not needed (and would name routes that don't exist yet), so `vercel.json` only pins **`regions: ["hnd1"]` (Tokyo)** to sit next to Supabase `ap-northeast-1`. Pipeline routes will still `export const maxDuration = 300` (TRD §21); ask/rfx/email routes 120. **Risk for P1-T4:** uploads > 4.5 MB (phone photos, scanned PDFs) can't go through a function body → upload straight to Supabase Storage with a signed upload URL, then post the path.
- **2026-09-23 · One Supabase project for local and production.** Local dev and Vercel share the same database and buckets; production gets its own `SESSION_SECRET`. `DATABASE_URL` is not set on Vercel (only migrations use it).
- **2026-09-23 · Prototype renders in quirks mode.** `design/prototype.html` has no `<!DOCTYPE>`, so browsers use quirks mode, where tables don't inherit `line-height` (rows 33 px vs 37 px in standards mode). The app matches the rendering with `table.t { line-height: normal }`. Also deviating from the prototype on purpose: `th.num` headers use Plex Sans (the prototype's `.num` rule makes "Lines"/"Annual value" mono; DESIGN §1.3 says table headers are Sans).
- **2026-09-23 · Responsive (DESIGN §6) fixes after a check at 375/768/1440/2560 px.** ≤ 900 px: tables get `min-width: 680px` and scroll inside their card (page never scrolls sideways); codes/numbers `nowrap`; ⌘K hint and org name hidden; brand mark shown above "Sign in" (the left column that carries it is hidden). ≥ 1180 px content stays left-aligned at max 1180 px, as in the prototype.
- **2026-09-23 · Derived text layout (P1-T2).** TRD §7 names per-sheet files (`sheet-{n}.txt`); we store one derived text per file (all sheets, each headed `=== sheet "Name" ===`) because `response_files.derived_text_path` is a single column. `.txt` bodies go through the same email cleaner as pasted text; `.eml` files are parsed with mailparser and only the body is used (attachments inside a dropped `.eml` are not unpacked — Gmail sync in P6 handles real MIME attachments). PDF page count/split via `pdf-lib` (TRD option).
- **2026-09-23 · Response detail placement (P1-T6).** DESIGN §3.5 shows a response's pipeline/files/terms/items as an expanded row inside the Responses list; TRD §3/§17.7 has a route `rfx/[id]/responses/[rid]`. Built the route (linkable, and the Review queue/drawer can deep-link to it later). Easy to reverse: the page body is self-contained and can render inline under the row.
- **2026-09-24 · Jev via OpenRouter (P2-T1; checked openrouter.ai/typesafe/jev-1.13 → 404 for the fetcher, then openrouter.ai/docs/guides/community/typesafe-sdk and openrouter.ai/blog/insights/what-is-jev).** Not the chat-completions API. Body `{model, state, questions: {id: {type: "noul"|"choice"|"score", instructions, criteria?}}}`; choice `criteria` = `{key: description}`, score `criteria` = ordered level list. Response `{id, model, provider, answers: {id: {type, noul | choice + probabilities + confidence | score + legend + probabilities + confidence}}, usage}`. The two pages disagree on the path (SDK guide: `POST /api/v1/systemone`; blog: `POST /api/alpha/decisions`) → adapter tries the SDK path, then the blog path on 404. Model from `OPENROUTER_JEV_MODEL` (default `typesafe/jev-1.13`; the SDK sample uses bare `jev-1.13` — change the env if OpenRouter rejects the prefixed id). Choice options are sent as keys `o0..oN` (text in the description) so long option strings never round-trip. Score answer (weighted mean of level index) mapped linearly onto our `{min,max}`. 8 s timeout, one retry on 429/5xx, any error or unparseable answer → Gemini emulation, failure logged to `model_calls` as `jev-openrouter ok=false`. No key yet → never called. **Unverified until a key exists:** the exact path and model id.
- **2026-09-24 · Map/normalise/questionnaire design choices (P2-T3…T6).** (a) Range items ("items 1 to 12", Anand) map to every line in the range via one decision-layer boolean — TRD §8.3 only describes one item → one line. (b) Mapping lives in `responses.summary.map.mapping` (the runner stores each stage's summary under its key) rather than `summary.mapping`. (c) Kohinoor-style footnotes: two extra booleans in the terms decision ("rates printed net of the discount", "buyer's payment terms miss the condition") → gross-up ÷ (1 − pct), `discount_treatment` assumption with basis `system_inferred`, cell `inferred` — the gold key's primary expectation; a plain conditional discount (Balaji) stays unapplied per `discount_default`. (d) Pack size inferred from the vendor's other items is only a **best guess** (cell `ambiguous`), per the gold key; RFx `spec_attributes.pack_size` would be a real basis (`inferred`). (e) Questionnaire yes/no uses one **choice** (yes / no / unclear or pending / not answered) instead of TRD §10.2's two booleans, because "BRC audit in process" is neither yes nor no; `probability` stores p(yes)/(p(yes)+p(no)). (f) `v_vendor_status.cleared_questionnaire` redefined in `0004` (ambiguous → null, missing mandatory → false). (g) New setting `freight_default_inr_per_1000` = 180 (TRD §11.7 names it; §6.19 seed list didn't). (h) Re-runs never overwrite cells/answers a buyer reviewed or excluded, and never recreate a review item that exists in any status.
- **2026-09-24 · Questionnaire extraction on the fast model (P2-T6/T8).** TRD §8.5 doesn't name a tier. Strong model took 10–49 s per response (Anand's email 49 s — too close to the ~60 s budget); fast model 7–10 s with the same clean-set score (48/50). Trade-off seen on the realistic set: the fast model read OrientPack's "lead time 6 days ex-works" as Q4 (sample lead time); Q4 doesn't disqualify. Switch back = one word in `questionnaire.ts`.
- **2026-09-24 · Questionnaire rules decided with Sabarish after the P2 review.** (1) **Ranges** ("10-12 days") are stored at the end worse for the buyer — upper for lead times, MOQs and prices, lower for capacities — with the range kept verbatim in `answer_raw` (safety-stock practice plans on the maximum lead time). Gold key Anand Q8 changed 10 → 12 (note in gold.json); also removes run-to-run flip between 10 and 12. (2) **Caveated yes** ("only Nelamangala regularly; Hosur on request") counts as *yes*, caveat kept in the answer text. (3) **Missing mandatory answers** stay `missing` (vendor not cleared) and raise one vendor-level review item `questionnaire_missing` (new type, migration 0005) so the buyer can ask the vendor. (4) **Related-figure misreads** (regular lead time read as sample lead time) are blocked twice: P-QA-EXTRACT v2 says a related figure is not an answer, and the decision state leads with the supplier's own text, marking the extractor's reading as secondary. Fast model kept. Prompt v1 in `prompts/archive/`.
- **2026-09-24 · Further P2 choices not in PRD/TRD (listed for Sabarish, accepted as recorded here).** (a) **Flags writes the decision back** into the interpreted `response_terms` columns (`freight_included`, `taxes_included`, `references_prior_pricing`), replacing the extractor's value. (b) **`partial_quote`** raises a vendor-level `missing_line` review item. (c) **A vendor item with no price** and a good read → `not_quoted`, or `references_prior` when the vendor pointed at earlier pricing; a poor read (< 0.60) → `low_confidence`. (d) **Validity given only as an end date** → days counted from the response's `received_at`. (e) **`per set`** is treated as per piece (a partition set is one piece on our line sheet). (f) **Mapped column chips** (Response Detail) add `conflict`, `not quoted` and `unplaced` to DESIGN §3.5's `unit?` / `low read` / `prior`. (g) **Eval reading:** an `ambiguous` cell whose best guess is more than the tolerance away from gold's `best_guess` is `wrong` (README §5 only says within tolerance = correct).
- **2026-09-24 · Inconsistencies found in the docs (left as delivered).** PRD §V3 says Westline quotes "per box"; the file, TRD and gold say per bundle — built to the file. PRD per-kg example uses 0.412 kg/pc; `rfx_lines.csv` line 1 is 1,318.1 g — built to the line sheet.
- **2026-09-24 · Comparison grid (P3-T1).** (a) Vendor columns sorted by name: the prototype shows invitation order, but the five seeded vendors share one `created_at` and `rfx_vendors` has no order column, so there is no stable invitation order to sort by (add an ordinal if the order matters). (b) The basis toggle and "As written" are applied client-side on one grid model; `GET /api/rfx/{id}/comparison` ignores `?basis`/`?original` (same data either way). (c) Cells show whole rupees; the drawer shows the exact value. (d) "Lowest eligible" and vendor totals count only confirmed / inferred / reviewed cells of vendors whose questionnaire is cleared (✓), unless "show disqualified vendors" is on — as in the prototype's `eligible()`; Anand (? pending) is therefore never the teal minimum until Q6 is decided. (e) `conflict` cells get a red outline and appear in the legend only when present (DESIGN §2.6 has the style; §2.8's legend lists eight states).
- **2026-09-24 · Provenance drawer (P3-T2).** (a) **PDF evidence uses the browser's own PDF viewer**: an iframe of the signed URL at `#page=N` (served inline as `application/pdf`) plus the snippet, instead of rasterising pages — no PDF renderer is installed, and CLAUDE.md cut-list item 6 already drops bbox overlays. Headless test browsers show it blank; the "Open file" link is the fallback. (b) Photos: a ≤ 900 px JPEG, cropped around `location.bbox` (+4 %, TRD §12.2) when the reader gave one, else the whole photo; generated once into `derived/…/crops/` or `preview.jpg` and reused (keyed by extracted item / file, never by content). The extractor returned no bbox for the OrientPack photo, so line 14 shows the whole card. (c) Document paragraphs are windowed to ~180 characters around the snippet; email shows ±1 non-empty line; spreadsheet evidence is the full `[row N]` with the price cell marked. (d) Mapping shows the chosen line + the next two candidates with p; the none-of-these probability isn't stored. (e) Buttons: DESIGN's `.btn` is the restyled shadcn Button (no `.btn` class exists).
- **2026-09-24 · Review queue (P3-T3).** (a) **One `prior_pricing` card per vendor** (DESIGN §3.6's queue of 13 has one for Anand's 8 lines); TRD §8.4 also asks for one per affected line — dropped as queue noise; the vendor card's "Treat as not quoted" applies to all its lines. (b) Card titles follow DESIGN §3.6's wording ("Item 9: price per bundle, bundle size not stated", "USD → INR at 83.15 (23 Sep 2026)", "Validity 30 days (RFx asked 60)"); best-guess note per DESIGN §5 ("Best guess 25/bundle from the vendor's other 5-ply items — not applied"); an ambiguous card from the peer guess carries p 0.60. (c) **Ask vendor in P3 = draft + "Mark as asked"**: a template draft (To / Reply-To `…-clar-1` / Subject / body listing the vendor's open points), Copy, and "Mark as asked" (item → `asked_vendor`, `rfx_vendors.status` → `clarification_sent`); P-CLARIFY and real sending replace the template in P6. (d) **Map to line** writes a buyer mapping (p 1, provider `buyer`) into `summary.map.mapping`, marks the unmatched row, logs `mapping_override`, then re-runs normalise for that response (buyer-decided cells stay). (e) Confirm on ambiguous/low cells logs an `other` "buyer confirmed the system's value" row; overrides add a `buyer_override` chain step + `value_override` row; landed for reviewed cells = value + the vendor's `freight_treatment` amount (0 if freight included). (f) Any cell with `reviewed_by` set (incl. "treat as not quoted") survives a normalise re-run. (g) Vendor / type / status filters kept (TRD §17.8) although the prototype has none. (h) Acting is buyer/admin only (API 403 for approver; UI shows "Waiting for Sujit.").
- **2026-09-24 · Comparison tabs (P3-T4).** Tabs are URL-addressable (`/comparison?tab=ledger`) and server-rendered. Questionnaire: answers per DESIGN §3.7 with the vendor's words + probability as the hover (TRD "probability tooltip"); the prototype's "*" (answered with a condition) is not shown — we don't store a condition flag, and the vendor's words are in the tooltip. Ledger: per-line system rows of one kind/vendor folded into one row with a line span ("1–22") and a rule sentence; buyer rows are never folded. Timeline: audit events in words, newest first, pipeline runs folded to one "Processed X's reply" row per response (failures always shown), ask-vendor drafts omitted; capped at 150 rows. `GET /api/rfx/{id}/ledger` and `/timeline` added (TRD §16).
- **2026-09-24 · Unmatched items / senders and several replies per vendor (P3-T5).** (a) **Unknown sender**: classify/extract/map run; normalise/questionnaire/flags are *skipped* (stage stays pending, "waiting for a vendor") and one `unknown_vendor` card is raised — instead of a stage error. Assigning a vendor (existing, or new with name + email; a new supplier is added to the RFx as `responded`) closes the card and continues the pipeline from the first stage not done. (b) **A second reply from the same vendor never overwrites cells another reply wrote**: its "not quoted" fill skips those lines, and a price for an already-priced line raises a `conflict` card (Confirm = use this reply's price, Override, Dismiss = keep the earlier one). Clarification replies (TRD §8.7) will supersede on purpose in P6. (c) A reply that priced nothing (e.g. an IT quote dropped on Balaji) writes no vendor-level ledger rows or freight/discount/validity cards; its "missing" questionnaire answers never replace real ones. (d) **System ledger rows carry `value.response_id`**, so a normalise re-run deletes only its own reply's rows (untagged pre-P3 rows are cleaned on the next run). (e) Grid header terms and the Responses row use the vendor's **main reply** (the one that supplied most of its cells), not simply the latest; `v_vendor_status.freight_included/validity_until` still use the latest reply — revisit in P4 if the Ask panel reads them. (f) Unmatched items panel sits under the Prices grid (collapsed), Map / Ignore go through the item's `unmapped_item` review card.
- **2026-09-24 · P3 audit follow-up (after Sabarish asked for a full check).** Built the missing spec items: review **bulk bar** (select cards → Confirm / Dismiss; cards an action doesn't fit are skipped and counted), **Group by type / vendor** (PRD #17), **ledger rows for Accept-as-Yes / Treat-as-No** with before/after, and before/after on confirms (PRD #18; acknowledgements, dismissals and ignores stay audit-only as TRD §12.3 says), Questionnaire **click → evidence** panel (TRD §17.9), **"Clarification asked of … Waiting for their reply"** note (DESIGN §3.6), **approver tabs** = Comparison only until Decide/Award land in P7 (DESIGN §4; `/rfx/{id}` sends the approver to Comparison), Ledger **When** column, drawer "in the ledger" link, Response Detail **flag chips + counts + Go to Review Queue (n)** (TRD §17.7), informational cards accept **Dismiss** (TRD §12.3), a ledger row for **guessed pack sizes** (prototype Ledger), Timeline **oldest first** (prototype). Kept as is: questionnaire rows show the full question text (no short labels are stored) and no "*" condition marker; card probability bars carry no "LLM-estimated" tag (DESIGN §0.6 keeps that label to the drawer, though TRD §17.8 mentions a provider tag).
- **2026-09-24 · Robustness fixes found by the P3 audit (run-to-run model variance).** (a) **Map uses the full spreadsheet row** for cell-located items (the extractor sometimes quotes only the price cell, which shifted Balaji lines 12–22 once). (b) **Terms decision v2**: the "rates printed net of the discount" question now names the signs of net pricing (a divide-by-(1 − discount) instruction); v1 in `prompts/archive/TERMS-DECIDE_rates_net_v1.txt` (Kohinoor's gross-up was skipped once). (c) **Extraction self-check**: `ExtractionResult.rows_with_prices`; if the model returns ≥ 3 and ≥ a third fewer items than the priced rows it counted, one retry with the mismatch spelled out, keep the fuller result (OrientPack's photo collapsed to 1 item in 1 of 8 runs; also fixes the WhatsApp sample: 1 → 8 items). P-EXTRACT v1 in `prompts/archive/`. (d) **Questionnaire answers from another reply are never replaced**; a different answer raises a "two answers from this vendor" card (a stray IT quote had flipped Balaji Q10 to No). After the fixes: 150/150 on 2 fresh clean reloads + 3 re-runs from map + a realistic reload.
- **2026-09-24 · SQL guard beyond TRD §13.4 (P4-T1).** Added on top of the five TRD rules, because `run_readonly_query` is security definer: (a) every base-table name (all tables in 0001–0005 + `_migrations`) is rejected wherever it appears, even as an alias or CTE name; (b) every `FROM`/`JOIN` target must be one of the five views or a CTE defined in the same query (so a column alias can't smuggle in a table); (c) a token followed by `(` must be an allowed function; (d) any UUID literal other than this RFx's id is rejected; (e) each view referenced must be scoped to the RFx (count of `rfx_id` mentions ≥ view references) — without it a join on `vendor_id` alone multiplies rows by the number of RFx sharing those vendors. Aliases must use `AS` (implicit aliases are rejected as unknown identifiers; the repair round tells the model). `v_comparison_bestguess` is in the allowlist (TRD §13.2).
- **2026-09-24 · Ask route (P4-T2).** (a) **`run_readonly_rows`** (migration 0006) next to `run_readonly_query`: same timeout / read-only / service-role grant, but returns `json` (jsonb reorders keys, losing the query's column order) and wraps the query as a subquery (0002 appends `limit 500` straight after the SQL, which breaks any query ending in its own LIMIT). (b) **Migration 0007 `v_vendor_status`**: `validity_until` falls back to received date + `validity_days` (every seeded reply gives days, so the column was always null and "shortest validity" had no data); `validity_days` appended; terms come from the vendor's main reply (DECISIONS P3-T5 e). (c) **P-SQL** is §9.8 verbatim plus the extra column and three "guard notes" (aliases with AS; scope every view; `"sql": ""` when the views can't answer — gives the safe-query answer for off-topic questions instead of a nonsense query). (d) The **repair round** also covers a Postgres error (e.g. an ambiguous column), not only a guard rejection; still one repair. A single trailing `;` is stripped before the guard. (e) **P-SQL on `thinkingLevel: LOW`** (strong model): default thinking took 7–24 s per call and missed the 15 s budget; LOW gives 2–8 s with the same answers (MINIMAL is refused by the model). (f) **Exclusions and unsure-cell counts are computed from the data**, not taken from the model: vendors not cleared (when the query filters on `cleared_questionnaire`), and unsure cells in the query's scope (its lines, its vendors). `unresolved_cells` stays the TRD count for the whole RFx. The money riding on unsure cells (at the system's best guess) is handed to the narrator because the views can't see best guesses. (g) **Narration check**: every number in the answer must match a supplied value (money within rounding, incl. ₹x cr / ₹x L; whole numbers < 10,000 exactly; Indian grouping only) — one retry with the offending numbers, leftovers kept in `plan.narrate_unverified`. Money in the rows is pre-formatted (₹4,52,59,716) for the narrator after it once wrote "₹45,25,9716". (h) **Aggregates** match `/annual_(\w+_)?value|total/` (TRD's `/annual_value|total/` missed `annual_landed_value_inr`); a *total* column repeating one value on every row is taken once. (i) **Charts**: no chart over 12 rows (a 400 px sheet); a one-row answer comparing ≥ 2 totals is charted even if the planner didn't ask (PRD Q2 "number + bar chart"). (j) **History for follow-ups is per user**; `GET /api/ask/history` shows the RFx's last 20 from both users. (k) **"Include best guesses" re-runs an earlier answer**: `POST /api/ask` takes an extra `base_query_id`; the server rewrites that answer's SQL (view swap + widened state clause) and returns both totals.
- **2026-09-24 · Ask UI (P4-T3).** (a) **Inline bars, not Recharts**: TRD §13.6 says Recharts, DESIGN §2.15 says an inline bar chart with no library; DESIGN is the visual spec, so bars are plain divs (Recharts stays installed, unused). (b) **Save as scenario is not rendered** until P7-T1 builds scenarios (no dead buttons). (c) **History**: DESIGN's sheet shows stacked cards; TRD §17.9 asks for a history list — both: this session's cards, plus a collapsed "Earlier questions · n" list (the RFx's last 20, either user) that adds a stored card without re-running it. (d) **Include best guesses** keeps the card and adds a Without / With segmented toggle carrying both totals (DESIGN "shows both totals side by side"); the answer text and table switch with it. It is offered only when the answer's scope had unsure cells (an exclusions entry, or a `state` column). (e) The unresolved notice reads "13 cells unresolved in this RFx · ₹73.2 L at stake" (DESIGN §5 uncertainty copy); at stake is Σ best guess × annual qty and leaves out cells with no guess. (f) Charts over 12 rows are dropped (400 px sheet). (g) Suggestion chips use PRD wording ("…the cheapest single vendor") rather than the prototype's "…giving everything to Kohinoor" (a vendor name in UI copy would go stale on another RFx).
- **2026-09-24 · Exports (P4-T4).** (a) Comparison xlsx cells carry the state as fill + font colour from DESIGN §1.1 light tokens (inferred indigo tint, reviewed green, ambiguous/low amber, not quoted/prior grey, excluded strikethrough, conflict red); ambiguous/low cells show the best guess as text "27,960? (best guess)" so it can't be summed by accident; lowest eligible = bold + thick teal left border; money uses Indian grouping `#,##,##0`. (b) The toolbar has one "Export" (xlsx, in the basis on screen — "As written" exports unit) plus a quiet "CSV" link, since TRD asks for both formats and DESIGN shows one button. (c) CSV files start with a UTF-8 BOM so Excel shows ₹ and names correctly. (d) Both roles may export (PRD #28).
- **2026-09-24 · P-SQL tuning during the Q1–Q8 runs (CLAUDE.md §9, one change per round, previous versions in `prompts/archive/`).** v2 adds "a request to export or download an earlier answer is answered by returning that answer's query again (the app offers the file)" — Q8 had been refused as not answerable. v3 adds a factual line to the `v_comparison` description: unsure cells always have `unit_price` null, so list them by state — Q6 once filtered `unit_price is not null` and found nothing. Also in code (not the prompt): at-stake facts go to the narrator only for unsure cells that are rows of the result, and an empty result carries "no rows matched" (the narrator had turned a 0-row answer into "4 vendors didn't quote line 22" using those facts).
- **2026-09-24 · New RFx + co-pilot (P5-T1).** (a) **Migration 0008 `rfx.terms_set`**: every term column has a default, so "terms pending" (DESIGN §3.3) and the Issue precondition need an explicit flag; set when the co-pilot patches a term or the buyer edits the Terms tab. Backfilled true for the three seeded RFx (they were seeded with full terms) — the only write to MER-0418, a new column. (b) **Issue needs a response deadline too** (P-DISPATCH writes it into the email); the button's tooltip lists what's still missing. (c) **No empty drafts**: `/rfx/new` writes nothing until the first Send / Save / edit; the URL then gains `?id=` via `history.replaceState` (a router navigation would remount the page mid-send). (d) **Attachments** go up as multipart and through the vendor-file preprocessors (xlsx/csv/txt); the JSON `{name, text}` body from TRD §16 is also accepted. A parsed sheet **replaces** the draft's lines (re-attaching is a correction); verbally described lines are appended and marked "draft — confirm" (spec_attributes.draft), cleared when the buyer edits or clicks the chip. (e) **Vendors from the co-pilot** are matched to the address book by name/email; a new name without an email is not added (never invented) and the patch box says so. (f) Patch notes are plain text ("Lines · 30 parsed…"), the UI bolds the label — no HTML from model output. (g) The co-pilot and P-LINESHEET run on the strong model with LOW thinking (P-LINESHEET 30 lines ≈ 10 s; a co-pilot turn 4–6 s). (h) Observed: on the first turn the co-pilot filled the default terms and added all five address-book vendors without being asked; kept (the buyer edits either tab), not tuned. (i) Lines/questions/vendors are replaced as sets and renumbered in order (TRD §16); line SKU falls back to `LINE-n` when a sheet has none; `rfx.edited` audits only fields that actually changed. (j) `createVendor` moved to `src/lib/vendors.ts`, shared by the co-pilot, the Vendors tab and assign-vendor.
- **2026-09-24 · Issue + dispatch (P5-T2).** (a) **Throwaway RFx, not MER-0418**: CLAUDE.md P5-T2's done-when says "issuing MER-0418", but MER-0418 is kept as an untouched draft for the live Gmail run in P6 (handoff rule); the done-when was met on MER-0420 (via the API) and MER-0421 (via the UI), both deleted at the end of P5. (b) **Order**: attachments are built and stored first, then v1 is frozen with a `status = draft` guard (a double click can't dispatch twice), then each vendor's email is written and sent; a vendor whose email fails is recorded (`dispatch.failed`, communication `failed`) while the others go out. (c) **P-DISPATCH on the fast model**, temperature 0.7 (TRD §9: drafting), with a Zod refine that the body contains the exact reply sentence (retry with the error otherwise). The buyer's title comes from the role ("Category Buyer", as DESIGN §3.9 signs the memo) because `users` has no title. (d) **Reply-To** is `<GMAIL_USER local>+<reply_tag>@…` when Gmail is configured, else `sourcing+<reply_tag>@meridianfoods.example` in mock mode; mock sends get a `<mock-uuid@quotelens.local>` message id so the timeline shows one. `email_mode` other than mock raises NOT_CONFIGURED (Gmail send is P6). (e) Questionnaire PDF lists question, answer type and mandatory/optional with an answer box; it does not tell vendors which questions disqualify. Standard Helvetica (no ₹ glyph needed). (f) **Outbox** is its own page (`/rfx/{id}/outbox`, TRD §3), reached after issuing and from the Overview's communications card; not a tab (DESIGN has no Outbox tab).
- **2026-09-24 · Add response + portal (P5-T3).** (a) **Files over 4.5 MB get a clear error, no direct-to-Storage upload yet** (ponytail: signed upload URLs are the upgrade path when real phone photos or scans exceed it): checked in the browser before sending and again on the server (413 TOO_LARGE with the same advice). (b) **"Use seed file"** adds that vendor's clean dataset reply (files + email body, the same set "Load seeded responses" uses) to whatever the buyer dropped; source `mock_upload`. (c) **Landing on Response Detail with the pipeline running**: the sheet and the portal go to `…/responses/{rid}?run=1`; the strip starts "Run all" once, only if no stage has run, and drops `?run` from the URL. (d) The portal sits inside the RFx layout (TRD route `/rfx/{id}/portal/{vendorId}`), buyer-only, as a simulator for the demo operator; it shows the latest `rfx_dispatch` to that vendor. (e) Responses tab rows exist for every invited vendor (Add response works before any reply); the empty-state card keeps "Load seeded responses". (f) Seed reload resets the reloaded vendors' invitation status to `invited` (Westline had been left at `clarification_sent` by P3 testing); flags sets `responded` again.
- **2026-09-24 · Overview (P5-T4).** (a) **No "Go to Review" / "Go to Comparison" buttons** (TRD §17.4 lists them): DESIGN §3.4 / §0.8 says no buttons duplicate the tabs, and DESIGN is the visual spec; "Open the queue" in Needs you stays (DESIGN draws it). (b) **Lead sentence** is computed: replies within N days of the freeze (min "a day"), questionnaire-cleared count, open review items, and the cheapest-qualified-per-line total (same rule as the grid's teal edge and Ask Q1; "for N of 30 lines" when some lines have no qualified quote). (c) **Needs you** folds one vendor's cards of one type into a row ("4 × price per bundle, bundle size not stated (items 5, 9, 15 and 19)"), vendor name shown once per group. (d) **Scope** shows the range of yearly totals of vendors pricing ≥ 90 % of lines; **Transport** has no "change" link until the Settings page exists (P8) — no dead links. (e) The TRD's vendor cards became DESIGN's Vendors table plus the TRD's "Open response" / "Portal (mock)" actions in a last column. (f) Questionnaire chip: "cleared", "failed Q6" (red) or the pending reason (amber); "awaiting reply" before a vendor answers. (g) Communications show every row of `communications` for the RFx; inbound seeded/portal replies stay `received` (the pipeline doesn't mark them `processed` — TRD §6.7 status left as is). (h) Draft RFx opened at `/rfx/{id}` shows a draft notice linking to New RFx.
- **2026-09-24 · P4/P5 visual check against the prototype (1440 px).** Differences kept on purpose or waiting for later phases: (a) no "Sync inbox" header button (P6) and no Award tab (P7); (b) Overview "Sent as" shows the format only ("PDF"), not the prototype's "PDF, 2 pages" / "Excel, own layout"; (c) Transport has no "change" link until Settings (P8); (d) vendor names are shown in full (the prototype shortens "Kohinoor Corrugators" / "Westline"); (e) Outbox and Portal have no prototype screen — built from DESIGN's card, table, email-block and empty-state rules; (f) Ask answer text is longer than the prototype's canned answers (model narration, 2–5 sentences per P-NARRATE); (g) New RFx Lines table shows every TRD §17.3 column as inputs (the prototype shows 7 read-only columns), so it scrolls sideways inside its card.
- **2026-09-24 · Live Gmail out of scope (Sabarish's decision, P6).** CLAUDE.md P6 and TRD §15.3 specify Gmail SMTP send + IMAP sync with an App Password and plus-aliases. Sabarish decided there is **no live Gmail integration in this build**. Built instead: the same flow against a **mock mailbox** that behaves like Gmail — every email is a real RFC 822 `.eml` (nodemailer builds it without sending) with a real `Message-ID`, `In-Reply-To` and tagged `Reply-To`; vendor replies land as unread messages; **Sync inbox** pulls unread mail, matches it by tag (To → Subject → In-Reply-To), is idempotent on Message-ID and marks messages seen. `src/lib/email/gmail.ts` keeps the send/sync interface and throws `NOT_CONFIGURED` ("Live Gmail is out of scope for this build"), like TRD §15.4's Resend stub; `settings.email_mode` stays `mock`; no `GMAIL_USER` / `GMAIL_APP_PASSWORD` is read and no plus-address settings exist. **Where it matters:** (a) CLAUDE.md P6-T1/T2 done-when lines ("delivers five emails to the second inbox", "replying from the second account") are met by the mock mailbox (vendor portal) instead; (b) CLAUDE.md §10 gate "Gmail mode: one live round-trip tested within the last 12 hours" becomes "one mock mailbox round trip (portal reply → Sync inbox)"; (c) PRD §12 "gmail — the live story" and TRD §15.3 are not built beyond the stub; PRD G7 "live email works in Gmail mode" is not met; (d) `docs/05_Demo_Script.md` mentions Gmail in the set-up (second Gmail tab, email mode = gmail), Stage 5b (§1 "Switch to the Gmail tab … reply"), the live-demo table ("What about email — is it real?"), the live-run sequence ("Issue MER-0418 (gmail)") and the fallback row ("Gmail sync stalls") — each Gmail step is done in the vendor portal instead (open the vendor's mailbox, Reply, then Sync inbox). The PRD, TRD, CLAUDE.md and demo script are left as delivered. Switching to live Gmail later = implement `gmail.ts` (nodemailer SMTP transport with the same message options; imapflow fetch of unseen `source` → the same `ingestMessage()` the mock sync uses) and set `email_mode = gmail`.
- **2026-09-24 · Mock mailbox design (P6-T1).** (a) **Table `mock_mailbox`** (migration 0009) stands in for Gmail: one row per RFC 822 message, `direction` `to_vendor` (every email we send; the vendor portal reads it) or `to_buyer` (vendor replies; Sync inbox reads unread ones), `message_id` unique, `seen`, `eml_path` (bucket `outbound` for to_vendor, `raw` for to_buyer). `rfx_id` / `vendor_id` record where a message was sent from (portal listing, cleanup cascade); **Sync never matches on them** — only on the tag, like IMAP. (b) **`communications.eml_path`** (new column) points at the stored `.eml` of every sent/received email; the Outbox offers "Download .eml". (c) **Message-IDs** are generated by nodemailer (`<uuid@meridianfoods.example>` from the sender's domain) and saved on the communication and mailbox rows; the P5 `<mock-uuid@quotelens.local>` placeholder is gone. (d) **Reply-To** is always `sourcing+<tag>@meridianfoods.example` (the mock sourcing mailbox); the GMAIL_USER variant is removed with live Gmail. The From display name is quoted (`"Sujit Menon (Meridian Foods)" <…>`) — unquoted, RFC 5322 reads the parentheses as a comment and drops them. (e) `sendEmail()` builds the `.eml` before routing on `settings.email_mode`: mock stores + delivers; anything else calls the `gmail.ts` stub → NOT_CONFIGURED (501), communication marked `failed` with the message. (f) Attachments are read back from Storage for each email (5 dispatch emails = 10 small downloads) rather than passing buffers through `SendInput`, so the communications row's attachment list stays plain metadata.
- **2026-09-24 · Mock inbox + Sync inbox (P6-T2).** (a) **The P5 portal no longer creates the response.** It is now the vendor's mailbox: every email we sent them (newest first) with Reply; a reply is a real RFC 822 message To the tagged Reply-To of the email answered, left unread for the buyer, and becomes a response only when someone clicks **Sync inbox** (or the Overview's 30 s poll runs) — the same path a Gmail reply would take. The Add response sheet still creates responses directly (mock upload/paste, TRD §15.2). (b) **Source `portal`** for synced mailbox replies (communications `mode = mock`): the check constraint already allows it and it names what happened; `gmail` is not claimed for mock mail. (c) **Idempotency proof = re-delivery.** Sync reads unread messages only (TRD §15.3 `seen:false`), so a second click finds nothing; the Message-ID check is proven by marking the messages unread again (as if marking seen had failed) → 0 new, 2 skipped. (d) **Matching is global, like one Gmail inbox:** Sync for RFx A reads every unread `to_buyer` message; a message tagged for another RFx (or untagged but naming another RFx / answering its email) is left unread for that RFx; untagged mail that answers nothing we sent and names no RFx is marked seen and reported under `ignored`. (e) Pending replies for "Sync inbox — X replied" are counted from the tag in the mailbox row's To/Subject (no `.eml` download). (f) Sync inbox is hidden on draft RFx (nothing can reply yet). (g) The poller is quiet (no "Nothing new" toast every 30 s) and one sync runs per tab at a time (shared store; buttons disabled while busy).
- **2026-09-24 · Clarification loop (P6-T3).** (a) **One email per vendor**: "Ask vendor" on any askable card (ambiguous unit, low-confidence read, prior pricing, questionnaire ambiguous/missing) drafts one P-CLARIFY email covering all that vendor's open askable cards (DESIGN "items 5, 9, 15 and 19"); unticking items asks for a redraft before Send (the body must match what is marked asked). The P3 template + "Mark as asked" is gone; `POST /api/review/{id}/ask-vendor` now returns the same P-CLARIFY draft. (b) **P-CLARIFY** is TRD §9.6 verbatim on the fast (drafting) model, temperature 0.4, with two extra parts: a format note (one bullet per line — v1 without it ran the bullets into one paragraph) and the sign-off; a Zod refine requires every asked item number in the body. The `{items}` bullets are built in code from the cards ("Item 9 (550x350x350, 5-ply): priced per bundle but the bundle size is not stated — please state the number of pieces per bundle."), in line order. (c) **Threading**: Reply-To `<reply_tag>-clar-<n>` (n = clarifications already sent to that vendor + 1); In-Reply-To/References = the RFx dispatch email; a seeded RFx (MER-0419) has none, so the vendor's latest email with a Message-ID, else a new thread. (d) **Scope of a clarification reply (TRD §8.7)**: lines whose cell is references_prior / ambiguous / not_quoted / low_confidence, plus the lines of the cards asked in the request it answers (resolution.communication_id). Only those are upserted; every other cell, the vendor's terms, vendor-level ledger rows and flags are untouched (terms and the terms decision come from the main reply). This overrides the P3-T5 "a second reply never overwrites" guard for clarification replies only. (e) **Merging**: the reply's answer is laid over the item first read — its fields win where given (pack size, a new price, currency); "prices remain as offered" keeps the original price and its read confidence. A line the reply doesn't clearly answer (still ambiguous or unreadable) keeps its original cell and card. (f) **State `reviewed`** (gold `after_clarification`), `reviewed_by` null, review note "Answered by the vendor's clarification reply"; the drawer shows "Reviewed · Westline Packaging (clarification reply)" and a chain step "Vendor's clarification reply (date): pack size 20; price as first quoted, ₹1,374 per bundle" linking to the reply. (g) **Ledger**: one `vendor_stated` row per clarified line ("Line 9: bundle of 20 from the vendor's clarification reply — was ambiguous, best guess ₹54,960"); the earlier system rows for that line get `superseded_by` (kept, not deleted); a normalise re-run never deletes superseded rows, and a clarification re-run releases what it superseded first. (h) Cards for the clarified lines (open or asked) → `resolved_by_reply`; the vendor-level prior-pricing card too once no references_prior line is left; questionnaire cards when the reply answers the question. `rfx_vendors.status` → `clarified` in the flags stage. (i) **Extraction of a clarification reply**: P-EXTRACT unchanged, plus an input note quoting our request and saying the "no prices → items: []" rule doesn't apply (the reply lists bundle sizes, no prices — v1 without the note returned 0 items). (j) **Latest reply wins** (6.19): a clar-2 reply may replace lines a clar-1 reply answered when they are in its scope (asked again, or still unresolved); a buyer's own decision on a line is only replaced if the buyer asked the vendor about that line again. Re-running a clarification reply's stages rewrites its own cells and ledger rows in place (no duplicates). (k) **Mock paste path**: the Add response sheet shows "This answers the clarification sent <date>" (ticked by default) for a vendor in `clarification_sent`; the response is created with `is_clarification` without the mailbox. (l) Seed reload also deletes the reloaded vendors' clarification replies, clarification emails, their mock mailbox rows and `.eml` files.
- **2026-09-24 · Found while testing the clarification loop (P6-T3).** (a) **Map gets the request as context** for a clarification reply: a reply that names only "Item 14" (no size) went to Unmatched; the decision state now starts with the clarification email it answers (the same context extraction gets). (b) **Cards keep the request id**: `resolved_by_reply` first overwrote `resolution.communication_id` (the request the card was asked in) with the reply's id, so a second reply to the same clarification found nothing in scope; the reply is now stored as `reply_communication_id` / `reply_response_id`. (c) **Gemini request timeout 120 s + one retry on network failure** (`fetch failed`, timeout, reset), like the existing 429/5xx retry (TRD §19): during testing the connection dropped and calls hung for 18+ minutes, holding a stage `running`. (d) A clarification reply with no question in scope skips the questionnaire model calls (one run took 94 s for nothing it may write). (e) The "could not read the price" bullet asks for the price "in the currency and unit of your quotation" (OrientPack quotes USD), not INR.
- **2026-09-24 · P6 visual check against the prototype (1440 px).** Matching: header "Sync inbox" left of "Ask" (buyer only); Review with the clarification out — primary "Sync inbox — Westline Packaging replied", the waiting note in DESIGN's words, the inline email block (To / Reply-To / Subject, body, Send / Cancel); Overview after the reply — Westline out of "Needs you", 30/30 priced; Ledger "Bundle sizes 25 / 20 / 50 / 40 from the vendor's clarification reply". Kept different on purpose: (a) **asked cards leave the Open list** until the reply arrives (status `asked_vendor`, visible under Decided/All with the chip), so the Review count, the tab badge and the Overview agree; the prototype leaves the clicked card open. (b) Subject and body are **editable** in the email block (handoff 6.15); the prototype renders them as text. (c) Ask vendor lists the items as **tick boxes** above the email (untick → "Redraft for n items"). (d) Overview vendor rows carry a **"clarification sent" / "clarified" chip** (TRD §17.4 status chip; the prototype has none). (e) Vendor names in full ("Westline Packaging replied", as elsewhere since P4/P5). (f) The **vendor portal mailbox** has no prototype screen; built from DESIGN's card, email block and empty-state rules. (g) The Overview communications rows add attachment links, In-Reply-To links and a response link under DESIGN §3.4's timeline row.
- **2026-09-24 · Where scenarios live (P7, handoff §0 B).** TRD §17.11 describes a Scenarios screen; DESIGN §3.9 puts a scenario table on the **Award** tab and has no Scenarios tab. DESIGN is the visual spec, TRD wins on behaviour: the comparison (TRD §14.2 columns: annual total, vendors, lines allocated + unallocated, single-source, vs first, vs best single vendor, share per vendor as a one-colour stacked bar with the vendor names in the tooltip and legend — DESIGN §0.5/§2.15 allow one colour only), the per-line view with override / revert, and the **New scenario** rule builder are sections of the Award tab for both roles. No sixth buyer tab. The Award tab's count pill shows saved scenarios until a memo exists (prototype).
- **2026-09-24 · Approver's Decide page built (P7-T3, handoff §0 A).** PROGRESS "Open questions" had no answer from Sabarish, so Decide (DESIGN §3.8) is built as the approver's default tab: computed lead (the cheapest-qualified-per-line total from the same allocation engine as a saved scenario and Ask Q1), "See the grid" · "Award"/"Read the memo", the "Against the best single vendor" card (TRD §13.5 baseline = Ask Q2), the Ask box with Cost / Risk / Vendors suggestions, answer cards newest first and the "Earlier questions" list. Suggestions that name a vendor take the name from the data (the non-INR vendor, the vendor with per-kg conversions in the ledger), never fixed copy ("Why is the 5-ply from Anand Box Works so cheap?"). The Ask sheet's run logic moved into a shared `useAskRunner` hook.
- **2026-09-24 · Baseline rule (P7, TRD §13.5).** Best single vendor = the cheapest total among vendors who priced **every** line with a counted price (confirmed / inferred / reviewed; best guesses only when the scenario includes them), on the scenario's price basis, **among questionnaire-cleared vendors when the scenario is qualified-only** (a single-vendor award to a vendor who failed the questionnaire is no baseline). If nobody priced every line, the vendors with the most lines priced are compared over the lines each priced, and the note says so ("No qualified vendor priced all 30 lines; X priced 29, and the baseline covers those lines only") — TRD's literal "min over lines_priced" would let a vendor who priced one line win. On MER-0419 this is Sri Balaji ₹4,58,35,488 (= Ask Q2), saving ₹4,97,191 (1.08 %) against Q1. DESIGN §3.8's example text ("Kohinoor … with the three partition lines they don't make filled from the next cheapest") is a different rule; TRD wins, and Decide shows the same number as Q2.
- **2026-09-24 · Scenarios from an Ask answer (P7-T1, TRD §13.5).** (a) Saveable when the rows have `line_no`, a vendor column and one row per line; the planner names the column vendor / winning_vendor / cheapest_vendor run to run, so any column with "vendor" in its name whose values are all vendor names (or codes) of this RFx counts (runner-up/second columns excluded); a result with a `state` column (Q6, a list of unsure cells) is never an allocation. (b) Winners are copied; every price, runner-up, gap and total is recomputed from the comparison. The rule it implies is read from the SQL: qualified when it filters `cleared_questionnaire`, landed when it uses `landed_price`, best guesses when it reads `v_comparison_bestguess`; the reason per line says "cheapest qualified", "cheapest overall" or "as chosen by the query". The question and SQL are kept in `scenarios.rule` for the memo (no `query_id` column; `rule.query_id` holds it). (c) Both roles save from an answer; building by rule, overrides and delete are buyer-only (API 403 for the approver). (d) **P-SQL v4** (CLAUDE §9, v3 archived): "When the question allocates lines to vendors … return one row per line_no with the winning vendor, its price and annual_value_inr; put any total it is compared with in an extra column repeated on every row" — P4's Q5 answer was one total row and could not be saved. After v4: Q5 = 30 rows, ₹4,20,18,677, "better by ₹33,19,620"; Q1/Q3/Q4/Q7/Q8 unchanged from P4. Q2 now also comes back per line (answer unchanged, ₹4,97,191 vs ₹4,58,35,488); to keep its bar chart, `chartSpec` now charts total columns that repeat on every row (code change, not prompt). Seen once in the regression run: Q6 valued the 13 unsure cells at the average of other vendors' prices (₹1.41 cr) rather than at best guesses — planner variance, computed and shown with its SQL; not tuned.
- **2026-09-24 · Allocation details (P7-T1, TRD §14.1).** Ties go to the vendor whose name sorts first and the reason says so. Weighted score = w_price × (line minimum ÷ vendor price) + w_q × (mandatory questions with passes = true ÷ mandatory questions); the builder's slider sets price 0–100 % in 5 % steps. Grouped rules: first matching group wins; each group filters on one of ply / item type / plant / line numbers; uncovered lines are unallocated ("not covered by any group"). Best-guess landed price = guess + the vendor's freight (as `v_comparison_bestguess`). Single-source = an allocated line with no other eligible quote. `weighted` is built (cut-list item 1 not used).
- **2026-09-24 · Overrides (P7-T1).** Buyer only, reason required, and the vendor needs a counted price on that line; when the line's rule is qualified-only (its group's rule for grouped scenarios; for a query scenario, when the query's own pick on that line was a cleared vendor) the vendor must have cleared the questionnaire. The rule's allocation is kept in `scenario_lines.auto` (migration 0010) so **Revert** restores it exactly. After an override the runner-up is the cheapest other eligible quote, so a negative gap is the premium paid. If a memo was drafted from the scenario and the scenario changes afterwards, the memo is **stale**: Award says so and Approve is refused (409 STALE) until the buyer generates it again (memo_json keeps a fingerprint of line → vendor → price).
- **2026-09-24 · Send back and the award row (P7-T2, migration 0010).** `awards.status` gains `sent_back` with `sent_back_note` / `_by` / `_at` (TRD had only draft / approved; DESIGN §3.9 has Send back). One award row per RFx (unique index): Generate replaces a draft or a sent-back memo in place (clearing the note; the Timeline keeps it), never an approved one. The buyer sees the approver's note on the Award tab and can regenerate. Approve asks for a confirmation on the page ("Approving locks MER-…") because it cannot be undone.
- **2026-09-24 · Memo (P7-T2, TRD §14.3).** (a) **Font: IBM Plex Sans (400/600) + Plex Mono** WOFF files bundled in `src/lib/award/fonts/` with their OFL licence (the app's own typeface; Helvetica has no ₹). `fontkit` confirmed the ₹ glyph (U+20B9) in all three; the PDF read back with 130 ₹ signs as text. `next.config.ts` `outputFileTracingIncludes` ships the fonts with `/api/award/*` on Vercel. Hyphenation is off so numbers never split. (b) `memo_json` holds every number shown (allocation, totals, baseline, exclusions, validity, the Ledger tab's rows, unresolved cells with value at stake = best guess × annual qty ÷ 1000, overrides) and the P-MEMO narrative; the PDF and the HTML preview on the Award tab both render from it, and approval re-renders the PDF from it with the approver's signature. (c) P-MEMO is TRD §9.10 verbatim on the strong model (LOW thinking); inputs are pre-formatted Indian-style strings; the P4 number check applies to all five sections plus a check that the risks section names the unresolved-cell count; one retry naming the problems; leftovers are stored in `narrative_unverified` and printed in amber on the memo and the page (never silent). (d) Exclusions list vendors not cleared (with the grid's reason, e.g. "did not clear the questionnaire (Q6: No …)"), lines a vendor did not quote and lines pointing at prior pricing. (e) TRD §17.12 "PDF embed" → the memo is shown as HTML per DESIGN §2.16 plus **Download PDF** (`GET /api/export/memo/{award_id}`, both roles, served inline). (f) The approver's title comes from the role ("VP Procurement"), as the buyer's did in P5. (g) Scripts can't import `@react-pdf/renderer` under tsx (`@react-pdf/hyphenate/en-us` export error), so memo tests go through the running app's API.
- **2026-09-24 · Lock (P7-T2, PRD #33).** `src/lib/lock.ts` `assertOpen()` is called by every mutating route for the RFx: review action + bulk, `POST /api/responses`, seed-responses, run-all, stage, assign-vendor, email sync, vendor-reply, clarify, clarify/send, scenario save / override / delete, award generate / approve / send back → **409 LOCKED** "This RFx is awarded and locked — …". PATCH / copilot / issue were already draft-only (409 FROZEN). Ask stays open (read-only). Pages hide the actions via `isLocked()`: review actions ("Read-only — the RFx is awarded."), drawer "Open in queue", unmatched Map/Ignore, Sync inbox (header + Overview poll), Load seeded responses, Add response, Portal link, assign vendor, re-run stages, Save as scenario, New scenario, overrides, Generate memo. The lock note "Awarded — read-only. Memo approved by <name>." sits under the RFx header on every tab. The RFx list's **Annual value** shows the approved scenario's total (P0-T4's "—" until P7).
- **2026-09-24 · P7 visual check against the prototype (1440 px).** Matching: Decide (lead, "Against the best single vendor" card with the same 1.1 % · ₹5.0 L, Ask box, Cost / Risk / Vendors groups), Award before a memo, with a draft (buyer: Download PDF + "awaiting Priya"; approver: Download PDF · Send back · Approve) and approved (lead, memo signed). Kept different on purpose: (a) Award has a "Scenarios · n" card header with **New scenario** and extra TRD §14.2 columns (vs best single, Share) — the prototype has no rule builder; (b) the lock note also sits on Overview / Responses / Review / Award (handoff 7.15), not only above the grid; (c) **Sync inbox** is hidden once awarded (the prototype keeps it) because syncing would add responses to a locked RFx; (d) the Decide card has inner padding (the prototype's `card bd` puts the text on the border); (e) the approver's "Approve" asks for a confirmation on the page (irreversible); (f) the memo shows every TRD §14.3 part (totals, validity, ledger table, open items, rule + SQL), longer than the prototype's five short sections; (g) scenario names come from the question or the builder ("Q1 cheapest qualified per line"), the prototype's are fixed.
- **2026-09-24 · Pack size must be written by the vendor (found at the P7 checkpoint reload).** One `npm run pipeline:seed` gave 146/150: P-EXTRACT filled `pack_size` for Westline items 5/9/15/19 from the vendor's other items (its own note: "Pack size of 25 inferred from 5-ply cartons group description"), and normalise trusted it as vendor-stated → `confirmed` at the best-guess values instead of `ambiguous`. Fix in code, not the prompt (`statedPack()` in `normalise.ts`, unit-tested): a pack size counts as stated only when its number appears in the price unit, the quoted snippet or the item text; otherwise the existing best-guess path applies (cell ambiguous, ledger row "not applied"). Peer-pack guesses use the same test, so an invented pack can't seed another guess. Clarification replies keep their pack as stated (the vendor's answer to our question; P6). Re-running normalise on that same bad extraction → 150/150. Realistic MER-0417 was not reloaded (handoff: leave it unchanged); its stored eval stays 150/150 and the next realistic reload (P8-T2) runs through the same check.
- **2026-09-24 · P8 Settings, Eval and Model calls on one page.** TRD §17.13–17.15 describe three screens (Settings, Eval, Logs); DESIGN §3.10 puts all three on one page behind the rail's gear. Built **one `/settings` page** (DESIGN is the visual spec) with the sections anchored `#eval` and `#model-calls`, keeping all TRD content: Eval has the RFx select, Run eval, verdict and vendor filters and row → provenance drawer; Model calls has rfx / purpose / provider / result filters. The three TRD routes exist: `GET/PUT /api/settings`, `POST /api/eval/run`, `GET /api/logs`. Buyer/admin only (DESIGN §4): Priya has no gear, `/settings` sends her to `/rfx`, and the APIs return 403. With `loading.tsx` the page streams, so her redirect is a client-side replace (HTTP 200 carrying `NEXT_REDIRECT;replace;/rfx`); no settings data is sent (checked with curl).
- **2026-09-24 · P8 settings behaviour.** (a) **Global, next run only.** Settings apply to every RFx and take effect on the *next* stage run; cells already written keep their state and conversion chain (thresholds, FX, freight, discount, provider). The UI says so on each card and in every save toast. (b) **Email transport card:** Mock selected; Gmail disabled "not in this build" (DECISIONS "Live Gmail out of scope"); Resend disabled "not configured"; `PUT email_mode` accepts only `mock`. The TRD's "vendor demo addresses map" is **not built**: it only serves Gmail mode. (c) **Jev only without `OPENROUTER_API_KEY`:** the radio is disabled with "needs an OpenRouter key" rather than offered and silently falling back; Auto shows the amber "no key · Gemini" chip. With no key, Auto and Gemini-only both call Gemini, and Model calls says so. (d) **Thresholds** are two number inputs (not sliders), validated 0 < review < act ≤ 1. (e) **FX:** a change rewrites nothing; the next normalise run of a vendor in that currency writes a new `fx_rate` ledger row (it replaces that reply's own system row, as every normalise re-run does) and converts at the new rate. Tested on a throwaway RFx (USD 83.15 → 84 dated 24 Sep: cells unchanged until re-run; after it, OrientPack L1 69,675.54 → 70,387.80 = ÷83.15×84, ledger "USD→INR at 84 (manual, 2026-09-24)"); restored 83.15. Rates can be edited, added and removed (a mistyped currency could otherwise never be taken out). (f) **Landed cost & discounts** shows freight default (editable), total-level discounts Show gross / Apply to every line, and cost of money "not in this build" (cut list 2). **Include tax is not shown**: the normalise stage never passes `include_tax`, so a toggle would do nothing. (g) Every save is an audit event `settings.changed` with `{key, before, after}` and `rfx_id` null, listed in words under **Recent changes** on the Settings page (settings belong to no RFx, so they don't appear on any RFx Timeline).
- **2026-09-24 · Discount default "net" implemented (TRD §11.4).** Before P8, `discount_default` only changed the ledger wording. Now, with **net**, a vendor's total-level discount (not the Kohinoor-style "printed net of a discount we won't earn", which keeps its gross-up) is taken off every line on the next normalise run: chain step `total_discount` ("Less 3% total discount (Settings: apply to every line)", basis "assumption · discount"), the cell becomes `inferred`, and the ledger row says "applied to every line — Settings: net". Tested on a throwaway RFx: Balaji L1 70,610 → 68,491.70 (×0.97), back to 70,610 with gross. Default stays gross, so seed evals are unchanged.
- **2026-09-24 · Eval page choices.** (a) **Eligible RFx** = those whose replies were loaded from a seed set (`responses.source = 'seed'`); the set name comes from the latest `seed.responses_loaded` audit event. `POST /api/eval/run` on any other RFx → 422 `NO_GOLD_KEY` naming the eligible ones. (b) **Allowed on an awarded (locked) RFx:** it only reads the grid and writes `eval_runs`, so it is not blocked by the lock. (c) Migration `0011_eval_per_question.sql` adds `eval_runs.per_question` so the page lists questionnaire mismatches from the stored run (runs from before P8 say "Run eval to list mismatches"), plus indexes for newest-first reads. (d) The page shows the latest stored run without re-running and says "judged on the current state — n cells reviewed by the buyer" (TRD §18 "page shows which").
- **2026-09-24 · Model calls provider label.** The Provider column reads "LLM-estimated (Gemini)" / "measured (Jev)" (handoff P8 8.14), besides the drawer and the questionnaire tooltip (DESIGN §0.6 names only the drawer; the handoff adds these two places). Failed calls show their error on hover and on click (e.g. the P7 `504 DEADLINE_EXCEEDED` extract calls).
- **2026-09-24 · Review cards "Change in settings".** FX, freight and discount assumption cards now carry a quiet "Change in settings" link (DESIGN §3.6; deferred from P3 until the page existed). The Overview Transport row gets its "change" link (DESIGN §3.4; DECISIONS P5-T4 d).
- **2026-09-24 · Map reads the extractor's notes (found at the P8-T2 realistic reload).** The MER-0417 reload gave 147/150 twice in a row (different wrong patterns): Balaji's realistic sheet rows 36–38 are three "Corr. Sheet" items whose ply and B.F. appear only as bare columns ("E36=1200x800 F36=5 G36=32"), and the fast model mapped 5-ply 1200x800 and 3-ply 1200x800 to the wrong lines at p = 1.00 (the Gemini emulation's usual certainty, so nothing flagged it). Extraction was right (SBP-1023 38,610 · 1024 26,770 · 1025 23,840) and put the labelled fields in `notes` ("Size: 1200x800 mm, Ply: 5, B.F.: 32"), which the map stage never read. Fix in code, not a prompt: the shortlist scores ply/size/type on the notes too, and the item text sent to `decide()` includes `notes:` (unit test with the three real rows). After the fix: MER-0417 150/150 on two consecutive map re-runs; clean MER-0419 `pipeline:seed` 150/150, questionnaire 50/50. The P-DECIDE prompt is unchanged.
- **2026-09-24 · P-SQL v5 (one prompt round, CLAUDE §9, found asking Q7 on MER-0417).** Q7 ("OrientPack's USD conversion … rupee moves 3%") returned ₹13,99,78,512 twice: the planner joined `v_assumptions` to `v_comparison` on vendor, and after the review MER-0417's OrientPack has three ledger rows (FX, freight, the line-14 exclusion), so every line was counted three times (P4's MER-0419 answer was ₹4,66,59,502). v5 adds one hard rule: never join `v_assumptions` into a query that sums, totals or ranks prices; read the assumption in a separate subquery/CTE filtered by kind. v4 archived in `prompts/archive/P-SQL_v4.txt`. Regression on the throwaway RFx: Q1 ₹4,53,39,035 (30 rows), Q2 saving ₹4,96,453, Q5 ₹4,20,18,677 (30 rows) unchanged; Q7 now a CTE on `kind = 'fx_rate'`, base = the vendor's own total. Q7's two wrong answers stay in MER-0417's Ask history (append-only).
- **2026-09-24 · Answer-card number rules (P8 polish, found in Q7's table).** A column named like `annual_value_deprec_3pct` (rupees under a 3 % scenario) showed as "31,00,283%", and `original_value` of a USD quote showed as "₹838". Now a `pct` column counts as a percentage only when no digit names a scenario (`gap_pct` yes, `…_3pct` no), and `original_*` columns are plain numbers (the currency is its own column). Same rule in the answer Excel export.
- **2026-09-24 · No supplier terms → no terms decision (found in TRD §22.9 on production).** A reply holding only Balaji's ISO certificate was classified `supporting` (p 1.00, no crash), but the terms decision — asked with an empty "SUPPLIER TERMS" block — answered "refers to earlier pricing" p ≥ 0.5, so normalise filled all 30 Balaji cells with `references_prior` and raised a "same as before" card. Fix at the root in `decideTerms`: when the reply carries no supplier terms and no item remarks, no model is called and every flag is false (provider "none"). The certificate reply then leaves the vendor's lines not quoted and raises no prior-pricing card; the questionnaire-missing card stays (true: no answers came). Replies with any terms text are decided as before.
- **2026-09-24 · Co-pilot prose can claim a change its patch didn't make (found in TRD §22.2 on production).** Asked for a deadline, a questionnaire and vendors in one message, the co-pilot replied "set up a 10-question questionnaire" but its `rfx_patch` had no `questions_add`; the patch box under the message (the applied changes) showed only Terms · Vendors and the Questionnaire tab stayed 0. Asking again added 10 questions. Not tuned in P8 (no §22 item failed — Issue stays disabled until a questionnaire exists); listed under Known issues.
- **2026-09-24 · Empty states (P8-T3).** Every screen that can be empty says so in one bold and one plain sentence (DESIGN §5), incl. the Questionnaire tab (no answers yet — the grid of "not read" cells is no longer shown), Decide ("No prices yet." + why), Review ("The queue is clear." / "Nothing to review."), and the RFx list. Kept: Responses' empty state offers "Load seeded responses" plus the quiet "Realistic set" (two variants of one action, P5), not a second action.
- **2026-09-24 · Map fix revised: spreadsheet rows get their column headers (supersedes part of "Map reads the extractor's notes").** Two regressions showed the first fix was too blunt: (a) with the notes read by the shortlist, Westline's "items 5" (notes "5-ply carton") got five look-alike 5-ply cartons as options and the fast model answered "none of these" — clean MER-0419 148/150, once 138/150 when a whole batch went to "none"; an A/B (notes out of the decision text: still 148; shortlist change reverted: 150 twice) pinned it on the shortlist; (b) a realistic run whose extraction left `notes` empty still mis-mapped Balaji's bare "E37=1000x700 F37=5 G37=28" row. Now: (1) the map stage renders a spreadsheet item's row with its column headers — the nearest earlier row with 3+ cells and no number ("Sl=24 · Our Code=SBP-1024 · Particulars=Corr. Sheet · Size (LxBxH) mm=1000x700 · Ply=5 · B.F.=28 …"), in memory only (`labelSheetRow`, unit-tested); (2) the shortlist reads notes only when the item cites no line number; (3) notes stay in the decision text (neutral in the A/B). Results: realistic 150/150 twice (MER-0421), clean 150/150 twice (MER-0419), all from the map stage on the same extractions that failed.
- **2026-09-24 · Prior pricing needs the supplier's words (TRD §22.9 follow-up).** The certificate-only reply still carried an extractor note ("No quotation terms found."), so the empty-terms shortcut didn't fire and the model again said "refers to earlier pricing". `decideTerms` now sets prior pricing to 0 unless the reply has a prior-pricing phrase, item remarks, or notes that mention earlier / last year's / previous prices. Checked on the production throwaway: the certificate reply now leaves Balaji's 30 lines `not_quoted` with no prior-pricing card; seed sets unchanged (150/150 both).
- **2026-09-24 · "Questionnaire not returned" once per vendor.** A second reply from the same vendor (the IT quote in TRD §22.10) raised a duplicate card; the questionnaire stage now skips it when another reply from that vendor already has one open.
- **2026-09-24 · P8 visual check against the prototype (1440 px).** Settings matches the prototype's layout (h1 + sub-line, 2-column cards, "Eval — seed set" with three stat cards and the per-cell table, "Model calls" table). Kept different on purpose: (a) the cards are editable (threshold and freight inputs with Save, FX Edit / Add currency / Remove, discount segmented control) where the prototype shows static values; (b) Gmail reads "not in this build" (disabled) instead of "credentials found" — DECISIONS "Live Gmail out of scope"; (c) Include tax is not shown (not implemented), cost of money reads "not in this build"; (d) a "Recent changes" card under the grid (settings have no RFx timeline); (e) Eval adds the RFx select, Run eval, verdict / vendor filters, a verdict legend and the questionnaire card; state chips use the grid's state colours instead of mono state keys; (f) Model calls adds RFx and Result columns and filters. Polish-pass screens (Review, Award, Decide, RFx list) changed only in copy, toasts and phone layout; their 1440 px layout is the P7-checked one.
- **2026-09-25 · Cost of every model call (Sabarish's request after P8; not in the PRD/TRD).** Answers given before building: prices from Google's page, editable in the database; every token type Gemini reports; database only (no screen); USD stored plus ₹ at the call-time Settings rate; calls from before this left empty; one view per RFx and purpose. Built: migration `0012_model_costs.sql` — table `model_prices` (per model and period, USD per 1M tokens: input, cached input, output incl. thinking; source URL + date checked; seeded from https://ai.google.dev/gemini-api/docs/pricing read 2026-09-25, page updated 2026-09-24: gemini-3.5-flash-lite $0.30 / $0.03 / $2.50; gemini-3.8-flash $0.75 / $0.075 / $3.75 through 2026-12-31, then $1.50 / $0.15 / $7.50 from 2027-01-01; effective_from = the day checked because the page doesn't say since when); new `model_calls` columns input_text / image / document / audio / video tokens, cached_input, tool_use_prompt, thinking, total tokens, usage_raw (Gemini's usageMetadata as returned), price_id, cost_usd, usd_inr_rate, cost_inr, cost_note; view `v_cost_by_rfx_purpose`. Formula (`src/lib/cost.ts`, unit-tested): USD = ((input − cached + tool-use) × input price + cached × cached price + (output + thinking) × output price) ÷ 1,000,000 — input includes cached (Gemini API reference: promptTokenCount "includes the number of tokens in the cached content"); thinking is separate from output (checked on 38 real calls: input + output + thinking = totalTokenCount every time). Instruction (system) tokens are inside input — Gemini doesn't report them separately. Gemini reports PDF pages under IMAGE, not DOCUMENT, in these runs. Failed calls: no usage returned → cost null with a note. Jev/OpenRouter calls have no price row → cost null (Jev isn't called without a key). Before this, thinking tokens were not recorded at all, so earlier token totals understate output. Measured: one full seed run of MER-0419 (5 replies, 46 calls) = $0.2307 ≈ ₹19.18, extraction $0.16 of it.
- **2026-09-25 · Sign-in layout, footer, and Privacy / Terms / Help pages (Sabarish's request; not in the PRD/TRD).** DESIGN §3.1 specified a 20ch/30px headline, a 360–460px right column with the form 44px from the top, a 640px left column, and a separate footer hint under each column. At 1440px that left a ~250px empty band between the columns (itself a §0.8 "lone paragraph with empty space beside it"), a three-line headline and a cramped form. Now: columns `1fr | 400–520px`, left content up to 820px, headline 34px in two lines, left content and form both vertically centred, form 400px wide, one full-width footer (© line · Privacy · Terms · Help · "Private to Meridian Foods…"). The sample mini-grid now has a unit ("₹ per 1,000 pcs"), a legend above it (lowest on the line · needs a look · not quoted) in place of the caption below: the old grid's teal edges read as stray lines without it. A source callout on one cell was tried (dark tooltip, then a light card) and removed at Sabarish's request. New public pages `/privacy`, `/terms`, `/help` (`src/app/(public)/`, no session needed). Their content is placeholder text written for the fictional Meridian Foods (DPDP Act framing, retention periods, contacts @meridianfoods.in, Grievance Officer name and address are invented), not reviewed by anyone legal. Help describes real app behaviour where checked (J/K/C, reply tags, cell states, 7-day session); support hours and ext. 4400 are invented.
- **2026-09-25 · App frame and RFx list redesign (Sabarish's feedback on the RFx list screenshot, then live while it was built; overrides DESIGN §0.2, §2.1, §2.2, §3.2).** Feedback: the 56px icon rail was narrow and unlabelled; the right half of the screen was empty (content capped at 1180px on a grey page); avatar + "Name · Role" in the top bar looked odd and the initials weren't clickable; ⌘K did nothing; the count sentence ("Four events. One needs review…") said nothing useful; no search or filters. Answers: no Delete draft for now; sidebar expanded by default; commit and deploy. Built: (a) **Sidebar** 200px with labels, groups *Sourcing* (All RFx · New RFx · Needs review, with counts) and *Admin* (Settings · Evaluation · Model calls → `/settings`, `#eval`, `#model-calls`); Priya sees All RFx and Needs review only. A small round chevron button floating on the sidebar's right edge near the top collapses it to 56px icons (a worded "Collapse" button and a top-bar panel icon were tried and rejected by Sabarish); the choice is the cookie `ql-sidebar`, read on the server so there is no flash. Profile block at the foot (avatar, name, role) opens Sign out. ≤ 900px the sidebar is a drawer behind a menu button (before, the rail was hidden and phones had no navigation). (b) **Top bar:** brand (links to the list), org, and a sun/moon **theme toggle on the right** (Sabarish found it missing when it sat inside the profile menu); ⌘K removed (no command palette exists). The breadcrumb went into the RFx header ("Sourcing events / MER-0419", replacing the "MER-0419 ▾" pill): the header already knows the code, the top bar doesn't. (c) **Width:** `.page` has no max-width; `.page.read` keeps 1180px for Overview, Decide and Award. (d) **RFx list:** h1 "Sourcing events", a purpose line instead of counts (approver line unchanged); status tabs with counts (All · Draft · Out for quotes = issued+receiving · Reviewing · Awarded; Closed only when one exists); search on code and title; **Created** and **Updated** date-range filters, each one control (presets Last 7 days / Last 30 days / This month + From / To native date inputs in a popover; inclusive IST days); sortable Code, Responses, Deadline, Awarded value, Created, Updated; "Showing n of m · Clear filters"; empty-match state. Filters live in the URL (`?q=&status=&cfrom=&cto=&ufrom=&uto=&sort=&dir=`) via `history.replaceState` (no server round trip); filtering runs in the browser (fine at this size; move it server-side past a few hundred RFx). **Columns, in order:** Code · Title · Lines · Responses · Deadline · Awarded value · Created · Updated · Next step · Status · view (eye icon; the whole row stays clickable too). Next step: draft Finish lines / Ready to issue; out for quotes n to review / Waiting on n of m; reviewing n to review (amber) / Ready to award; awarded Approved dd Mon. Deadline: Due in n days (amber ≤ 3 days while out for quotes), Closed once passed or awarded. Created / Updated show the date ("24 Sep 2026"), time on hover, instead of "yesterday". A default-titled draft reads "Untitled draft" in grey; "Annual value" renamed Awarded value with "Not awarded" instead of "—". Rows 14px padding (roomier than the grid tables). No stat tiles (DESIGN §0.8). (e) **Category removed from every screen** (list column, filter and search; RFx header; New RFx Terms tab): it is free text (typed or set by the co-pilot, default "Uncategorised"), so a dropdown of values made no sense. The `rfx.category` column stays (NOT NULL in TRD §6) and the co-pilot still reads it to suggest questionnaire questions. DESIGN.md not edited.
- **2026-09-25 · One Excel quote form replaces the line sheet + questionnaire PDF (Sabarish's question on the Issue dialog; overrides TRD §16 issue attachments and the P-DISPATCH attachment sentence in §9.3).** The TRD's questionnaire PDF (`@react-pdf/renderer`, drawn answer boxes, no form fields) couldn't be typed into; suppliers had to print and scan or answer in the email. Now every dispatch email carries one file, `{code}_Quote_Form.xlsx` (`quoteFormXlsx` in `src/lib/dispatch-docs.tsx`), with tabs **Line Items** (as before, plus "Your price" / "Remarks") and **Questionnaire** (Q · Question · Answer type · Mandatory · Your answer · Remarks; yes/no answers have a Yes/No dropdown). Each tab opens with the RFx facts in a shaded, bordered block (merged across the table width, terms on labelled lines) above a spacer row and the table, instead of three loose text rows. P-DISPATCH now says "one Excel quote form is attached, with the line items on its first tab and the supplier questionnaire on its "Questionnaire" tab"; the rest of the prompt is verbatim. The pipeline needs no change: the xlsx preprocessor reads every sheet, the questionnaire stage already reads `quotation` files as well as `questionnaire` ones, and classify calls a file with prices a quotation. The Issue dialog gets **View** (the in-app file viewer, now rendering merged rows as one full-width tinted cell and bolding the first table row after them) and **Download**, both from `GET /api/rfx/{id}/issue/preview`, which builds the form from the current draft and stores nothing. RFx issued before this (MER-0417, MER-0419, MER-0422) keep their two old attachments in the outbox. Not re-run: `pipeline:seed` (the seed replies are vendor files, not our form).
- **2026-09-25 · Quote form input cells are yellow (follow-up to "One Excel quote form").** The cells the supplier fills in (Line Items "Your price" and "Remarks", Questionnaire "Your answer" and "Remarks") have a light yellow fill (#FFF6CC), the usual RFQ-sheet convention for "type here"; both info blocks say "yellow cells" / "yellow column". Excel only: the in-app viewer doesn't render cell fills.
