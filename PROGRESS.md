# Progress
## Current: Phase P8 in progress (Settings / Eval / Model calls, completed MER-0417, polish, TRD §22 on production) — P7 ✅
## Deploy URL: https://quotelens-seven.vercel.app
## Eval (latest): clean MER-0419 **150/150**, questionnaire **50/50** (P7 final reload, 2026-09-24; also 150/150 on the P7 throwaway RFx locally and on Vercel) · realistic MER-0417 **150/150**, questionnaire 43/50 (unchanged; the 7 misses = OrientPack sent no questionnaire)
## Done
- [x] P0-T1 Scaffold — Next 16.3.6 + Tailwind v4 + shadcn (radix-nova, sonner instead of toast), all listed deps, DESIGN tokens in globals.css, IBM Plex via next/font, `.env.example`, vitest config. `npm run dev` shows a page.
- [x] P0-T2 Supabase — migrations 0001 (TRD §6.1–6.20 verbatim + RLS lock-down), 0002 (§6.21 views + `run_readonly_query`), 0003 (buckets); validated on embedded Postgres; `src/lib/db.ts`, `src/types/db.ts`. Applied 2026-09-23 with `npm run db:migrate`; `db:check`: rfx count ran, 4 private buckets, `run_readonly_query` OK. Publishable key blocked from `users` and views (42501).
- [x] P0-T3 Auth — iron-session cookie, `POST /api/auth/login` + `/logout`, `requireUser()`/`requireApiUser()`, sign-in at `/` per DESIGN §3.1 (demo rows prefill email). Verified: logged-out `/rfx` → 307 to `/`; bad body → 400 `{error,code}`. Real login verified for Sujit and Priya; wrong password → 401; signed-in `/` → `/rfx`.
- [x] P0-T4 Seed — `scripts/seed.ts` (users, 5 vendors, MER-0417/0418/0419 with 30 lines, 10 questions, 5 invited vendors + reply tags, settings defaults, dataset → bucket `seed`). App shell (top bar, rail, theme toggle, sign-out) and RFx list `/rfx` per DESIGN §2.1–2.2, §3.2. Build/lint/types/tests green. Ran twice (idempotent): 2 users, 5 vendors, 3 RFx, 90 lines, 30 questions, 15 invitations, 7 settings, 34 dataset files in bucket `seed`. `/rfx` lists MER-0417/0418/0419 for both users; New RFx button buyer-only.
- [x] P0-T5 Deploy — GitHub `Suj1234/quotelens`, Vercel Hobby project `quotelens`, functions pinned to `hnd1` (Tokyo, next to Supabase ap-northeast-1). Verified on production: sign-in renders, logged-out `/rfx` → `/`, wrong password 401, Sujit and Priya sign in and see MER-0417/0418/0419; `x-vercel-id` shows `hnd1`.
- [x] P0-T6 Model check — key works (32 Gemini models visible). FAST=`gemini-3.5-flash-lite`, STRONG=`gemini-3.8-flash`; both return valid schema JSON on text, the Kohinoor PDF and the OrientPack photo (`npm run check:models`). Details in DECISIONS.md.
- [x] P1-T1 Storage helpers — `src/lib/storage.ts` put/get/list/signedUrl with TRD §5 paths; round-trip + signed-URL fetch verified against Supabase.
- [x] P1-T2 Preprocessors — `src/lib/preprocess/` xlsx (cell refs, text numbers verbatim, comments, hidden rows marked, empty sheets dropped, 2,000-cell cap), docx (`[p N]`, `[table t row r]`), email/txt/eml (quoted blocks, `On … wrote:`, `-- ` and `Sent from my` removed; `[l N]`), image (EXIF rotate, ≤2000 px, normalise), pdf (page count, >20 pages split). 13 unit tests on the realistic seed files green.
- [x] P1-T3 Gemini client — `src/lib/ai/gemini.ts` `generateJSON` (Zod → `responseJsonSchema`, validate, one retry with the validation error, one retry on 429/5xx after 2 s, `MODEL_INVALID`/`MODEL_ERROR`), `generateText`, `inlineFile`; `src/lib/log.ts` `logModelCall`/`audit`. Live: strong model read the Kohinoor PDF (vendor, 2 pages, the * footnote verbatim); calls land in `model_calls`. 4 unit tests (mocked provider) green.
- [x] P1-T4 Response intake — `src/lib/responses.ts` (`createResponse`: inbound mock communication → response → each file raw to bucket + row → derived text/image/page count), `POST /api/responses` (multipart), `POST /api/rfx/{id}/seed-responses[?set=realistic]` (replaces earlier seed responses and their outputs; leaves other sources alone). MER-0419 has 5 seeded responses: Balaji xlsx+cert, Kohinoor pdf+profile, Westline docx+profile, OrientPack photo+questionnaire, Anand email text.
- [x] P1-T5 Classify — decision layer interface `src/lib/ai/decision/index.ts` + Gemini emulation provider (P-DECIDE §10.4, per-option probabilities, renormalise, margin confidence); `src/lib/pipeline/classify.ts` (one `decide()` per response over all files + email body; P-CAPTION for PDFs/images; top < 0.50 → unknown; `not_a_quote` review items). MER-0419: Balaji xlsx/Kohinoor pdf/Westline docx/OrientPack photo/Anand email → quotation (p 1.00); certificate + profiles → supporting; OrientPack questionnaire PDF → questionnaire.
- [x] P1-T6 Extract — `src/lib/pipeline/extract.ts` (P-EXTRACT verbatim + input-format note; Zod `ExtractionResult`; text/PDF/image sources; >60k-char chunking; >20-page PDF split; terms merged file > email with disagreements noted). Response Detail page `/rfx/{id}/responses/{rid}` (files + kind chips, terms read, extracted items with price/unit as written, pack, where, read bar) and Responses tab `/rfx/{id}/responses` with Load seeded responses (clean/realistic).
  **Extraction counts on MER-0419 (clean set):** Balaji 30/30 priced (cells G8…G37, 3% conditional total discount) · Kohinoor 27/27 (validity 30 d, 2.5% early-payment footnote with ÷0.975 rule) · Westline 30/30 (items 5, 9, 15, 19 have no pack size; the other 26 do) · OrientPack 30 items, 29 priced — line 14 `unit_price null`, `raw_confidence 0.2` (thumb shadow), USD, FOB Chennai · Anand 2 rate lines (₹42/kg, ₹38/kg) + 1 unpriced "rest same as last year" item; terms `references_prior_pricing = true`, freight extra.
  **Realistic set (loaded on MER-0417, classify+extract):** Balaji 30/30 — all gold prices found incl. text cells `84,080/-` and `41810 (revised)` · Kohinoor 27/27 — hand-corrected item 17 read as 3,090 (not struck 3,210) · Westline 30/30 · OrientPack 29/30 priced, line 14 null @ 0.2 · Anand quoted RFx thread stripped, 2 rates + 1 prior-pricing line.
  **Production check:** run-all on Vercel for Westline — classify 4.2 s, extract 27.0 s, 30/30 items, NDJSON streamed; 32 s wall.
- [x] P1-T7 Pipeline runner — `src/lib/pipeline/run.ts` (`runStage`: status running→done/error, `stage_errors`, `summary[stage]` + timings, `audit_events pipeline.stage`; `runAll` chain stops at first error), `POST /api/responses/{id}/stage/{stage}`, `POST /api/responses/{id}/run-all` (NDJSON stream), pipeline strip with Run all / Retry per stage and elapsed time. Verified in the browser: Run all on Kohinoor → classify 6.1 s, extract 24.4 s; stages not built yet stay `pending`.
- [x] P2-T1 Decision layer — `providers/jev.ts` (OpenRouter → Jev adapter: boolean→noul, choice→choice with o0..oN keys, score→score; 8 s timeout, retry on 429/5xx, two documented endpoints) + routing in `decide()` (`auto` → Jev only when `OPENROUTER_API_KEY` is set; `jev` same; `gemini` never calls Jev; any Jev error → Gemini, failure logged). No key yet → the adapter stays inert. 4 unit tests (mocked SDK/fetch): choice probabilities sum to 1, Jev mapping, fallback, gemini-only.
- [x] P2-T2 Candidate shortlist — `src/lib/pipeline/shortlist.ts` (pure): size ±5% (TRD regex, also `×` and spaces), ply, item type words, Jaccard, our-SKU substring, explicit `item N` (+0.5), bare row number (+0.15 weak prior), ranges `items 1 to 12`. Reads the description/SKU first and the location snippet second (Westline's "items 5 and 9" sentence). 6 tests on the real line sheet green.
- [x] P2-T3 Map stage — `src/lib/pipeline/map.ts`: shortlist top 5 + `none_of_these` → one `decide()` choice per item, 10 items per call (parallel batches, neighbour items as context); range items ("items 1 to 12") → one boolean "this offer covers lines N–M" and one mapping entry per line; thresholds from settings (≥ act mapped; review–act mapped + `low_confidence_read` "Mapping needs a look"; < review or none → `unmatched_items` + `unmapped_item`); conflict rule (higher p keeps the line, loser → `conflict` review item). Mapping stored in `responses.summary.map.mapping` (stage summaries live under the stage key). Review items tagged `evidence.stage` so a re-run replaces only its own open items (`src/lib/pipeline/reviews.ts`, TRD §12.1 dedupe key). **MER-0419: 147/147 items on the right line** (Balaji 30, Kohinoor 27, Westline 30 incl. 5/9/15/19, OrientPack 30, Anand 3 items → 30 lines via ranges), all p = 1.00 (Gemini emulation); ~4 s per response.
- [x] P2-T4 Normalise rules — `src/lib/normalise/units.ts` (TRD §11.1 dictionary, widened for "1000 pcs" without "per" and "/ 1000"; pack from "of 25"; amounts with Indian grouping and `/-`; factor to per 1000 pcs), `fx.ts` `fxRate` (missing rate → null, never a silent 1), `price.ts` (line discount, gross-up, landed). 6 tests incl. all CLAUDE.md cases and gold values for Anand/OrientPack/Kohinoor line 1.
- [x] P2-T5 Normalise stage — `src/lib/pipeline/normalise.ts` + `terms.ts`. One `decide()` per response on the terms text (TRD §10.2 flags set + two discount questions: "rates are printed net of the discount" and "buyer's payment terms miss the condition") drives prior pricing, freight and discount treatment — no extracted boolean is trusted. Per mapped item: line discount → gross-up (when net-of-discount and condition missed) → unit (pack stated / RFx spec / else ambiguous with a best guess from the vendor's other items of the same ply+type) or per-kg × our weight → FX from Settings. States per TRD §11.5; `not_quoted` / `references_prior` fill; landed = unit + freight (Settings `freight_default_inr_per_1000` = 180, new key, or the vendor's own assumption). Ledger rows for every non-vendor basis; review items per §8.4. Cell ids stay stable across re-runs; cells a buyer reviewed/excluded are never overwritten. Response Detail gets the Mapped column (DESIGN §3.5 chips).
  **MER-0419 first pass:** states exactly the README §5 table (Balaji 30 confirmed · Kohinoor 27 inferred + 3 not quoted · Westline 26 confirmed + 4 ambiguous · OrientPack 29 inferred + 1 low confidence · Anand 22 inferred + 8 references_prior); a scratch comparison with gold.json: **150/150 state + value within 1 %**, Westline best guesses 27,960 / 54,960 / 14,080 / 11,580 = gold. (The eval runner proper lands in P2-T8.)
- [x] P2-T6 Questionnaire stage — `src/lib/pipeline/questionnaire.ts`: one P-QA-EXTRACT call per response over questionnaire + quotation files (text, PDF, image) + email body (text capped 12,000, questionnaire files first), then one `decide()` per vendor — yes/no as a choice (yes / no / unclear or pending / not answered), number/text as "explicitly stated" booleans. `passes` vs `disqualify_if` (`no`, `yes`, `lt/lte/gt/gte:N`); `questionnaire_ambiguous` review items; reviewed answers never overwritten. Migration `0004_cleared_questionnaire.sql` fixes `v_vendor_status.cleared_questionnaire` (the §6.21 version counted ambiguous/missing as a pass): false if any fail or a mandatory one is missing, null while any is ambiguous or none read, else true.
  **MER-0419:** cleared = balaji ✓ kohinoor ✓ orientpack ✓ · westline ✗ (Q6 BRC = No) · anand null (Q6 pending). 48/50 answers match gold; both misses are Anand judgement calls (see Open questions). 10–23 s per response.
- [x] P2-T7 Flags stage — `src/lib/pipeline/flags.ts`: flags from the decided terms (reused from normalise, decided afresh if normalise didn't run) + deterministic checks: `references_prior_pricing`, `freight_excluded`, `validity_short`, `currency_not_inr`, `total_discount_present`, `partial_quote` (any `not_quoted` cell). Writes the decision into the interpreted `response_terms` columns (so the realistic-Balaji "Revised from 13200" comment no longer reads as prior pricing), review items `validity_short` / `missing_line` (+ deduped freight/prior/discount), `rfx_vendors.status` invited → responded, RFx issued → receiving → reviewing when nobody is left waiting. **MER-0419:** Balaji [total_discount_present] · Kohinoor [validity_short, total_discount_present, partial_quote] · Westline [] · OrientPack [freight_excluded, currency_not_inr] · Anand [references_prior_pricing, freight_excluded]; RFx → reviewing. ~1.3 s.
- [x] P2-T8 Eval runner — `src/lib/eval/run.ts` (TRD §18 + README §5: correct / flagged_ok incl. Kohinoor alt with a discount_treatment item / wrong / missing; ambiguous best guess must be within tolerance; OrientPack 14 legible read accepted; reviewed cells judged on the approved value), gold key read from bucket `seed` (works on Vercel for the P8 page), writes `eval_runs`. `scripts/run-eval.ts` (`--rfx`), `scripts/run-seed-pipeline.ts` (`--set clean|realistic`, `--rfx`, `--from STAGE` to skip reloading). Verdict unit test. Fixed the seed reload's delete order (review items / ledger before cells).
  **Full `npm run pipeline:seed` from scratch** — clean (MER-0419): 150/150, questionnaire 48–49/50, 83 s wall for 5 responses in parallel; slowest single stage = Balaji extract 33 s. Realistic (MER-0417): 150/150, questionnaire 41/50 (OrientPack's realistic reply has no questionnaire PDF → 7 × missing is correct behaviour; Anand Q8/Q9 as in clean).
- [x] **P2 checkpoint** — 37 tests, lint, build green; pushed `13a1ff0`, Vercel redeployed. **Production check:** Westline on MER-0419 → Re-run all stages: classify 2.9 s · extract 22.0 s · map 2.7 s · normalise 2.0 s · questionnaire 5.5 s · flags done; Mapped column shows L5/L9 with the amber `unit?` chip (DESIGN §3.5), compared with the prototype at 1440 px.
- [x] **P2 review follow-up (Sabarish's answers, 2026-09-24)** — ranges read at the end worse for the buyer (gold Anand Q8 → 12); caveated yes = yes; `questionnaire_missing` review card (migration 0005) when mandatory answers are missing; related-figure misreads blocked (P-QA-EXTRACT v2 + decision state leads with the supplier's own text; v1 archived in `prompts/archive/`). Clean questionnaire 48 → **50/50** (stable over two runs); realistic 41 → 42–43/50 and no wrong answers left (Q4 misread gone). Prices unchanged at 150/150 on both sets.
- [x] P3-T1 Comparison grid — `src/lib/comparison.ts` (grid model: lines, vendor headers with coverage / freight / currency / validity-short / cleared ✓✗? with reason / yearly total, cells with state, unit, landed, best guess, as-written, one-line source tooltip from the conversion chain or the read location), `GET /api/rfx/{id}/comparison`, `/rfx/{id}/comparison` with `PricesGrid` (DESIGN §2.6–2.8: sticky first column and headers, Unit price / Landed cost / As written, show disqualified vendors, teal edge on the lowest eligible cell, legend with counts), Comparison tab in the RFx header, `inrShort` (₹4.39 cr / ₹38.2 L) + test. Checked against the prototype at 1440 px on MER-0419: headers, cells, dots, amber states, legend match; Landed adds ₹180 to OrientPack/Anand only; Westline appears with the toggle.
- [x] P3-T2 Provenance drawer — `src/lib/evidence.ts` (shared evidence block: photo crop/preview, PDF page, spreadsheet row, paragraph/email lines with the phrase marked), `src/lib/provenance.ts` + `GET /api/rfx/{id}/cell/{line}/{vendor}` (state, value, as written, mapping options with p and "LLM-estimated (Gemini)" / "measured (Jev)", conversion chain with basis labels and ledger text, review items and reviewer), `ProvenanceDrawer` (DESIGN §2.9; Esc / scrim closes; approver sees "Waiting for Sujit."). Checked all five source types on MER-0419: Balaji cell G8 row, Kohinoor PDF page 1 + snippet + ÷0.975 step, Westline paragraph 5 with "For items 5 and 9…" marked + best-guess step, OrientPack photo (ink blot on line 14 visible), Anand email line 5 + "₹/kg × 1,318.1 g (RFx spec)"; not-quoted cells say so.
- [x] P3-T3 Review queue — `src/lib/review.ts` (list with evidence, DESIGN order, per-type actions; `act()` with TRD §12.3 effects on cells / answers / ledger + an audit event per action), `GET /api/review`, `POST /api/review/{id}/{action}`, `POST /api/review/bulk`, `/rfx/{id}/review` (DESIGN §3.6: lead, Acknowledge all assumptions, filters, cards per §2.11, inline Override / Exclude / Map to line, Ask-vendor draft email block, J/K/C keys, `?item=` focus from the drawer, toasts per §5), Review tab with open count. Pipeline titles/notes aligned with DESIGN copy; one prior-pricing card per vendor. **Done-when check on MER-0419:** cleared all 14 items (confirm, override ×3, treat as not quoted, accept as Yes, bulk acknowledge 7, ask-vendor draft) → 0 open, Westline 4 + OrientPack 1 cells `reviewed`, Anand cleared ✓, 5 buyer ledger rows, 13 `review.*` audit events; bad override (no value/reason) rejected 400. Eval then scores the reviewed state (141/150 with my deliberate choices). MER-0419 reset to the seeded state afterwards (see checkpoint).
- [x] P3-T4 Comparison tabs — `src/lib/rfx-tabs.ts` (questionnaire grid, documents, ledger with folded per-line rows, timeline in words) + `GET /api/rfx/{id}/ledger|timeline`; `/comparison?tab=…` with Prices · Questionnaire · Documents · Ledger · Timeline (DESIGN §2.8 / §3.7). Checked on MER-0419 at 1440 px: 10 × 5 answers (◆, red No for Westline Q6, amber — for Anand Q7), 9 documents with kind chips and use, 13 ledger rows (7 system + 6 buyer from the queue test), timeline of the review decisions and pipeline runs. `spans()` test.
- [x] P3-T5 Unmatched items + senders + assign vendor — `src/lib/unmatched.ts`, `POST /api/responses/{id}/assign-vendor`, `GET /api/rfx/{id}/unmatched`; Unmatched senders card on Responses (assign to existing or new vendor → pipeline continues), Unmatched items panel under the grid (Map / Ignore); runner skips pricing stages for vendor-less replies with an `unknown_vendor` card; guards so a stray or second reply can't wipe a vendor's cells, ledger rows or questionnaire answers (conflict card instead). **Tested on MER-0419:** `wrong_category_IT_quote.xlsx` assigned to Balaji → 5 items unmatched, 0 cells written, Balaji's 150-cell grid, 28 ledger rows and cleared flags byte-identical before/after, header still "30/30 priced"; WhatsApp PNG with no vendor → classify/extract/map done, pricing waits, card raised; assigned to new vendor "Sunrise Packers" → 1 line priced (L16), 29 not quoted, shown under "show disqualified" (no questionnaire). Ignore on an unmatched item works. Clean + realistic eval unchanged (150/150, 50/50 · 150/150, 42/50).
- [x] **P3 checkpoint** — 39 tests, lint, build green; pushed `2384ace`, Vercel redeployed. Test data from P3-T5 removed and MER-0419 reloaded (150/150, questionnaire 50/50, 14 open review items). **Production, as Sujit:** Comparison grid 120 cells (4 cleared vendors) with legend counts, drawer on Kohinoor L7 (PDF source, as written ₹40,510, ÷0.975 step), Review queue 14 cards. **As Priya:** queue read-only (no buttons, "Waiting for Sujit." ×14), POST action → 403, drawer shows "Waiting for Sujit." Side-by-side with the prototype at 1440 px done for Comparison, drawer and Review (differences recorded in DECISIONS). CLAUDE.md asks for a 2-minute screen capture for pacing — not possible from here; the screenshots above were used instead (Sabarish can record one).
- [x] **P3 audit (2026-09-24, on Sabarish's request).** The first P3 report said "finished" without a full checklist; the audit found 9 unbuilt spec items, untested actions and 4 real bugs. All fixed; see DECISIONS ("P3 audit follow-up", "Robustness fixes"). **P3 requirement checklist** (✓ = built and checked, with how):
  | # | Requirement (source) | Status / evidence |
  |---|---|---|
  | 1 | Grid: sticky column/headers, vendor headers (coverage, validity, cleared, freight, total), 8 state renderings + legend counts, per-line min, Unit/Landed, As written, show disqualified (TRD §17.9, DESIGN §2.6–2.8, PRD #20–22) | ✓ screenshots vs prototype at 1440 px; toggles checked (landed +₹180 OrientPack/Anand) |
  | 2 | Hover tooltip from the chain; click → drawer; selected outline (DESIGN §2.6) | ✓ |
  | 3 | Drawer: source (row / PDF page / photo / text), as written, mapping + p + provider label, chain with basis + ledger link, review history, Open in queue, approver "Waiting for Sujit." (TRD §17.10, PRD #24) | ✓ all 5 source types; Open in queue lands on the focused card; approver checked on prod |
  | 4 | Review queue: filters, evidence, proposed value + p, actions by type, keyboard J/K/C, bulk bar, group by vendor/type, clarification note (TRD §17.8, §12.3, DESIGN §2.11/§3.6, PRD #16–17) | ✓ each action run in the UI: Confirm, Override, Exclude, Map to line, Ignore, Ask vendor draft + Mark as asked, Treat as not quoted, Accept as Yes, Treat as No, Dismiss, bulk Confirm/Dismiss, J/J/C |
  | 5 | Every outcome-changing action in the ledger with before/after; audit event per action (PRD #18, TRD §12.3) | ✓ ledger rows checked for confirm, override, exclude, prior, map, Q-decisions |
  | 6 | Tabs Questionnaire (click → evidence), Documents, Ledger (filter, When), Timeline (TRD §17.9, PRD #23) | ✓ side by side with prototype |
  | 7 | Unmatched items panel, unmatched senders, assign vendor, IT quote + WhatsApp don't crash (TRD §16, CLAUDE P3-T5) | ✓ Balaji grid/ledger/answers unchanged by the IT quote; WhatsApp → 8 items, new vendor assigned |
  | 8 | Roles: approver tabs Comparison only, read-only queue, API 403 (DESIGN §4) | ✓ local + prod |
  | 9 | Response Detail: flags chips, counts, Go to Review Queue (n) (TRD §17.7) | ✓ |
  | — | Deferred by plan: Export (P4-T4), Ask panel (P4), Outbox/Sync inbox (P5/P6), lock bar (P7), Settings link on assumption cards (P8), 2-minute screen capture (needs Sabarish) | open, by phase |
## In progress
- [x] **P4 Ask panel + exports — checkpoint passed** (commits 17ec43b … aae6c02). What works: SQL guard, Ask route with best-guess re-run and follow-ups, Ask sheet for both roles, grid/answer exports. What doesn't: model latency varies (one 19 s outlier seen on curl; UI runs 3.4–10.2 s); column names in answers vary run to run.
- **Phase 4 requirement checklist** (definition of done; sources: handoff §4, TRD §6.17/§6.21/§9.8–9.9/§13/§16/§17.9, PRD §8 Stage 7/§13, DESIGN §2.3/§2.13/§2.15/§3.7/§4). ✓ only with evidence.
  | # | Requirement (source) | Status / evidence |
  |---|---|---|
  | 1 | Guard: trimmed SQL starts with `select`/`with` (TRD §13.4) | ✓ `sql-guard.test.ts` rule 1 (`explain …` rejected) |
  | 2 | Guard: rejects DML/DDL words, `;` `--` `/*` `pg_` `information_schema` `current_user` `set\s` `lateral` `into\s` (TRD §13.4) | ✓ test rule 2: delete/CTE-delete, `;`, `--`, `/*`, pg_sleep, information_schema, current_user, lateral, into, set_config() each rejected |
  | 3 | Guard: identifier allowlist = 5 views + their columns + keywords + allowed functions (aggregates, coalesce, round, case, string/number fns, window fns); other token → rejected by name; base tables never (TRD §13.4, DECISIONS "Known risk for P4") | ✓ test rule 3: users, line_quotes, alias-named-users UNION, comma join to users, public.*, password_hash, dblink(), implicit alias each rejected naming the token; plus base-table names rejected anywhere and FROM/JOIN targets limited to views/CTEs (see DECISIONS) |
  | 4 | Guard: literal `rfx_id = '<this id>'`; any other rfx id rejected (TRD §13.4) | ✓ test rule 4: no rfx filter, other id only, other id added, and a join that leaves one view unscoped — all rejected |
  | 5 | Guard: ≤ 4,000 chars (TRD §13.4) | ✓ test rule 5 (4,010-char query rejected) |
  | 6 | Guard unit tests: one per rule incl. `users`, `line_quotes`, other rfx id, missing rfx_id; accepts Q1 (CTE + window) and Q6 SQL | ✓ 9 tests green: Q1 (2 CTEs + row_number/min over partition) and Q6 accepted; extract/cast/quoted alias/best-guess view accepted; best-guess rewrite tests |
  | 7 | Migration 0006: `v_comparison_bestguess` (same columns, best guess folded into unit/landed/annual values, state unchanged, security_invoker, same grants) (TRD §13.2) | ✓ `0006_ask.sql` applied; Westline L5/9/15/19 read 27,960 / 54,960 / 14,080 / 11,580 in the view (state still ambiguous); security_invoker, revoked from anon/authenticated |
  | 7a | Result columns keep the SQL's order (the §6.21 function returns `jsonb`, which sorts keys) and a model-written `LIMIT` doesn't break the function's appended `limit 500` (found while reading 0002) | ✓ `run_readonly_rows` (0006): json keeps column order (checked: vendor, line_no, unit_price came back in that order); query wrapped so its own LIMIT works; same timeout/read-only/grants; a data-modifying CTE is refused by Postgres |
  | 8 | `POST /api/ask` per TRD §13.1 steps 1–9 (history 4, P-SQL strong + Zod, guard + 1 repair, safe-query fallback text + log, 500-row cap + timing, TS aggregates incl. unresolved_cells, P-NARRATE fast, chart_spec, `queries` row incl. `error`, response shape) | ✓ `src/lib/query/ask.ts` + `POST /api/ask`: history 4 (per user), P-SQL strong (LOW thinking) + Zod, guard + one repair (guard or Postgres error, reasons kept in plan.repairs), safe-query text on failure (weather question → fallback in 2 s, logged), 500-row cap, TS aggregates + unresolved_cells (13 on MER-0419), P-NARRATE fast with a number check, chart_spec, `queries` row with every applicable §6.17 column incl. error; response shape per §13.1 |
  | 9 | `include_best_guess` → same SQL rewritten onto `v_comparison_bestguess`; both totals returned (TRD §13.2) | ✓ Q1 re-run via base_query_id → SQL reads v_comparison_bestguess, NOT IN list lost ambiguous/low_confidence; totals without/with returned (₹4,53,38,297 both — the only best guesses in scope are Westline's, disqualified; 0 cells filled). Q6 with best guesses → ₹73,16,208 at stake on 13 cells |
  | 10 | Follow-ups use history ("and on landed cost?" → landed_price) (TRD §13.3) | ✓ "and on landed cost?" after Q1 → same query re-planned on landed_price (30 rows, annual_landed_value_inr) |
  | 11 | `GET /api/ask/history?rfx=` last 20 (TRD §16) | ✓ `GET /api/ask/history?rfx=` → 20 items newest first, with asker name and stored column order; bad rfx → 400 |
  | 12 | Both roles can ask; `maxDuration = 120` (DESIGN §4, TRD §21) | ✓ curl: Priya and Sujit both 200 with computed answers; logged out 401; empty question 400 "Type a question first."; maxDuration 120 |
  | 13 | Done-when: Q1 on MER-0419 → 30 rows, total, exclusions (Westline disqualified, Anand pending Q6, unsure cells count), SQL (CLAUDE.md P4-T2) | ✓ Q1 on MER-0419: 30 rows, ₹4,53,38,297, exclusions Westline (failed Q6 BRC) · Anand (Q6 unclear) · 1 unsure cell (OrientPack L14), SQL returned; 4.5–9.4 s |
  | 14 | Buyer: "Ask" (chat icon) in RFx header → 400px side sheet; approver: "Ask" in Comparison toolbar → same sheet; no Sync inbox yet (DESIGN §2.3/§3.7) | ✓ browser 1440 px: Sujit — header shows Ask (chat icon), sheet 400 px wide (measured); Priya — no header buttons, Ask in the Comparison toolbar opens the same sheet; no Sync inbox |
  | 15 | Sheet: stacked answer cards, 3 suggestion chips, textarea, Ask, history list (DESIGN §3.7, TRD §17.9) | ✓ browser: stacked cards, 3 chips (asked ones drop out), textarea (Enter asks, Shift+Enter newline), Ask button, 'Earlier questions · 20' list from /api/ask/history (opening one adds its card) |
  | 16 | Card: question, answer, amber exclusions, How I computed this + Show query `<pre>`, 260px table box paginated, inline bar chart (no library), unresolved notice + Include best guesses → both totals, Export (DESIGN §2.13/§2.15, TRD §17.9, PRD #25–26) | ✓ browser on Q1/Q2: answer, amber 'Excluded: …', How I computed this + Show query → <pre>, 260 px table (nowrap, scrolls), pager 'Rows 26–30 of 30', Q2 inline bars (₹4.58 cr vs ₹4.53 cr, no library), '13 cells unresolved · ₹73.2 L at stake' + Include best guesses → Without/With toggle with both totals. Export arrives with P4-T4 |
  | 17 | No dead "Save as scenario" button; DECISIONS says it lands with P7-T1 | ✓ no Save-as-scenario button rendered; DECISIONS records it lands with P7-T1 |
  | 18 | All PRD §13 questions computed on MER-0419 through the UI (Q1–Q8 + shortest validity, line 22, Anand 5-ply per-kg) with answer / rows / total / SQL summary recorded | ✓ all 11 asked through the Ask sheet as Priya on MER-0419 (table 'Ask results' below); all computed, SQL generated at run time |
  | 19 | Q1–Q8 asked twice; both runs computed and equivalent | ✓ Q1–Q8 asked twice through the UI (runs 21:39 and 21:51): Q1 30 rows ₹4,53,38,297 both; Q2 ₹4,97,191 (1.08 %) vs Balaji ₹4,58,35,488 both; Q3 0 lines both; Q4 ranking unchanged (Kohinoor, Balaji, OrientPack) both; Q5 hybrid ₹4,20,18,677 'better' both; Q7 83.15, ±₹13,99,785 both. Differences found and fixed: Q8 refused in run A (fixed by P-SQL v2 export note; then computed 2/2 = Q1's 30 rows), Q6 returned 0 rows in run B (P-SQL v3; then 13 rows 4/4 incl. one more UI run). Column names vary run to run (vendor / winning_vendor / cheapest_vendor) |
  | 20 | Each answer ≤ 15 s; timings recorded (CLAUDE.md §10) | ✓ UI timings 3.4–10.2 s, all ≤ 15 s (Q5 10.2 s and Q7 9.9 s in run A, 4.8 s in run B); earlier default-thinking runs took 14–43 s → LOW thinking (DECISIONS). One curl-run outlier 19.3 s (slow first model call + repair) — model latency varies |
  | 21 | Off-topic/unsafe → "I couldn't form a safe query for that; try rephrasing"; network error → toast with code (TRD §13.1, §19) | ✓ weather question → safe-query answer (script + curl); fetch forced to fail in the browser → toast 'Couldn't reach the server — check the connection and ask again (NETWORK)', question kept in the box; API errors toast '{error} ({code})' |
  | 22 | `GET /api/export/comparison?rfx=&format=xlsx|csv&basis=` — xlsx line × vendor, state fills (DESIGN colours), legend + ledger sheet; csv tidy rows (TRD §16, PRD #28) | ✓ `src/lib/export.ts` + route: MER-0419 xlsx = Comparison (30 lines × 5 vendors, headers ✓/✗/?, totals row), 'Legend & notes' (9 states with meanings + counts, vendor questionnaire status), 'Ledger' (8 folded rows); csv = 150 tidy rows (line, vendor, state, unit, landed, best guess, as written, annual value) |
  | 23 | `GET /api/export/query/{id}?format=csv|xlsx` (TRD §13.6/§16) | ✓ `/api/export/query/{id}` xlsx (Answer sheet in the query's column order + Question sheet with answer, how, exclusions, SQL) and csv; Q1 → 30 rows |
  | 24 | Export button in Comparison toolbar and on each answer card (DESIGN §2.8/§2.13) | ✓ Comparison toolbar: Export (xlsx, basis on screen) + CSV, both roles; each answer card with rows: Export + CSV |
  | 25 | Proof: downloaded files read back with a script (rows, values, fills) | ✓ curl as Priya → 200 with attachment names; read back with exceljs: line 5 Westline '27,960? (best guess)', line 14 OrientPack '?', line 30 'prior pricing'/'not quoted'; fills FFE7EAF6 ×78 (inferred), FFFAF0DB ×5 (4 ambiguous + 1 low), FFF1F0EB ×11 (3 not quoted + 8 prior) = grid legend; line 1 lowest (Kohinoor ₹67,508) bold with teal left edge; logged out → 401 |
  | 26 | Tests + lint + build green; pipeline:seed 150/150; push | ✓ 54 tests (13 files, incl. 9 guard + 5 result tests), lint clean, `next build` green; `npm run pipeline:seed` from scratch: 150/150, questionnaire 50/50 (231 s); pushed aae6c02 |
  | 27 | Production: Q1 as Priya and as Sujit, computed with SQL shown; one export downloads | ✓ production: Q1 as Priya (4.6 s) and as Sujit (3.6 s) → 30 rows, ₹4,53,38,297, SQL returned; answer xlsx (9.8 kB) and grid xlsx (12.7 kB) download as valid Excel files; browser as Priya: toolbar Ask → sheet, history card, Show query |
  | 28 | This table fully ticked with evidence | ✓ rows 1–27 ticked with evidence |
  **Ask results on MER-0419 (UI, as Priya, 2026-09-24; answer · rows · total · SQL shape · time):**
  | Q | Answer (computed) | Rows | Total | SQL | Time (run A / B) |
  |---|---|---|---|---|---|
  | Q1 cheapest qualified per line | Kohinoor, Balaji, OrientPack win lines; excluded Westline (failed Q6 BRC), Anand (Q6 unclear), 1 unsure cell (OrientPack L14) | 30 | ₹4,53,38,297 | v_comparison ⋈ v_vendor_status, cleared = true, row_number() over line | 4.9 / 4.1 s |
  | Q2 saving vs single vendor | ₹4,97,191 (1.08 %) cheaper than Sri Balaji (only vendor pricing all 30) at ₹4,58,35,488; bar chart of both totals | 1 | ₹4,58,35,488 vs ₹4,53,38,297 | per-line min vs per-vendor sums with count = 30 | 5.4 / 5.2 s |
  | Q3 single qualified quote | none — every line has ≥ 2 qualified quotes (checked with a direct SQL count) | 0 | — | count(*) over line having = 1 | 6.6 / 3.8 s |
  | Q4 landed vs unit ranking | ranking unchanged: Kohinoor ₹4.39 cr, Balaji ₹4.58 cr, OrientPack ₹4.67 → ₹4.71 cr landed | 3 | — | sums + dense_rank() on both bases | 4.6 / 4.7 s |
  | Q5 5-ply qualified + 3-ply overall | ₹4,20,18,677 — better than Q1 by ₹33,19,620 (7.32 %) | 1 | ₹4,20,18,677 | two per-line mins by ply, union, vs Q1 | 10.2 / 4.8 s |
  | Q6 unsure cells + money | 13 cells (Westline 4 ambiguous, OrientPack L14 low, Anand 8 prior); ₹73,16,208 at best guess on 4 cells; Include best guesses fills the Westline values | 13 | ₹73.2 L at stake | state in unsure set | 4.5 / 4.7 s (after v3) |
  | Q7 OrientPack USD ±3 % | ledger: USD→INR 83.15 (manual, 23 Sep 2026); base ₹4,66,59,502, ±₹13,99,785 → ₹4,80,59,287 / ₹4,52,59,716 | 1–2 | — | v_assumptions (fx_rate) + sums × 1.03 / 0.97 | 9.9 / 4.8 s |
  | Q8 export Q1 | re-issues Q1 (30 rows, ₹4,53,38,297); Export/CSV buttons on the card download it | 30 | ₹4,53,38,297 | same as Q1 | 6.5 / 4.1 s |
  | Shortest validity | Kohinoor, 30 days, valid until 24 Oct 2026 (others 60) | 5 | — | v_vendor_status order by validity_days | 3.4 s |
  | Who didn't quote line 22 | nobody — all five quoted line 22 (checked directly) | 0 | — | v_comparison line 22, state not priced | 4.3 s |
  | Why is Anand's 5-ply so cheap | ₹42 per kg converted with *our* spec weight per piece (ledger rows "₹/kg × 1318.1 g per piece from our spec (not the vendor's)") vs other vendors' prices | 40 | — | v_comparison ⋈ v_assumptions (weight_per_piece) | 8.3 s |
  | Off-topic (weather) | "I couldn't form a safe query for that; try rephrasing" | 0 | — | planner returned no SQL | 2.0 s |
- [x] **P5 New RFx co-pilot, dispatch (mock), Add response + portal, Overview — checkpoint passed** (commits 58b5425 … final). What works: co-pilot drafts an RFx from a description + line sheet, editable tabs, Issue freezes v1 and sends 5 mock emails with XLSX + PDF, Outbox, Add response sheet, vendor portal, Overview as the buyer's default tab. What doesn't / limits: uploads over 4.5 MB are refused with a message (no direct upload yet); the co-pilot sometimes fills default terms and vendors before being asked.
- **Phase 5 requirement checklist** (sources: handoff §5, TRD §6.3–6.7/§9.1–9.3/§15.1–15.2/§16/§17.3–17.6/§22 item 2, PRD §8 Stages 1–3, DESIGN §2.3/§2.16/§3.3–3.5/§4). Test RFx rule: create/issue tests only on a throwaway "TEST co-pilot run" RFx, never MER-0418.
  | # | Requirement (source) | Status / evidence |
  |---|---|---|
  | 29 | `/rfx/new` creates a draft (`POST /api/rfx`); `/rfx/new?id=` opens a draft; buyer only (approver redirect, API 403) (TRD §16/§17.3) | ✓ browser: /rfx/new shows 'Draft · new' (no row written); first Send creates MER-0421 via POST /api/rfx and the URL becomes /rfx/new?id=… (history.replaceState, no remount); reopening ?id= loads the draft; Priya: /rfx/new → 307 /rfx, POST/PATCH/copilot → 403 |
  | 29a | New RFx codes continue the series (MER-0420…), unique (found: codes are hand-seeded) | ✓ MER-0420 then MER-0421 allocated from the MER-#### series (unique code, retry on conflict) |
  | 30 | DESIGN §3.3 layout/copy: eyebrow "DRAFT · code", h1 "New RFx", sub-line, "Save draft", primary "Issue to N vendors" disabled until lines+terms+questionnaire; split 340–460 px | rest | ✓ screenshot at 1440: eyebrow 'DRAFT · MER-0421', h1 'New RFx', DESIGN sub-line, Save draft + primary 'Issue to 5 vendors' (disabled until lines, terms, deadline, questionnaire, vendors — tooltip lists what's missing), split minmax(340px,460px) | 1fr |
  | 31 | Co-pilot card: header + status "terms pending · questionnaire pending", SUJIT/CO-PILOT log with 2 px rules, patch box, 4 chips, placeholder, hint "Attach xlsx / csv", Send (DESIGN §3.3) | ✓ Co-pilot card: status 'terms set · 10 questions' (was 'terms pending · questionnaire pending'), SUJIT/CO-PILOT labels with 2 px ink/teal rules, numbered questions, patch box (Lines · 30 parsed…, Terms · …, Vendors · …), 4 chips, DESIGN placeholder, 'Attach xlsx / csv' opens the picker, Send (⌘/Ctrl+Enter) |
  | 32 | `POST /api/rfx/{id}/copilot`: P-COPILOT §9.1 + `CopilotTurn` Zod; applies rfx_patch; transcript in `rfx.copilot_transcript`; ≤ 3 questions; never invents lines; audit per applied suggestion (TRD §9.1, PRD Stage 1) | ✓ `src/lib/copilot.ts`: P-COPILOT verbatim + CopilotTurn Zod (questions max 3), patch applied via patchDraft, transcript in rfx.copilot_transcript (4 turns survived reload), ≤ 3 questions every turn, lines only from the sheet parser or marked 'draft — confirm'; audit copilot.applied per turn with what changed |
  | 33 | Attachments xlsx/csv/txt → preprocessors → P-LINESHEET; `rfx_lines.xlsx` → exactly 30 lines matching `rfx_lines.csv` (script) (TRD §9.2, CLAUDE P5-T1) | ✓ attach rfx_lines.xlsx (UI upload and script) → xlsx preprocessor → P-LINESHEET → 30 lines; script compare: 30/30 identical to rfx_lines.csv on all 13 fields (SKU, ply, L/W/H, GSM, BF, type, weight, monthly, annual, plant, description) |
  | 34 | "standard terms" → delivered, freight incl., 45 d, 60 d, 12 months, INR, per 1000 pcs (TRD §9.1) | ✓ 'Standard terms' → INR · per 1000 pcs · delivered, freight included · 45-day payment · 60-day validity · 12 months (patch box + Terms tab); terms_set flag (migration 0008) |
  | 35 | Questionnaire request → 8–12 questions with types, mandatory, suggested disqualifiers; editable (TRD §9.1) | ✓ 'Attach questionnaire' → 10 questions (yes/no, number, text), all mandatory, BIS/ISO + BRC disqualifying ('no'), capacity 'lt:100'; edited in the tab (removed Q10 → 9, saved) |
  | 36 | Tabs Lines [n] / Terms / Questionnaire [n] / Vendors [n], editable, `PATCH /api/rfx/{id}` draft-only (409 otherwise); Lines columns + add/remove + empty state + footer hint; Vendors from address book or name + email (TRD §17.3, DESIGN §3.3) | ✓ tabs Lines 30 / Terms / Questionnaire n / Vendors 5: line inputs for every TRD §17.3 column, annual follows monthly × 12 (7,000 → 84,000) unless edited, add/remove, empty state 'No lines yet.' + button, footer hint 'Parsed from rfx_lines.xlsx …'; Vendors: remove, add from address book, new vendor name+email (created, then removed); PATCH draft-only (409 after issue, see P5-T2); empty description → 400 |
  | 37 | Every change survives a reload | ✓ reload after Save draft: monthly 7,000 / annual 84,000, 9 questions, deadline 2026-10-15, transcript and vendors all back from the database |
  | 38 | Issue confirm dialog lists what is sent and to whom (TRD §17.3) | ✓ browser: 'Issue MER-0421 to 5 vendors' dialog lists what freezes (30 lines, terms, 9 questions, vendors), both attachment names, each vendor's email and Reply-To tag, transport = mock |
  | 39 | `POST /api/rfx/{id}/issue`: freeze v1 + status issued; line-sheet XLSX (exceljs) + questionnaire PDF (react-pdf) to `outbound`; P-DISPATCH per vendor incl. the exact sentence; `sendEmail()` queued → sent (mock, sent_at, attachments, Reply-To tag); invited_at; audits `rfx.frozen` + `dispatch.sent`; frozen → PATCH 409 (TRD §15.1–15.2, §9.3, PRD Stage 2) | ✓ `src/lib/dispatch.ts`: MER-0420 (API) and MER-0421 (UI) → version 1, frozen_at, status issued; XLSX (exceljs) + PDF (@react-pdf/renderer) in bucket outbound; P-DISPATCH per vendor (Zod refine requires the exact sentence — present in all 10 emails); sendEmail() writes the row queued then sent/mock with sent_at, message id, attachments, Reply-To sourcing+rfx-mer-0421-<vendor>@…; invited_at 5/5; audit rfx.frozen + 5 × dispatch.sent; PATCH after issue → 409 FROZEN; second issue → 409; missing deadline → 409 NOT_READY; approver → 403; editor on an issued RFx → redirect |
  | 39a | Dispatch email signs with the buyer's name, title and email (P-DISPATCH {buyer_title}; users table has no title) | ✓ signed 'Regards, Sujit Menon, Category Buyer, sujit.menon@meridianfoods.example'; From 'Sujit Menon (Meridian Foods) <sujit.menon@…>' (empty GMAIL_USER fallback fixed) |
  | 40 | Outbox page: outbound list, Open → email block (DESIGN §2.16), downloadable attachments; XLSX read back = 30 lines, PDF has 10 questions (TRD §15.2) | ✓ /rfx/{id}/outbox: rows (vendor, email, subject, attachments, '24 Sep 03:48', 'sent (mock)'), Open → DESIGN §2.16 email block + message id; both links fetched in the browser (xlsx 200, pdf 200); XLSX read back = 30 lines + 'Your price' column; PDF text = 10/10 questions (number + text) |
  | 41 | Done-when (throwaway RFx): 5 outbox entries, each with both attachments | ✓ MER-0420: {sent: 5, failed: []}; MER-0421 via the UI: 5 outbox entries, each with MER-0421_Line_Sheet.xlsx + MER-0421_Supplier_Questionnaire.pdf |
  | 42 | "Add response" quiet button per vendor row → 400 px sheet per DESIGN §3.5 (drop zone, OR PASTE THE EMAIL BODY, textarea, Use seed file, Submit and run, hint) → `POST /api/responses` + run-all → Response Detail with pipeline running | ✓ browser on MER-0421: quiet 'Add response' on every vendor row (also vendors with no reply) → 400 px sheet (measured) with DESIGN §3.5 copy (drop zone text, OR PASTE THE EMAIL BODY, textarea, Use seed file, Submit and run, hint); Kohinoor + Use seed file → POST /api/responses → Response Detail ?run=1 → six stages ran visibly (classify 5.8 s … flags 1.5 s); drag-and-drop supported; Responses lead per DESIGN ('One response in of 5, all processed. …'), 'n/30 priced', questionnaire chip |
  | 43 | `/rfx/{id}/portal/{vendorId}`: mailbox look (From Sujit Menon, subject, body, attachments), Reply form (files + text), source='portal'; a portal reply runs the pipeline and its cells reach the grid (TRD §17.6) | ✓ /rfx/{id}/portal/{vendorId}: 'Vendor portal (mock) · inbox of Sri Balaji Packaging', 'From Sujit Menon', subject, dispatch body, both attachments (links), Reply form (files + text); Balaji xlsx + text → source=portal → six stages done (extract 27.4 s) → MER-0421 grid 30 Balaji cells confirmed; ?run dropped so a reload doesn't re-run |
  | 44 | Files > 4.5 MB: clear error or direct upload; never silent (Vercel limit) | ✓ clear error, no direct upload: 5 MB file → toast 'big_scan.pdf is over 4.5 MB — the upload limit here. Send a smaller photo or scan, split the files, or paste the email text.' before uploading; server → 413 TOO_LARGE with the same advice (DECISIONS) |
  | 45 | Portal reply to the throwaway RFx doesn't touch MER-0419; MER-0419 still 150/150 afterwards | ✓ after the portal and Add-response replies on MER-0421: `npm run eval` MER-0419 150/150, questionnaire 50/50 (MER-0419 untouched); full reload at the P5 checkpoint below |
  | 45a | Seed reload resets the reloaded vendors' invitation status (found: Westline left at `clarification_sent` from P3 testing) | ✓ after the reload Westline is 'responded' (was 'clarification_sent' from P3 testing); `clearResponses` resets reloaded vendors to invited, flags sets responded |
  | 46 | `/rfx/{id}/overview` = buyer default; `/rfx/{id}` → overview (buyer) / comparison (approver); buyer tabs Overview · Responses · Review [n] · Comparison (DESIGN §2.3/§4) | ✓ /rfx/{id} → buyer 307 to /overview, approver to /comparison; /overview as approver → comparison; buyer tabs Overview · Responses · Review 14 · Comparison (screenshot) |
  | 47 | DESIGN §3.4: computed lead, "Needs you" card + "Open the queue", "The event" card, Vendors table (Vendor · Sent as · Received · Priced n/30 · Valid to + amber chip · Questionnaire chip · Needs you), Vendor communications timeline (direction, time, subject, attachments, message id, status) | ✓ screenshot 1440 on MER-0419: computed lead 'All five vendors replied within a day. Three cleared the questionnaire. 14 items need your call …; … ₹4.53 cr a year' (cheapest qualified per line, same as Ask Q1); Needs you grouped by vendor (+ 'Open the queue'; Westline '4 × price per bundle, bundle size not stated (items 5, 9, 15 and 19)', unit-tested); The event (Issued · Deadline · Scope incl. ₹4.39 cr–₹4.67 cr · Terms · Plants · Questionnaire 'Q1, Q3, Q6 disqualifying' · Transport); Vendors table (Sent as, Received, Priced n/30, Valid to + amber '30d', questionnaire chip, Needs you); communications timeline (time, ←/→, vendor + subject/attachments, kind · mode · status · message id) + Outbox link |
  | 48 | Per vendor "Portal (mock)" + "Open response"; Load seeded responses in mock mode; no Go to Review/Comparison buttons (DESIGN §0.8, recorded); unmatched panel when any (TRD §17.4) | ✓ per vendor 'Open response' + 'Portal (mock)' (mock mode), row click → response (links inside the row don't double-navigate); Load seeded responses / Realistic set in the Vendors card; no Go to Review / Go to Comparison (DECISIONS); Unmatched card with Assign when strays exist |
  | 49 | Header meta "Reviewing · 5 of 5 responded" (DESIGN §2.3) | ✓ header meta 'Reviewing · 5 of 5 responded' (MER-0419), 'Issued — awaiting 5 responses' (MER-0420) |
  | 50 | Empty/early states: draft RFx (link to New RFx), issued with 0 replies | ✓ issued with 0 replies (MER-0420): 'Issued 24 Sep 2026 to five vendors. No replies yet — the deadline is 12 Oct 2026.' + 'Nothing yet — replies raise their questions here.'; draft (MER-0418, viewed only): 'This RFx is still a draft. …' + 'Open in New RFx' |
  | 51 | Production: TRD §22 item 2 flow end to end on a throwaway RFx; then delete it with a script | ✓ production (quotelens-seven.vercel.app, browser as Sujit): New RFx → MER-0422; first message + rfx_lines.xlsx → 30 lines; chips Standard terms → terms set, Attach questionnaire → 10 questions (7 disqualifying), Add vendors → 5; deadline 16 Oct; Issue dialog → Outbox 5 × 'sent (mock)' with both attachments; portal reply as Anand (email text) → six stages done on Vercel (extract 12.6 s). Throwaway MER-0420/0421/0422 then deleted with a script (rows + 12 storage files + test vendor) |
  | 52 | Production approver: no New RFx / Issue / Add response; no Overview tab; APIs 403 | ✓ production as Priya: rail = RFx only, no New RFx button, /rfx/new → /rfx, MER-0419 opens on Comparison, tabs = Comparison, no header buttons, no Add response; POST /api/rfx, PATCH, issue, copilot, POST /api/responses → 403 |
  | 53 | Tests + lint + build green; pipeline:seed 150/150 clean; push | ✓ 55 tests (14 files), lint clean, `next build` green; `npm run pipeline:seed` after cleanup: 150/150, questionnaire 50/50 (236 s); pushed |
  | 54 | Final state: MER-0419 seeded with 14 open review items; MER-0417 realistic; MER-0418 untouched draft (v0, no comms, not invited); RFx list shows exactly these three | ✓ script check after cleanup + reload: RFx list = MER-0417 (reviewing v1, realistic 150/150, q 43/50), MER-0418 (draft v0, 0 comms, 0 responses, 5 vendors not invited), MER-0419 (reviewing v1, 150 cells, 14 open review items, all vendors 'responded') |
  | 55 | Side by side with the prototype at 1440 px: Ask sheet, New RFx, Overview, Responses + Add response sheet, Outbox, Portal | ✓ prototype (file://design/prototype.html, same routes) vs app screenshots: Ask sheet, New RFx, Overview, Responses + Add response sheet, Outbox and Portal checked. Fixed from the comparison: co-pilot question list numbering, Needs-you vendor name once per group, assumption cards folded into one 'Assumptions · n to acknowledge' row (prototype lists decisions only), 'failed Q6' chip instead of a cut-off note, nowrap answer tables. Remaining differences recorded in DECISIONS (P5 visual check) |
  | 56 | This table fully ticked with evidence | ✓ rows 29–55 ticked with evidence |
- [x] **P6 Mock Gmail, Sync inbox, clarification loop — checkpoint passed** (commits d31f82b … final). Gmail is mocked by decision (DECISIONS 2026-09-24 "Live Gmail out of scope"): real `.eml` files, a mock mailbox table, Sync inbox matching by tag; `gmail.ts` is a stub. **What works:** every email is a real RFC 822 message (Outbox "Download .eml"); the vendor portal is the vendor's mailbox (Reply → waits unread); Sync inbox (header button, Review "Sync inbox — X replied", Overview 30 s poll) matches by tag, is idempotent on Message-ID, handles unknown senders and unrelated mail; Ask vendor drafts one P-CLARIFY email per vendor, Send, reply by mailbox or paste → only the asked/unresolved lines change, as reviewed, cards resolved_by_reply, vendor clarified, ledger superseded; Westline 5/9/15/19 = ₹27,960 / ₹68,700 / ₹14,080 / ₹14,475 on all 4 runs; production round trip done. **What doesn't / limits:** no live Gmail (by decision); Sync reads unread mail only, so a second click finds nothing (idempotency shown by re-delivery); asked cards leave the Open list until the reply arrives; the dev machine's connection dropped repeatedly during testing (timeouts added, see Known issues).
- **Phase 6 requirement checklist** (sources: handoff P6 §5, CLAUDE P6-T1…T3, TRD §6.6–6.8/§6.15/§8.4/§8.7/§9.6/§12.3/§15.1–15.4/§16/§17.4/§17.6/§17.8/§22 items 3, 5, 8, PRD §8 Stages 3 + 5b/§9/§12, DESIGN §2.3/§2.11/§2.16/§3.4/§3.6/§4/§5). Rows are numbered 6.x so they are unique in this file. Test RFx rule: issue tests only on a throwaway RFx, never MER-0418.
  | # | Requirement (source) | Status / evidence |
  |---|---|---|
  | 6.1 | `tag.ts` `formatTag` / `parseTag` resolved against known reply tags (longest match); tests: plus-address, bare tag in subject, `-clar-n`, uppercase, digits in vendor code, prefix-sharing RFx codes, no tag (TRD §15.1, handoff 6.1) | ✓ `src/lib/email/tag.ts` `formatTag` + `parseTag(text, knownTags)` bounded, longest known tag wins; `tag.test.ts` 7 tests green: plus-address, bare tag in subject, -clar-2, uppercase, testsupplier vs testsupplier2, rfx-mer-04 vs rfx-mer-04-2, no tag / near-miss → null |
  | 6.2 | Every outbound email is a real `.eml` (nodemailer streamTransport: From, To, Reply-To tag, Subject, text, attachments, Message-ID, In-Reply-To/References for clarifications) stored in `outbound` `rfx/{id}/outbound/mail/{commId}.eml`; generated Message-ID in `communications.message_id`; queued → sent, mode mock (TRD §15.1–15.3) | ✓ `buildEml()` (nodemailer streamTransport, buffer) in `sendEmail()`: attachments read from `outbound`, `.eml` stored at `rfx/{id}/outbound/mail/{commId}.eml`, generated Message-ID in `communications.message_id` (+ new `eml_path`), queued → sent, mode mock; In-Reply-To/References when given; `eml.test.ts` round trip (headers, Reply-To tag, threading, display name, 2 attachments byte-identical) |
  | 6.3 | Migration 0009: mock mailbox table (id, rfx_id, direction to_vendor/to_buyer, from/to, subject, message_id unique, in_reply_to, eml_path, seen, created_at), RLS on, no anon/authenticated grants; every outbound email also lands as `to_vendor` (handoff 6.3) | ✓ `0009_mock_mailbox.sql` applied (`npm run db:migrate`): `mock_mailbox` with the listed columns + vendor_id, message_id unique, RLS on, revoked from anon/authenticated; `communications.eml_path`. MER-0420 dispatch → 5 `to_vendor` rows (checked for Westline: to_vendor, unseen, same eml_path) |
  | 6.4 | `gmail.ts` stub with the same send/sync interface → NOT_CONFIGURED "Live Gmail is out of scope for this build"; `sendEmail()` routes on `settings.email_mode`, non-mock → 501, never a silent no-op (TRD §15.4 pattern) | ✓ `src/lib/email/gmail.ts` `sendGmail`/`syncGmail` throw NOT_CONFIGURED 501 "Live Gmail is out of scope for this build…"; script with `email_mode = gmail` → sendEmail threw NOT_CONFIGURED 501, communication `failed` with the message, setting restored to mock. GMAIL_* env no longer read (dispatch From = buyer email) |
  | 6.5 | Proof: issue a throwaway RFx in the UI → 5 outbox entries; one `.eml` downloaded and parsed back with mailparser: headers, Reply-To tag, Message-ID = communications row, 2 attachments intact (XLSX 30 lines, PDF opens); Outbox and portal still render | ✓ throwaway MER-0420 (draft via API from MER-0418 data, MER-0418 only read) issued with the UI Issue dialog → Outbox "Five emails sent", 5 × sent (mock) with both attachments; "Download .eml" link from the Outbox → mailparser: From/To/Reply-To `sourcing+rfx-mer-0420-westline@…`/Subject, Message-ID = communications row, XLSX sheet "Line Items" 30 lines, PDF 2 pages loads; portal for Westline renders the email. Found + fixed: "(Meridian Foods)" in the From name was read as an RFC comment → display name quoted |
  | 6.6 | Portal = the vendor's mailbox: every message to that vendor newest first, each with Reply; reply = real `.eml` (From vendor, To the tagged Reply-To of the answered message, `Re:` subject, In-Reply-To + References, attachments + text) in `raw` `mock-mailbox/{id}.eml`, unread `to_buyer`; no response created; toast "Sent — it arrives when the buyer syncs the inbox"; > 4.5 MB → existing error (TRD §17.6 reworked, handoff 6.6) | ✓ `/rfx/{id}/portal/{vendorId}` = "mailbox of <vendor> · <email>": every to_vendor message newest first (`vendorInbox`), each with Reply, "replied" chip; reply (`POST /api/email/vendor-reply` → `replyAsVendor`) builds an `.eml` From the vendor, To the answered email's tagged Reply-To, `Re:` subject, In-Reply-To + References, attachments + text, stored `raw/mock-mailbox/{id}.eml`, unread to_buyer; no response created; browser: toast "Sent — it arrives when the buyer syncs the inbox" (Balaji xlsx, Anand pasted text); 5 MB file → "big_scan.pdf is over 4.5 MB — …" before upload (server 413 too). DECISIONS P6-T2 (a) |
  | 6.7 | `POST /api/email/sync {rfx_id}` (buyer/admin, approver 403, maxDuration 120): simpleParser; tag from To → Subject → In-Reply-To vs `communications.message_id`; idempotent on Message-ID (skipped); `.eml` + attachments raw; response via `createResponse()` (inbound comm mode mock, message_id, in_reply_to, from, subject, attachments); mark seen; `{new_response_ids, skipped}`; untagged → vendor-less response + `unknown_vendor` card only if it answers our mail or names the RFx, else ignored and reported (TRD §15.3, §16) | ✓ `POST /api/email/sync` (`src/lib/email/sync.ts`, maxDuration 120): simpleParser, tag To → Subject → In-Reply-To (answered communication's Reply-To), Message-ID checked against communications (skipped) + unique index on race, `.eml` copied to `raw/rfx/{id}/responses/{rid}/message.eml` (`communications.eml_path`), attachments via `createResponse()` (inbound comm mode mock, message_id, in_reply_to, from, to, subject), mailbox row seen; returns `{new_response_ids, new_responses, skipped, ignored}`; approver 403. Untagged + RFx code in subject → vendor-less response → pipeline: `unknown_vendor` + `unmapped_item` cards, Overview Unmatched card "ravi_rates.csv · Ravi Traders"; untagged unrelated → `ignored` with reason, marked seen |
  | 6.8 | Response `source` for mailbox replies chosen and recorded | ✓ source `portal` for mailbox replies (the vendor replied from the portal mailbox; no constraint change; `gmail` not claimed; communications.mode `mock`); pasted/uploaded replies keep `mock_paste`/`mock_upload`. DECISIONS P6-T2 (b) |
  | 6.9 | "Sync inbox" in the RFx header, buyer only, left of Ask; sync → run-all for each new response, two at a time, visible progress; toasts "Synced — 1 new reply from Westline Packaging" / "Nothing new"; errors with code (DESIGN §2.3, §4, §5; TRD §1.3) | ✓ header "Sync inbox" left of Ask, buyer only, hidden on drafts (`SyncInboxButton`); browser on MER-0420: label "Syncing…" → "Syncing… 2 new replies, processing" → run-all two at a time → toast "Synced — 2 new replies from Sri Balaji Packaging and Anand Box Works" (45 s incl. extraction); "Nothing new"; with email_mode = gmail → toast "Live Gmail is out of scope … (NOT_CONFIGURED)"; Priya: no button (page HTML), sync 403 |
  | 6.10 | Overview polls sync every 30 s while open and visible; no overlapping syncs; pauses when hidden (TRD §15.3) | ✓ `SyncPoller` on the Overview (mock mode): 30 s interval, skips when `document.visibilityState` ≠ visible or a sync is running (one shared store per tab, buttons disabled while busy), quiet unless something arrived. Browser: tab forced hidden 35 s → 0 sync requests, Kohinoor reply still unseen; visible → auto sync after 15 s → "Synced — 1 new reply from Kohinoor Corrugators Pvt Ltd", reply seen |
  | 6.11 | Overview "Vendor communications": inbound mail with ←, Message-ID, In-Reply-To link to the email it answers, attachments; Outbox lists outbound only (DESIGN §3.4, TRD §17.4) | ✓ Overview timeline: ← inbound rows with vendor + subject, attachment links, `vendor reply · mock · received · <message-id>`, "in reply to our RFx email of 24 Sep 09:18" anchor link to the answered row, "response" link (screenshot); Outbox = `listComms(direction outbound)` only |
  | 6.12 | Proof on the throwaway RFx: 2 portal replies (spreadsheet, pasted text) → Sync → 2 responses, pipeline done, cells; re-sync idempotent; untagged RFx-mentioning `.eml` → unknown_vendor card; untagged unrelated `.eml` → ignored + reported; MER-0419 150/150 | ✓ MER-0420: portal replies Balaji (xlsx) + Anand (pasted text) → Sync → 2 responses, six stages done, Balaji 30/30, Anand 22/30 in the grid; plain re-sync → 0 new; replies re-delivered (seen reset by script) → 0 new, 2 skipped "already received" (the literal "sync again" finds nothing unread, so re-delivery is the idempotency proof — DECISIONS P6-T2 (c)); untagged RFx-mentioning `.eml` → unknown_vendor card, no crash; untagged unrelated → ignored + reported; MER-0419 `npm run eval` 150/150, questionnaire 50/50 |
  | 6.13 | `POST /api/clarify {rfx_id, vendor_id, review_item_ids}` → P-CLARIFY §9.6 verbatim, drafting model, Zod `{subject, body_text}`; items built from review items (line, description, what is missing); draft only; buyer only (TRD §9.6, §16) | ✓ `POST /api/clarify` → `draftClarification()` (`src/lib/clarify.ts`): P-CLARIFY verbatim on the fast model + format note + sign-off, Zod `{subject, body_text}` with a refine that every asked item number is in the body; bullets built from the cards ("Item 9 (550x350x350, 5-ply): priced per bundle but the bundle size is not stated — please state the number of pieces per bundle."), in line order; draft only (nothing written); buyer only (Priya 403 — row 6.21). MER-0419: draft for Westline 5/9/15/19, 4–8 s. v1 (no format note) ran the bullets into one paragraph; a first build read another vendor's cell unit → fixed (DECISIONS P6-T3) |
  | 6.14 | `POST /api/clarify/send` → `sendEmail(kind clarification)`: Reply-To `<tag>-clar-<n>`, In-Reply-To/References = the thread's message; `.eml` + mailbox row; items → `asked_vendor` with the communication id; `rfx_vendors.status` → `clarification_sent`; audit `clarification.sent` (TRD §12.3, §16) | ✓ `POST /api/clarify/send` → `sendClarification()`: Reply-To `sourcing+rfx-mer-0419-westline-clar-1@…` (n = sent clarifications + 1; OrientPack test → `-orientpack-clar-1`), `.eml` in outbound + `to_vendor` mailbox row (portal lists it), 4 cards → `asked_vendor` with `communication_id` + `clar_n`, Westline → `clarification_sent`, audit `clarification.sent` (vendor, n, titles, message id); In-Reply-To: dispatch if any, else the vendor's latest email, else none (6.19a) |
  | 6.15 | Review UI replaces template + "Mark as asked": Ask vendor on an askable card drafts one email for all that vendor's open askable items (untick possible); inline email block To / Reply-To / Subject / body, subject + body editable, Send / Cancel; toast "Sent — logged under vendor communications"; note "Clarification sent to Westline Packaging for items 5, 9, 15 and 19. Waiting for their reply."; primary "Sync inbox — Westline replied" when a tagged reply waits unread (`GET /api/email/pending`); approver view only, API 403 (DESIGN §2.11, §3.6, §4) | ✓ browser on MER-0419 as Sujit: Ask vendor on Item 9 / Item 5 → one draft for all 4 open Westline cards (tick boxes; unticking one → "Redraft for 3 items" replaces Send, re-ticking → Send), inline block To / Reply-To `…-westline-clar-1@…` / editable Subject / editable body, Send / Cancel; toast "Sent — logged under vendor communications"; note "Clarification sent to Westline Packaging for items 5, 9, 15 and 19. Waiting for their reply."; after the portal reply the primary button reads "Sync inbox — Westline Packaging replied" (server count + 30 s poll of `GET /api/email/pending`) → toast "Synced — 1 new clarification reply from Westline Packaging". Priya: review page has no Ask vendor / Send / Sync inbox, 10 × "Waiting for Sujit."; `/api/clarify`, `/api/clarify/send`, `/api/review/{id}/ask-vendor` with a real item → 403 |
  | 6.16 | Clarification ingestion: `-clar-n` reply → `is_clarification`, `supersedes_response_id` = main reply; normalise upserts only lines that were references_prior / ambiguous / not_quoted / low_confidence or were asked about; other cells untouched; P3-T5 guard overridden for clarifications only; original's items for those lines → `resolved_by_reply`; vendor → `clarified`; replaced best-guess ledger rows superseded, not deleted; clarification's questionnaire answers never replace real ones (TRD §8.7) | ✓ synced `-clar-1` reply → response `is_clarification`, `supersedes_response_id` = Westline main reply, summary.clarification {request_id, n}; normalise scope = unresolved lines ∪ asked lines (`clarificationScope`), reply merged over the first read (`mergeClarified`), written as `reviewed`; script diff before/after: cells changed = westline L5/L9/L15/L19 only, answers unchanged, ledger 32 active before and after (4 best-guess rows superseded, 4 vendor-stated rows added, 36 rows total); 4 ambiguous cards → resolved_by_reply; Westline → clarified (flags); P3-T5 guard kept for non-clarification replies (scope-only override); questionnaire answers unchanged (clarification with no question in scope makes no model calls) |
  | 6.17 | Mock paste path: Add response sheet offers "This answers the clarification sent <date>" when one is outstanding → same `is_clarification` response without the mailbox (CLAUDE P6-T3) | ✓ Responses → Westline → Add response: "This answers the clarification sent 24 Sep" (ticked, only while the vendor is clarification_sent) → `POST /api/responses` with `clarification_of` → `mock_paste` response, `is_clarification`, same request/supersedes; six stages ran; same 4 cells reviewed at gold values (screenshot + snapshot diff) |
  | 6.18 | Done-when on MER-0419, both paths, each twice: Westline 5/9/15/19 → reviewed at ₹27,960 / ₹68,700 / ₹14,080 / ₹14,475; 4 ambiguous cards resolved_by_reply; Westline clarified; every other cell byte-identical (script diff); eval 150/150; drawer chain says pack size came from the clarification reply with a link (CLAUDE P6-T3, gold `after_clarification`) | ✓ four runs on MER-0419, reloaded before each (nothing else cleared): run 1 portal + Sync (UI), run 2 mock paste (UI), run 3 portal + Sync (endpoints, script), run 4 mock paste (endpoints, script). Every run: L5/L9/L15/L19 ambiguous (guesses 27,960 / 54,960 / 14,080 / 11,580) → reviewed ₹27,960 / ₹68,700 / ₹14,080 / ₹14,475; 4 ambiguous cards → resolved_by_reply; Westline clarified; snapshot diff: cells changed = westline L5/L9/L15/L19 only, vendors changed = westline only, answers 0 changed, ledger 32 active before/after (4 superseded + 4 new); `npm run eval` 150/150, questionnaire 50/50, Westline "confirmed 26, reviewed 4"; drawer L9: "Vendor's clarification reply (24 Sep 2026): pack size 20; price as first quoted, ₹1,374 per bundle" linking to the reply, "Reviewed · Westline Packaging (clarification reply)" |
  | 6.19 | clar-2 / reply about an already-clarified line: latest reply wins, nothing duplicated; rule recorded | ✓ rule recorded (DECISIONS P6-T3 (j)): latest reply wins inside its scope, nothing duplicated. Test on MER-0419 OrientPack L14: ask (clar-1) → reply "USD 63.00" → Sync → L14 reviewed ₹5,238.45, card resolved_by_reply; second reply to the same clarification "USD 64.50" → L14 ₹5,363.18 from the newer reply, still 30 OrientPack cells, ledger L14 = 1 active + 1 superseded; re-run of that reply → identical (no new rows). Found + fixed on the way: map had no request context ("Item 14" → Unmatched); resolved cards lost the request id (second reply out of scope) |
  | 6.19a | Found while reading: seeded RFx (MER-0419) have no dispatch email — clarification threads on the vendor's latest message, else no In-Reply-To; the portal still lists the clarification | ✓ MER-0419 has no dispatch email and its seeded replies carry no Message-ID → the Westline clarification went out as a new thread (no In-Reply-To); the portal still lists it ("From Sujit Menon · clarification"); the reply threads on it (In-Reply-To = the clarification) and the Overview links "in reply to our clarification" |
  | 6.19b | Found while reading: seed reload deletes only `source='seed'` responses — it must also remove clarification replies, clarification emails and mailbox rows of the reloaded vendors (needed for 6.18 reloads and 6.23) | ✓ `clearResponses` also removes the reloaded vendors' clarification replies, clarification emails, their mock mailbox rows and `.eml` files. After `npm run pipeline:seed` following run 1: MER-0419 clarification responses 0, communications = 5 inbound seed replies only, mailbox rows 0, open review items 14, vendors all responded, eval 150/150 |
  | 6.19c | Clarification round trip visible in words: Timeline (`clarification.sent`, reply processed), ledger row "pack size from vendor clarification", vendor status chips "clarification sent" / "clarified" (prototype Timeline/Ledger, TRD §17.4) | ✓ Timeline tab: "Sujit Menon sent Westline Packaging a clarification (4 points: Item 5, Item 9, Item 15, Item 19)", "Clarification reply received from Westline Packaging (mailbox)", "Processed Westline Packaging's clarification reply — 4 cells resolved", "… synced the inbox — 1 new"; Ledger: "Bundle sizes 25 / 20 / 50 / 40 from the vendor's clarification reply (4 lines) · vendor stated" (the folded row first used the best-guess sentence — fixed); Overview vendor row chip "clarified" (amber "clarification sent" while waiting); Responses row links "clarification reply 24 Sep" |
  | 6.20 | Production, TRD §22 item 8 for the mock: issue throwaway RFx → 5 emails in vendor mailboxes → portal reply with the OrientPack synthetic photo → Sync → response + extraction → cells; then the clarification loop once on production | ✓ production (quotelens-seven.vercel.app, deploy of 0b163c5): throwaway MER-0421 (draft via API from MER-0418 data) issued with the UI Issue dialog → Outbox "Five emails sent", 5 × sent (mock); each of the 5 portal mailboxes shows "1 email from Meridian Foods" with its own tag (`sourcing+rfx-mer-0421-<vendor>@…`); portal reply from OrientPack with `OrientPack_Rate_Card_PHOTO_synthetic.jpg` (338 KB) → "Sent — it arrives when the buyer syncs the inbox" → header Sync inbox → "Synced — 1 new reply from OrientPack Ltd" in 60 s incl. extraction → OrientPack 29/30 priced, line 14 unreadable (ink blot) card. (A first photo reply went from Anand's mailbox by mistake — my click — and was correctly matched to Anand by its tag.) Clarification loop on production: Ask vendor on Anand L14 → redraft after unticking the questionnaire point → Send → "Clarification sent to Anand Box Works for item 14. Waiting for their reply." → reply from Anand's mailbox to `…-anand-clar-1@…` → "Sync inbox — Anand Box Works replied" → "Synced — 1 new clarification reply from Anand Box Works" → Anand L14 reviewed ₹5,238.45 (USD 63.00 × 83.15, chain "Vendor's clarification reply … price stated"), card resolved_by_reply, Anand clarified, OrientPack L14 untouched |
  | 6.21 | Production approver: no Sync inbox, no Ask vendor, no Send; sync / clarify / clarify-send → 403 | ✓ production as Priya (MER-0421): `/api/email/sync`, `/api/email/pending`, `/api/email/vendor-reply`, `/api/clarify`, `/api/clarify/send` → 403 (also with a real review item, and `/api/review/{id}/ask-vendor` → 403); Comparison page has no Sync inbox; Review page has no Ask vendor / Send / Sync inbox, 7 × "Waiting for Sujit." |
  | 6.22 | Tests (tag parser, `.eml` round trip, clarification scoping if unit-testable), lint, build green; pipeline:seed 150/150; push | ✓ 67 tests (17 files) green — new: `tag.test.ts` (7), `eml.test.ts` (nodemailer → mailparser round trip), `clarify-merge.test.ts` (4: first price + stated pack, pack in the unit, new price wins, empty answer changes nothing); eslint clean, `tsc` clean, `next build` green; `npm run pipeline:seed` 150/150, questionnaire 50/50 (final reload); pushed to main (Vercel deployed, production checks 6.20/6.21 on that deploy) |
  | 6.23 | Final state: MER-0419 seeded (14 open items, all vendors responded, no clarification rows); MER-0417 unchanged; MER-0418 untouched draft; RFx list = these three; throwaway RFx + mailbox rows + files deleted by script | ✓ script check after `_cleanup.ts --go`: RFx list = MER-0417 (reviewing v1, 5 seed replies, realistic eval 150/150, questionnaire 43/50 — unchanged), MER-0418 (draft v0, 0 communications, 0 responses, 5 vendors not invited — untouched), MER-0419 (reviewing v1, 5 seed replies, 14 open review items, all vendors responded, 0 clarification responses, 0 clarification emails, eval 150/150, questionnaire 50/50); throwaway MER-0420 (local) and MER-0421 (production) deleted with 16 + 17 storage files and 8 + 9 mailbox rows; 2 loose test mails deleted; `mock_mailbox` empty; storage `rfx/` folders only for MER-0417/0419 |
  | 6.24 | Side by side with the prototype at 1440 px: Review email block + waiting note, Sync inbox button, Overview timeline with a clarification round trip, portal mailbox; differences fixed or recorded | ✓ prototype (`design/prototype.html`, states clarSent / clarified) vs app at 1440 px: header Sync inbox + Ask match; Review with the clarification out matches (primary Sync button, DESIGN note, email block); Overview after the reply matches (Westline out of Needs you, 30/30); portal mailbox has no prototype screen (built from DESIGN card/email/empty rules). Fixed from the check: ledger wording, "failed Q6, Q1" chip (read a "Q1" inside an answer), pasted reply not threaded. Kept differences recorded in DECISIONS "P6 visual check" |
  | 6.25 | Rows 6.1–6.24 ticked with evidence; header, eval line, what works / doesn't, known issues, open questions updated | ✓ rows 6.1–6.24 (+ 6.19a–c) ticked with evidence; header (P6 ✅, next P7), eval line, P6 "what works / what doesn't", Known issues (flaky connection + timeouts, audit history, clarification timing) and Open questions (asked cards in the Open list, demo-script Gmail steps) updated |
- [x] **P7 Scenarios, award memo, approval, Decide — checkpoint passed** (commits 989f095 … final). **What works:** a deterministic allocation engine (cheapest per line, by group, weighted) that gives the Ask totals exactly (Q1 ₹4,53,38,297, Q5 ₹4,20,18,677; best single vendor Sri Balaji ₹4,58,35,488, saving ₹4,97,191 / 1.08 %); Save as scenario on Ask answers (both roles) and a New scenario rule builder (buyer); scenario comparison on the Award tab with lines, overrides (with reason) and revert; the award memo as an HTML page and a PDF (six parts, ledger, open items, rule + SQL, ₹ in IBM Plex) with a number-checked P-MEMO narrative; Send back with a note, regenerate, Approve → RFx **Awarded** and locked (every editing endpoint 409, buttons hidden, lock note on every tab, RFx list shows the awarded total); Priya's Decide page as her default tab; all run on production. **What doesn't / limits:** the memo's prose is model-written — numbers are checked, wording isn't (once "landed unit price" for a unit-price rule, and Anand called "disqualified" when it is "not cleared yet"); memo generation takes 13–20 s; Gemini returned 503 "high demand" twice in one session (the UI shows the error, a retry works).
- **Phase 7 requirement checklist** (sources: handoff P7 §5, CLAUDE P7-T1/T2, TRD §6.18/§6.20/§9.10/§13.5/§14.1–14.3/§16/§17.11–17.12/§22 item 7, PRD §8 Stages 8–9/§10/§13, DESIGN §2.3/§2.13–2.16/§3.8–3.9/§4/§5). Rows numbered 7.x. Approve only on throwaway RFx, never MER-0417/0418/0419.
  | # | Requirement (source) | Status / evidence |
  |---|---|---|
  | 7.1 | `src/lib/scenarios/allocate.ts` deterministic, no model: `cheapest_per_line` {qualified_only, price_basis unit/landed}; `grouped` [{filter ply/item_type/delivery_location/line_nos, rule}] with uncovered lines unallocated + flagged; `weighted` {price, questionnaire} score = w_p × min/price + w_q × mandatory passes share; eligible = confirmed/inferred/reviewed with a price, best guesses only with include_best_guess; per line winner, price, annual value, runner-up + price, gap %, reason; unit tests: tie (lower vendor name wins, said so), nobody qualified → unallocated, grouped gap, weighted by hand (TRD §14.1, handoff 7.1) | ✓ `src/lib/scenarios/allocate.ts` (pure, no model): cheapest_per_line / grouped / weighted + fromQuery; eligibility confirmed/inferred/reviewed with a price, best guesses only with include_best_guess (marked '— best guess'); per line winner, price, annual value, runner-up + price, gap %, reason. `allocate.test.ts` 7 tests green: tie → 'cheapest qualified (tie with Beta at the same price; lower name wins)'; nobody qualified → unallocated 'no qualified quote'; grouped gap → 'not covered by any group'; weighted by hand (0.5/0.5 → Gamma score 1.00, gap 11.11 %; 0.9/0.1 case); landed; baseline; from-query; rule text |
  | 7.2 | Totals: total, vendors used, share per vendor, single-source lines, unallocated lines; baseline = cheapest single vendor among vendors who priced every line (else over the lines each priced, with a note) + savings_vs_baseline; cheapest qualified/unit on MER-0419 = Ask Q1 ₹4,53,38,297; baseline = Q2 Sri Balaji ₹4,58,35,488, saving ₹4,97,191 (1.08 %); DESIGN §3.8 Kohinoor example vs TRD rule recorded (TRD §13.5, §14.2) | ✓ totals(): total, vendor_count, share per vendor, single-source, unallocated lines; baseline() per DECISIONS P7. MER-0419 by script and API: cheapest qualified/unit ₹4,53,38,297 = Ask Q1 (same 30 winners and prices as the saved Q1 answer); baseline Sri Balaji Packaging ₹4,58,35,488 = Q2, saving ₹4,97,191 (1.08 %); Decide card '1.1% lower · ₹5.0 L' vs Sri Balaji ₹4.58 cr; DESIGN §3.8 Kohinoor example vs TRD rule recorded in DECISIONS |
  | 7.3 | Migration 0010 only for what the schema lacks (send-back status + note, original allocation for override revert, baseline note); RLS on, no anon/authenticated grants | ✓ `0010_award.sql` applied (npm run db:migrate): awards.status + 'sent_back' with note/by/at, unique awards(rfx_id), scenario_lines.auto (for revert), scenarios.baseline_note; tables keep RLS on and no anon/authenticated grants (0001) |
  | 7.4 | `POST /api/scenarios` {rfx_id, name, rule} or {rfx_id, name, query_id} → scenario + lines + totals; from a query only when rows have line_no + a vendor column + one row per line; winners copied, prices/runner-up/gap/totals from the comparison; rule_text = the question; SQL kept for the memo; `GET /api/scenarios?rfx=` list with totals; `DELETE` buyer only; both roles save (TRD §13.5, §16, DESIGN §4) | ✓ `POST /api/scenarios` {rule} or {query_id} (`src/lib/scenarios/index.ts`): Q1 and Q5 answers → 30-line scenarios, winners copied, prices/runner-up/gap/totals recomputed (rule inferred from the SQL, question + SQL kept in rule for the memo); a non-allocation answer → 400 NOT_AN_ALLOCATION; `GET /api/scenarios?rfx=` list with totals + lines (7 on MER-0419 during testing); DELETE buyer only (409 IN_USE when a memo was drafted from it); Priya saved 2 from answers in the UI, rule body as Priya → 403 (checked in 7.21) |
  | 7.4a | Found while reading: P4's Q5 answer was one row (a total), so it can't be saved as a scenario — allocation questions must return one row per line (P-SQL tuning per CLAUDE §9 if needed) | ✓ P-SQL v4 (v3 archived, DECISIONS P7): Q5 → 30 per-line rows, ₹4,20,18,677, 'better by ₹33,19,620', saveable; Q1/Q3/Q4/Q7/Q8 regression unchanged from P4; Q2 per-line now, its two-total bar chart kept by chartSpec (new test) |
  | 7.5 | Save as scenario on Ask answer cards only when the rows qualify (no dead button); name prompt defaulting to the question; toast "Saved — …"; Sujit and Priya (DESIGN §2.13, TRD §17.9) | ✓ browser: Save as scenario on allocation answers only (allocationColumn; Q6-style state lists and 1-row answers get none); inline name prompt defaulting to the question; Priya on Decide saved 'Q1 cheapest qualified per line' and 'Q5 5-ply qualified + 3-ply overall' (MER-0419), Sujit in the Ask sheet saved 'Q1 cheapest qualified per line' (MER-0421) → card reads 'Saved as “…” — compare it on the Award tab', Award tab refreshes behind the sheet with count pill 1; toast 'Saved — <name>' (1.9 s, seen in the P7 run via the POST 200s); hidden on a locked RFx |
  | 7.6 | `POST /api/scenarios/{id}/override` {rfx_line_id, vendor_id, reason}: buyer only, reason required, vendor must have an eligible price (else says why); recomputes totals, is_override + reason, audit event, listed in the memo's open items; revert possible | ✓ UI on MER-0419 (Q5 by rule, line 1 → Sri Balaji, 'Incumbent tooling for this carton'): override chip, runner-up Kohinoor −4.39 % (premium), total ₹4.20 cr → ₹4.22 cr, saving ₹38,16,811 → ₹36,82,791 (= line difference ₹1,34,020); Revert → back to ₹4.20 cr. Found + fixed: options ignored the line's group rule (offered Westline/Anand on a qualified 5-ply line) — now per line. MER-0421 line 7 override → memo open items 'Line 7 → Sri Balaji Packaging instead of Kohinoor Corrugators Pvt Ltd: Balaji holds the printing plates for this tray'; audit + Timeline row; reason required (button disabled without it); Priya → 403 (7.21) |
  | 7.7 | Scenario comparison on the Award tab: Scenario (600, green selected chip) · Rule · Annual total · Vendors · Lines · Single-source · vs first (+ vs baseline); share per vendor as an inline stacked bar (no library); open a scenario → its lines (winner, price, runner-up, gap, reason, override badge) + override (TRD §14.2, §17.11, DESIGN §3.9) | ✓ Award tab (screenshots 1440 px): Scenario (600) + green 'selected' chip, Rule, Annual total (₹ cr, full value in the tooltip), Vendors, Lines n/30 (+ amber 'n unallocated'), Single-source, vs first, vs best single, Share (one-colour stacked bar, vendor % in tooltip + legend); row / 'Lines' opens the 30 lines (winner, ₹/1000, annual, runner-up + price, gap, reason, override chip) with Override… / Revert for the buyer |
  | 7.8 | New scenario rule builder on the Award tab, buyer only: rule type (cheapest per line / by group / weighted), qualified only, price basis, include best guesses, groups by ply / item type / plant, price-vs-questionnaire slider (TRD §17.11; cut-list 1 says which) | ✓ New scenario (buyer only): Cheapest per line / By group / Weighted, Unit / Landed, 'Only vendors who cleared the questionnaire', 'Include best guesses', groups by Ply / Item type / Plant with add/remove and per-group rule, price-vs-questionnaire slider (5 % steps); saved in the UI 'Weighted 70/30 (unit)' → ₹3,72,58,133, 3 vendors, 30/30; grouped rule (5-ply qualified + 3-ply overall) and cheapest-qualified built through the same POST /api/scenarios body. weighted kept (cut-list 1 not used) |
  | 7.9 | Done-when CLAUDE P7-T1 on MER-0419: Q1 and Q5 asked in the Ask sheet → Save as scenario on both → compared side by side (Q1 ₹4,53,38,297; Q5 ₹4,20,18,677, better by ₹33,19,620 / 7.32 %); the same two built with the rule builder give the same totals | ✓ MER-0419: Q1 and Q5 asked through the UI (Decide Ask box, as Priya) and saved as scenarios → side by side on Award: Q1 ₹4,53,38,297 · Q5 ₹4,20,18,677 (vs first −7.3 %; narrator 'better … by ₹33,19,620'; 7.32 % by script). Same two by rule: 'Q1 by rule' ₹4,53,38,297 and 'Q5 by rule' (grouped) ₹4,20,18,677, same 30 winners and prices line by line (script). Numbers = P4's |
  | 7.10 | `src/lib/award/memo.tsx` (react-pdf) with the six TRD §14.3 parts, every number from the database (header + recommendation + basis; allocation table; totals, baseline, exclusions, single-source, validity; full ledger; open items with value at stake + overrides; rule + SQL + signatures); DESIGN §2.16 look; Indian grouping; ₹ renders (font checked) | ✓ `src/lib/award/memo.tsx`: six parts from memo_json (all numbers from the DB), DESIGN §2.16 look (uppercase muted headings, table rows, signature grid, page footer 'Page n of m'), IBM Plex with ₹. MER-0421 PDF read back (pypdf): 5 pages, all ten headings, 30 allocation rows, 125 ₹ signs as text, 8 ledger rows = Ledger tab 8, override in open items, 13 unresolved cells + value at stake, SQL printed (from an answer), 'Prepared — Sujit Menon, Category Buyer', 'Approved — ________________' on the draft |
  | 7.11 | P-MEMO TRD §9.10 verbatim, strong model, Zod five strings; numbers pre-formatted; number check (one retry naming offenders, leftovers recorded and shown); narrative mentions unresolved cells when any | ✓ P-MEMO verbatim (strong, LOW thinking), Zod 5 strings; inputs pre-formatted; number check + 'unresolved cells named' check with one retry; narrative_unverified stored and shown. Three generations (MER-0419 once, MER-0421 twice): unverified [] each time; risks section names 13 unresolved cells, ₹73,16,208 at stake |
  | 7.12 | `POST /api/award/{rfx}/generate` {scenario_id} buyer/admin (approver 403): memo_json, PDF to outbound `rfx/{id}/outbound/award/…pdf`, awards row draft; regenerate replaces the draft (never an approved one); audit award.memo; `GET /api/export/memo/{award_id}` streams the PDF (both roles) | ✓ POST /api/award/{rfx}/generate: memo_json + PDF to outbound rfx/{id}/outbound/award/{award}.pdf, awards draft, audit award.memo; 13–20 s; regenerate after send back replaced the same row (status draft, note cleared, 'generated the award memo again'); approved award → 409; Priya → 403; GET /api/export/memo/{id} 200 application/pdf as Priya and Sujit |
  | 7.13 | `POST /api/award/{rfx}/approve` approver only (buyer 403): approved_by/at, rfx.status awarded, audit award.approved, PDF re-rendered with Priya's signature, toast "Approved — RFx locked"; Send back (approver, note required) → stored per 7.3, audit award.sent_back, toast "Sent back to Sujit with your note"; Sujit sees the note and can regenerate | ✓ MER-0421 in the UI: Priya Send back (confirm disabled until a note) → toast 'Sent back to Sujit with your note', lead 'You sent the memo back…'; Sujit sees the amber note card and regenerates; Priya Approve → confirm 'Approving locks MER-0421…' → 'Approve and lock' → toast 'Approved — RFx locked'; awards approved (by/at), rfx.status awarded, audit award.approved; PDF re-rendered: 'Approved — Priya Raghavan, VP Procurement'; Sujit approve / send back → 403 |
  | 7.14 | Award tab (both roles): leads per state (pick / drafted from <scenario> / Approved); buyer scenario select + primary Generate memo, then amber "awaiting Priya"; approver Download PDF · Send back · primary Approve; memo preview in HTML (DESIGN §2.16) + PDF download | ✓ Award leads per state seen in the UI: none ('No scenario saved yet. Save one from an Ask answer, or build one below with New scenario.'), pick ('Pick a scenario and generate the memo. Every number in it comes from the grid and the ledger.'), drafted ('Memo drafted from <b>…</b>. Waiting for Priya's approval.' / '…Read it and approve, or send it back.'), sent back (both roles), approved ('<b>Approved.</b> The grid is locked and the memo is on file.'); buyer select + Generate memo ('Generating… n s'), then Download PDF + amber 'awaiting Priya'; approver Download PDF · Send back · Approve; HTML memo + Download PDF |
  | 7.15 | Lock once awarded: every mutating endpoint for that RFx → 409 LOCKED (listed, each tested by script); UI hides review actions, Ask vendor, Sync inbox, Add response, Load seeded responses, Re-run stages, overrides, New scenario, Generate memo; lock note on Overview / Review / Comparison / Award; Ask still works; header "Awarded"; RFx list Awarded + Annual value from the approved scenario | ✓ `_lock.ts` on awarded MER-0421: 21 mutating endpoints refused (18 × 409 LOCKED incl. review, bulk, responses, seed, run-all, stage, assign-vendor, sync, vendor-reply, clarify, clarify/send, scenario save as both roles, override, delete, generate, approve, send back; PATCH/copilot/issue 409 FROZEN); Ask 200. UI as Sujit: lock note 'Awarded — read-only. Memo approved by Priya Raghavan.' on Overview/Responses/Review/Comparison/Award, no action buttons (Sync inbox, Load seeded, Add response, review actions, New scenario, Generate, overrides), queue cards 14 × 'Read-only — the RFx is awarded.'; header 'Awarded' (green dot); RFx list 'MER-0421 … Awarded … ₹4.53 cr' |
  | 7.16 | Timeline rows in words: scenario saved (with total), memo generated from '…', approved — RFx locked, sent back with the note, overrides (PRD #34) | ✓ Timeline (MER-0421 API): 'Sujit Menon saved scenario “Q1 cheapest qualified per line” (₹4.53 cr) from an answer', '… gave line 7 of “…” to Sri Balaji Packaging instead of Kohinoor Corrugators Pvt Ltd — Balaji holds the printing plates for this tray', '… generated the award memo from “…”', 'Priya Raghavan sent the memo back: “Please get Kohinoor's validity extended…”', '… generated the award memo again …', 'Priya Raghavan approved the award — RFx locked'; also revert/delete rows |
  | 7.17 | Approver tabs Decide · Comparison · Award, `/rfx/{id}` → Decide; buyer tabs … Comparison · Award (DESIGN §2.3, §4) | ✓ Priya: tabs Decide · Comparison · Award, /rfx/{id} → /decide (browser), /overview → /decide; Sujit: Overview · Responses · Review · Comparison · Award (count pill = scenarios until a memo) |
  | 7.18 | Done-when CLAUDE P7-T2 on a throwaway RFx: load seeded (clean) → save Q1 scenario → override one line with a reason → Generate memo → PDF read back by script (six sections, 30 allocation rows, full ledger, ₹, override in open items) → Send back (Priya) → regenerate (Sujit) → Approve (Priya) → locked (7.15), memo with both signatures; MER-0419 eval still 150/150 | ✓ throwaway MER-0421 (draft via API from MER-0418 data — MER-0418 only read; issued in the UI; Load seeded responses (clean) + stages → 150/150, questionnaire 50/50): Q1 saved in the Ask sheet → line 7 override with reason → Generate memo (UI) → PDF read back (6 parts, 30 rows, ledger 8/8, ₹, override) → Priya Send back → Sujit regenerate → Priya Approve → locked (7.15), PDF with both signatures; MER-0419 eval checked in 7.22 |
  | 7.19 | Decide page (DESIGN §3.8, built per handoff §0 A): computed lead, See the grid · Award/Read the memo, "Against the best single vendor" card (same number as 7.2), Ask box with Cost / Risk / Vendors groups, answer cards newest first; Priya's default tab | ✓ built (DECISIONS P7): /rfx/{id}/decide for Priya — lead 'Five vendors quoted the 30 lines. Three cleared the questionnaire. … ₹4.53 cr a year. 13 cells are still unresolved…', See the grid · Award/Read the memo, card '1.1% lower · ₹5.0 L a year · Best single vendor: Sri Balaji Packaging at ₹4.58 cr.' (= Q2), Ask box + Cost / Risk / Vendors groups, answers newest first, Earlier questions · 20; Q1 and Q5 asked there (Gemini 503 once → error toast, retry fine) |
  | 7.20 | Production, TRD §22 item 7 on a throwaway RFx: save a scenario, compare two, generate the memo, approve as Priya, grid locks; PDF downloaded from production and read back; throwaway deleted by script | ✓ production (quotelens-seven.vercel.app, deploy of 57be16e): throwaway MER-0422 (draft via API from MER-0418 data) issued in the UI → Load seeded responses (clean) → six stages run on Vercel for all 5 replies (run-all 24–180 s; OrientPack photo extract 156 s) → eval 150/150, questionnaire 50/50; Sujit asked Q1 in the Ask sheet → Save as scenario (₹4,53,38,297) + New scenario 'By group' → '5-ply qualified + 3-ply overall (unit)' ₹4,20,18,677, compared (−7.3 %, vs best −1.1 % / −8.3 %); Generate memo → Priya Approve → 'Approved. The grid is locked…' + lock note; memo PDF downloaded from production (200, 50.6 kB): 5 pages, 10 headings, 30 rows, 124 ₹, ledger 8 = 8, both signatures, unverified []. Throwaway deleted by script (7.23) |
  | 7.21 | Production boundaries: Priya saves a scenario but override / generate / new-scenario-by-rule / delete → 403; Sujit approve / send back → 403; awarded RFx → 409 LOCKED on mutating endpoints | ✓ production `_roles.ts` on MER-0419: Priya override / New scenario by rule / delete / generate → 403; Sujit approve / send back → 403 (Priya saving from an answer → 200, 7.5). `_lock.ts` on awarded MER-0422 (production): 21 mutating endpoints refused (18 × 409 LOCKED, 3 × 409 FROZEN), Ask 200 |
  | 7.22 | Tests (allocation rules, baseline, memo number check), lint, build green; `npm run pipeline:seed` 150/150; push | ✓ 77 tests (18 files; new: allocate.test.ts 7, allocationColumn + per-line chart in result.test.ts, statedPack in clarify-merge.test.ts); eslint clean on committed code; tsc clean; next build green (fonts traced into /api/award routes). npm run pipeline:seed: 1st run 120/150 (Gemini 504 on OrientPack extract → re-run), 2nd 146/150 (Westline pack sizes invented by the extractor → fixed with statedPack, DECISIONS), fresh 3rd run 150/150, questionnaire 50/50; realistic MER-0417 eval 150/150, questionnaire 43/50; pushed |
  | 7.23 | Final state: MER-0419 seeded (14 open items, vendors responded, no P7 scenarios/awards/queries left, reviewing); MER-0417 unchanged, not awarded; MER-0418 untouched draft; RFx list = these three; throwaway RFx + files deleted | ✓ `_final.ts` after cleanup + reload: MER-0417 reviewing v1, 150 cells, 0 scenarios/awards (not awarded, unchanged); MER-0418 draft v0, 0 comms/responses; MER-0419 reviewing v1, 150 cells, 14 open review items, vendors responded, 0 scenarios, 0 awards, 64 queries = the pre-P7 snapshot (12 P7 queries deleted); throwaway MER-0421 (local) and MER-0422 (production) deleted with 31 + 19 storage files and 5 + 5 mailbox rows; mock_mailbox 0. Open: the list also shows MER-0420 'Untitled RFx' — created from another session (not this phase's), left untouched and raised under Open questions |
  | 7.24 | Side by side with the prototype at 1440 px: Award before memo, with a draft memo (buyer + approver), after approval (lock note); scenario comparison; Decide; Ask card with Save as scenario; differences fixed or recorded | ✓ prototype vs app at 1440 px: Decide (same lead, card, Ask box, groups; card padded — the prototype's card text sits on the border), Award draft as buyer and approver, Award approved + lock note, scenario comparison, Ask card with Save as scenario. Differences kept and recorded (DECISIONS P7 visual check) |
  | 7.25 | Rows 7.1–7.24 ticked with evidence (script check: requirement text unchanged, older rows unchanged); header, eval line, what works / doesn't, Known issues, Open questions updated | ✓ rows 7.1–7.24 (+ 7.4a) ticked with evidence; the tick helper cuts each row by its fixed 7.x prefix, refuses evidence containing a pipe (it caught one here), and `--check` confirms every P7 requirement text unchanged and all 116 older rows byte-identical; header, eval line, P7 what works / doesn't, Known issues, Open questions updated |
- [ ] **P8 in progress.** P8-T1 built and tested locally (commit "P8-T1"): one `/settings` page (Email transport, Decision layer, FX rates, Landed cost & discounts, Recent changes, Eval — seed set, Model calls), `GET/PUT /api/settings`, `POST /api/eval/run`, `GET /api/logs`, migration 0011, discount "net" in normalise, "Change in settings" on assumption cards, Overview Transport "change" link. Eval after the normalise change: MER-0419 150/150, questionnaire 50/50. Doesn't yet: production checks (P8-T4), MER-0417 completion (P8-T2). Next: P8-T2.
- **MER-0417 decisions (Sujit, production UI, 24 Sep 2026 22:49–22:52 IST).** Handoff §0 B defaults (no answers from Sabarish yet); no value was taken from the gold key.
  - Westline items 5, 9, 15, 19 (bundle size not stated) → **Ask vendor**, sent as clarification clar-1; the reply in the dataset (`westline_clarification_reply.txt`) pasted as "This answers the clarification" → 25 / 20 / 50 / 40 per bundle → ₹27,960 / ₹68,700 / ₹14,080 / ₹14,475 per 1000, reviewed.
  - OrientPack line 14 (price under the ink blot) → **Exclude**, reason "Unreadable on the rate card photo: an ink blot covers the price, only the last digits '…32' show" (photo checked in full resolution; the whole-number part is hidden).
  - Anand items 23–30 ("rest same as last year") → **Treat as not quoted**: no prior price list on file.
  - Anand Q6 (BRC "audit is in process, expecting by Dec") → **Treat as No**: not certified today (conservative) → Anand not cleared.
  - OrientPack "questionnaire not returned, 7 mandatory answers missing" (not in the handoff defaults) → **Dismiss**: the answers stay missing, so OrientPack stays not cleared (conservative, like Anand Q6); asking would leave the RFx waiting on an answer the dataset doesn't contain.
  - Acknowledged as stated: Kohinoor printed rates net of 2.5 % (grossed up ÷ 0.975), Balaji 3 % total discount (not applied — condition "all 30 items for the full year"), USD → INR 83.15, freight excluded for OrientPack and Anand (₹180 per 1000 in landed), Kohinoor validity 30 days, Kohinoor 3 lines not quoted (partitions not made).
  - Result: cleared vendors Balaji and Kohinoor; Westline not cleared (its own Q6 = No); 0 open items.
- **Phase 8 requirement checklist** (sources: handoff P8 §0/§5, CLAUDE P8-T1…T4 and §10, TRD §6.19–6.20/§10/§11.3–11.4/§11.7/§16/§17.13–17.15/§18/§19/§22, PRD §4/§10, DESIGN §2.2/§2.16/§3.10/§4/§5/§6). ✓ only with evidence.
  | # | Requirement (source) | Status / evidence |
  |---|---|---|
  | 8.1 | `/settings` page (buyer/admin; approver: no gear, `/settings` redirects, APIs 403) per DESIGN §3.10: h1, sub-line, 2-column card grid, then "Eval — seed set" and "Model calls" sections with anchors `#eval` / `#model-calls`; rail gear active there; Overview Transport row gets a buyer-only "change" link | ✓ `/settings` built (DESIGN §3.10): h1 + sub-line, 2-column cards, Recent changes, then Eval — seed set (#eval) and Model calls (#model-calls); screenshots at 1440 px as Sujit; gear link class active on /settings; Priya: rail shows only RFx, /settings → /rfx (streamed NEXT_REDIRECT, no settings data in the HTML), APIs 403; Overview Transport row reads 'Mock email · change' → /settings (buyer page only; approver is redirected to Decide) |
  | 8.2 | `GET /api/settings` (all keys with defaults) and `PUT /api/settings` `{key, value}` (buyer/admin): Zod per key (thresholds 0 < review < act ≤ 1; FX rate > 0 with date and source; provider enum; discount enum; freight ≥ 0), unknown keys 400, audit event `settings.changed` with before / after (TRD §16, §17.13) | ✓ GET /api/settings → all 8 keys with defaults + openrouter_key flag; PUT validated by per-key Zod (settings-schema.test.ts, 3 tests: thresholds, FX, enums/freight); curl: unknown key 400 UNKNOWN_SETTING, act 0.5 < review 400, rate −1 400, provider openai 400, freight −5 400, email gmail 400, missing value 400; logged out 401, Priya 403; freight 180→181→180 wrote two settings.changed events with before/after and rfx_id null |
  | 8.3 | Email transport card: Mock selected; Gmail disabled "not in this build"; Resend disabled "not configured"; hint "All three raise the same “response received” event."; no dead radio (handoff §0 D) | ✓ Card: Mock checked; Gmail disabled 'not in this build'; Resend disabled 'not configured'; hint 'All three raise the same “response received” event.'; only mock accepted by PUT (DECISIONS P8 settings behaviour b) |
  | 8.4 | Decision layer card: sentence; Auto (amber "no key · Gemini" without `OPENROUTER_API_KEY`), Gemini only "LLM-estimated", Jev only "measured" (without a key: says it falls back to Gemini); act / review thresholds editable with current values; hint that thresholds apply on the next stage run only (UI + DECISIONS) | ✓ Card: sentence; Auto with amber 'no key · Gemini'; Gemini only 'LLM-estimated'; Jev only 'measured' disabled with 'needs an OpenRouter key' (no key; DECISIONS c); act/review inputs 0.85/0.6 + Save thresholds; hint and toasts 'Applies from the next stage run; cells already written keep their state and chain.' (DECISIONS a). UI: 0.5 → toast 'Couldn't save: Thresholds must satisfy 0 < review < act ≤ 1. (BAD_REQUEST)'; 0.86 saved, restored 0.85 |
  | 8.5 | FX rates card: table (currency, rate, date, source) with edit and add; hint "Changing a rate writes a new ledger entry; old cells keep their chain." made true and tested on a throwaway RFx (change USD → before re-run cells and chains unchanged → re-run normalise OrientPack → new `fx_rate` ledger row, cells at the new rate); USD restored to 83.15 | ✓ Table with Edit / Add currency / Remove; UI: add 'US' → error toast, add EUR 90 then Remove; throwaway TEST p8 settings: USD → 84 (2026-09-24) → OrientPack cells + chain byte-identical before re-run (diff empty) → re-run normalise → ledger 'USD→INR at 84 (manual, 2026-09-24)', L1 69,675.54 → 70,387.80 (= ÷83.15×84), L2 57,475.77 → 58,063.32; USD restored 83.15 (DECISIONS e) |
  | 8.6 | Landed cost & discounts card: freight default (₹/1000), discount default gross / net (net → that vendor's cells × (1 − pct) on the next normalise run, tested on a throwaway, restored gross), include tax only if the pipeline implements it, cost of money "not in this build" (cut list 2); what is shown and why recorded | ✓ Card shows freight default (editable, saved 181 and back), Show gross / Apply to every line, cost of money 'not in this build'; include tax not shown (pipeline never applies it; DECISIONS f). Net implemented in normalise (DECISIONS 'Discount default net'): throwaway Balaji L1 70,610 → 68,491.70 with chain total_discount 3% and ledger 'applied to every line — Settings: net'; gross restored → 70,610 again |
  | 8.7 | Decision provider switch works end to end (TRD §22.12): switch, re-run classify on one response of a throwaway RFx, Model calls shows the provider per call; switched back to auto | ✓ Throwaway Westline: provider gemini → re-run classify → Model calls rows classify · gemini · gemini-3.5-flash-lite; back to auto → re-run → same provider (no OpenRouter key, honest); settings read back decision_provider auto (production run in 8.38) |
  | 8.8 | Every settings change visible in words (audit events without an rfx_id → "Recent changes" on the Settings page; recorded) | ✓ Audit events settings.changed (rfx_id null) listed in words under Recent changes on the Settings page, e.g. 'FX rates: EUR 90 (2026-09-24, manual), USD 83.15 … → USD 83.15 …', 'Freight default: ₹181 → ₹180 per 1000 pcs · Sujit Menon' (DECISIONS g) |
  | 8.8a | Assumption cards in the Review queue (FX, freight, discount) get "Change in settings" (DESIGN §3.6; deferred from P3 to P8) | ✓ Review queue: FX, freight and discount cards carry a quiet 'Change in settings' link to /settings (checked on the throwaway queue: 5 cards — gross-up, 3% total, USD → INR, two freight) |
  | 8.9 | `POST /api/eval/run` `{rfx_id}` (buyer/admin): `runEval(…, {save: true})`, returns totals + per-cell + questionnaire; an RFx with no seeded replies is refused cleanly (eligible RFx named); locked-RFx behaviour decided and recorded | ✓ POST /api/eval/run: MER-0419 → totals 150/150 + 150 per-cell + 50 per-question in 1.5 s, eval_runs row saved (per_question column, migration 0011); unknown RFx → 422 NO_GOLD_KEY 'The answer key only covers RFx loaded with the seed replies (MER-0417, MER-0419).'; Priya 403; allowed on a locked RFx (DECISIONS 'Eval page choices' b) |
  | 8.10 | Eval section (TRD §17.14, DESIGN §3.10): RFx select (seeded RFx), Run eval with elapsed time, stat cards (correct+flagged-OK / 150, correct · flagged OK, wrong · missing, questionnaire n/50), last run time, "judged on the current state — n cells reviewed by the buyer"; loads the latest `eval_runs` row without re-running | ✓ Section: RFx select (MER-0417 · realistic set, MER-0419 · clean set, plus seeded throwaways), Run eval shows 'Running… Ns' then refreshes, three stat cards (n/150 + questionnaire n/50 · correct · flagged OK · wrong · missing), 'Last run 24 Sep 22:38 · judged on the current state — before review' (or 'n cells reviewed by the buyer'); loads the latest eval_runs row without re-running; empty states for no eligible RFx / never run |
  | 8.11 | Per-cell table: line, vendor, expected, got, state, verdict (+ why), filters (verdict, vendor), wrong/missing first; each row opens the provenance drawer; questionnaire mismatches listed under it | ✓ Per-cell table Vendor · Line · State chip · Expected · Got · Verdict chip + why, sorted wrong → missing → flagged OK → correct, filters verdict (incl. Not correct) and vendor, scrolls inside its card with a sticky header; row click / Enter opens the provenance drawer (Anand L1: email line 5 '₹42/kg', mapping LLM-estimated p 1.00, chain); verdict legend under the table; Questionnaire card lists mismatches (expected vs got) or 'Every answer matches the key.' |
  | 8.12 | Eval numbers on the page = `npm run eval` for the same RFx (screenshot vs script output) | ✓ npm run pipeline:seed (after the normalise change) → MER-0419 TOTAL 150/150, questionnaire 50/50; the page right after Run eval: 150/150 · questionnaire 50/50 · 150 · 0 · 0 · 0; MER-0417 page 150/150 · 43/50 = its stored script run |
  | 8.13 | `GET /api/logs?rfx=` (buyer/admin; approver 403): latest model calls (cap 200, newest first) with filters rfx, purpose, provider, ok | ✓ GET /api/logs: newest first, cap 200, filters rfx / purpose / provider / ok; curl ok=false 200 for Sujit, 403 for Priya; provider=bogus → 400 |
  | 8.14 | Model calls section (TRD §17.15, DESIGN §3.10): Time · Purpose · Provider ("LLM-estimated (Gemini)" / "measured (Jev)") · Model · In · Out · Latency · ok; filters; failed calls show their error; the P7 Gemini 503/504 rows shown honestly | ✓ Section: Time · RFx · Purpose · Provider ('LLM-estimated (Gemini)') · Model · In · Out · Latency · Result; filters work in the UI (failed → 26 rows; memo; normalise → 'No model calls match these filters.'); failed extract row shows '{…504 Deadline expired… DEADLINE_EXCEEDED}' on hover and on click |
  | 8.15 | MER-0417 reloaded with the realistic set: eval recorded (expected 150/150, questionnaire 42–43/50); any wrong cell investigated first | ✓ run-seed-pipeline --rfx MER-0417 --set realistic: first 147/150 (Balaji L23–25 mis-mapped at p 1.00, twice in a row); investigated (extraction right, bare sheet columns, labelled ply/BF only in notes) → fix in map (DECISIONS 'Map reads the extractor's notes'), then 150/150 on two re-runs, questionnaire 43/50 (7 × OrientPack no questionnaire, expected); clean MER-0419 regression 150/150, 50/50 |
  | 8.16 | MER-0417 queue cleared as Sujit in the UI per handoff §0 B: every card decided with a reason; Westline clarification loop run for real; PROGRESS lists each decision; ledger and Timeline show them; 0 open items | ✓ Production UI as Sujit, 15 cards: Westline items 5/9/15/19 Ask vendor → Send (clar-1) → reply pasted from westline_clarification_reply.txt via Add response ('This answers the clarification sent 24 Sep') → 6 stages in 11 s → 4 cells reviewed at 25/20/50/40 per bundle (₹27,960 / ₹68,700 / ₹14,080 / ₹14,475), cards resolved_by_reply; the other 11 decided in the UI (list under 'MER-0417 decisions (Sujit)'); review_items: 0 open, 0 asked (7 confirmed, 2 overridden, 1 excluded, 1 dismissed, 4 resolved_by_reply); Ledger shows the exclusion, prior pricing → not quoted, Q6 treated as No, bundle sizes from the reply; Timeline shows each action by Sujit Menon in words |
  | 8.17 | Q1–Q8 asked once on MER-0417 as Priya on Decide — all computed, SQL shown, times recorded; answers listed in PROGRESS | open |
  | 8.18 | Two scenarios saved from the answers (Q1, Q5), compared on Award; totals equal the answers | open |
  | 8.19 | Sujit generates the memo from Q1 → PDF read back (six parts, 30 rows, ledger rows = Ledger tab, ₹, no unverified numbers, open items reflect the cleared queue) → then Priya approves → MER-0417 Awarded, locked, RFx list shows its annual value; approved PDF has both signatures | open |
  | 8.20 | Eval of MER-0417 after review recorded on the Eval page (judged on the reviewed state; number reported honestly with why any cell moved) | open |
  | 8.21 | Empty states (DESIGN §5: one bold sentence, one plain sentence, at most one button): RFx list, Responses, Review (no items), Comparison tabs, Award (no scenarios), Decide (no prices), Settings eval (never run), Model calls (none) | open |
  | 8.22 | Loading states: route-level `loading.tsx` skeletons for the RFx pages and Settings (Next 16 docs), no layout jump; long actions show elapsed time (pipeline, Ask, memo, eval) | open |
  | 8.23 | Error states with Retry (DESIGN §5): pipeline stage, Ask (question kept), memo generate, eval run, settings save; a forced network failure on each shows a toast with the code | open |
  | 8.24 | Numbers: every ₹ amount with Indian grouping (`money`, `inrShort`), no raw `toLocaleString()` of money, no "NaN" / "undefined" anywhere (grep + screenshots); percentages one or two decimals consistently | open |
  | 8.25 | Legend wherever states appear (grid, eval table, export legend sheet, drawer state chip); "measured (Jev) / LLM-estimated (Gemini)" in the drawer, questionnaire tooltip and Model calls — and nowhere else (DESIGN §0.6) | open |
  | 8.26 | Known cosmetic issues fixed (pipeline strip shows later stages pending during Re-run all) and anything else found; responsive check of Award, Decide, Settings at 375 / 768 / 1440 px (no sideways page scroll, tables scroll inside their card) | open |
  | 8.27 | §22.1 on production: login both users | open |
  | 8.28 | §22.2 on production: new RFx via co-pilot → 30 lines, questionnaire, terms, vendors; Issue → Outbox shows 5 (mock) | open |
  | 8.29 | §22.3 on production: Load seeded responses → all five reach flags done; queue contents compared item by item with §22.3 | open |
  | 8.30 | §22.4 on production: provenance drawer on an Excel cell, a PDF footnote, a Word sentence, an image crop, an email line | open |
  | 8.31 | §22.5 on production: clear the queue; cell states update | open |
  | 8.32 | §22.6 on production: Q1–Q8 computed with SQL visible; include best guesses changes totals; export works | open |
  | 8.33 | §22.7 on production: save scenario; compare two; generate memo; approve as Priya; grid locks | open |
  | 8.34 | §22.8 on production: email round trip on the mock mailbox (issue → portal reply with a photo → Sync inbox → response → extraction → cell lands) | open |
  | 8.35 | §22.9 on production: unrelated PDF (certificate) as a vendor response → supporting / not relevant, no crash | open |
  | 8.36 | §22.10 on production: `wrong_category_IT_quote.xlsx` → Unmatched; grid unchanged | open |
  | 8.37 | §22.11 on production: Eval page shows ≥ 143/150 or the honest number | open |
  | 8.38 | §22.12 on production: Model calls show provider per call; switch provider in Settings, re-run classify on one response; switch back | open |
  | 8.39 | CLAUDE §10 gate on production, every box (incognito login both users; MER-0417 awarded, MER-0418 draft, MER-0419 seeded with queue open; Eval latest number + per-cell diff; IT quote → Unmatched, no crash; Q1–Q8 each within 15 s with times; mock mailbox round trip; provider switch visible; Model calls show provider; memo PDF downloads; PROGRESS and DECISIONS up to date) | open |
  | 8.40 | Settings restored to the defaults and verified by script (USD 83.15 dated 2026-09-23, thresholds 0.85 / 0.60, provider auto, discount gross, freight 180, email mock) | open |
  | 8.41 | Tests (settings validation, eval route shape), lint, build green; `npm run pipeline:seed` 150/150, questionnaire 50/50; push; production checked after the deploy | open |
  | 8.42 | Final state (script): MER-0417 awarded, realistic, 0 open items, memo approved; MER-0418 untouched draft; MER-0419 seeded, 14 open items, no scenarios / awards, saved queries = count before P8; MER-0420 per Sabarish; throwaway RFx deleted with files and mailbox rows; settings at defaults | open |
  | 8.43 | Side by side with the prototype at 1440 px: Settings (cards, eval, model calls) and every screen touched by the polish pass; differences fixed or recorded | open |
  | 8.44 | Rows 8.1–8.43 ticked in PROGRESS.md with evidence (`--check`: every row keeps its requirement text, no older row changed); header ("P8 ✅ — build complete; next: human tasks"), eval line, what works / doesn't, Known issues, Open questions updated | open |
## Open questions (for Sabarish)
- **Asked cards and the Open list (P6):** once you ask a vendor, their cards move out of "Open" until the reply comes (visible under Decided/All), so the counts agree everywhere; the prototype keeps the clicked card open and actionable. Keep, or show asked cards under Open with an "asked" chip? (Easy to change.)
- **Demo script:** CLAUDE.md §10's "Gmail mode: one live round-trip" gate is replaced by the mock round trip (portal reply → Sync inbox); the Gmail steps in `docs/05_Demo_Script.md` become "open the vendor's mailbox (Portal) → Reply → Sync inbox" (DECISIONS "Live Gmail out of scope"). Update the script when convenient.
- ~~Approver's Decide page (DESIGN §3.8)~~ — no answer came, so it was **built in P7** as the handoff instructed (DECISIONS P7). Say if you'd rather Priya land on Comparison.
- **MER-0420 "Untitled RFx"** (draft, no lines) was created from the New RFx co-pilot at 15:56 on 24 Sep by a session that wasn't the P7 test browser — probably you trying the app. Left untouched; the RFx list therefore shows four events. Delete it, or keep it?
- **Memo wording:** keep P-MEMO verbatim (numbers are checked; wording occasionally imprecise), or allow one prompt round to pin the qualification words ("not cleared yet" vs "disqualified")?
- **Co-pilot initiative:** on the first message it sometimes fills standard terms and adds all five address-book vendors without being asked (the buyer can change either tab). Keep, or make it wait until asked?
- **P1 review (CLAUDE.md §6):** open https://quotelens-seven.vercel.app → MER-0419 → Responses → each vendor, and tell me any extracted item that looks wrong.
- Recommended: reset the Supabase database password (it appeared once in a script error during setup) and update `DATABASE_URL` in `.env.local`.
- ~~Add `NEXT_PUBLIC_APP_URL` in Vercel env~~ — done by Sabarish 2026-09-24 (used from P5; any deploy after that picks it up).
- ~~Confirm Fluid Compute is on~~ — confirmed Enabled by Sabarish 2026-09-24 (screenshot). Function Region shows "Overridden" because `vercel.json` pins `hnd1` (Tokyo, next to Supabase) — intended.
## Known issues
- P7: the memo narrative is model prose — every number is checked against the data, the words are not (seen: "lowest landed unit price" for a unit-price rule; Anand described as "disqualified" though its Q6 is pending). The tables below it are exact.
- P7: memo scripts can't import `@react-pdf/renderer` under `tsx` (`@react-pdf/hyphenate/en-us` export error) — memo tests go through the app's API (DECISIONS P7).
- P7: audit events of the P7 test runs stay on MER-0419's Timeline (scenarios saved/deleted, one memo), as in P6 — audit is append-only.
- P7: on Vercel one OrientPack photo extraction took 156 s (normally ~30 s) — Gemini latency; it finished inside the 300 s limit.
- P7: Ask planner variance seen once in the regression run: Q6 valued the unsure cells at other vendors' average price (₹1.41 cr) instead of best guesses (₹73.2 L) — computed and shown with its SQL; not tuned.
- P6 testing ran on a flaky connection and a heavily loaded Mac (load average up to 30; the macOS media-analysis/photo indexer). Gemini calls hung 18+ minutes and one reload died on ECONNRESET. Now: Gemini requests time out after 120 s with one retry on network failure; Supabase requests time out after 60 s (the stage shows an error and can be retried). If a seed reload fails half-way, run `npm run pipeline:seed` again (it is idempotent).
- Audit events outlive a seed reload (by design, append-only), so the Timeline tab of MER-0419 shows the clarification test runs of 24 Sep; replies from before the "vendor in the payload" fix read "from a vendor".
- Clarification replies take 25–60 s end to end (extract ~10 s on the strong model, map ~3 s, normalise ~4 s); the Sync button stays on "Syncing… 1 new reply, processing" meanwhile.
- Ask: model latency varies — UI answers took 3.4–10.2 s, one curl run 19.3 s (slow first call + repair). Answer column names vary run to run (vendor / winning_vendor / cheapest_vendor).
- Local dev only: after adding new route folders, Turbopack's cache sometimes serves 404 for every `/rfx/[id]/…` page until `.next` is deleted and `npm run dev` restarted. Production builds are unaffected.
- Dispatch email bodies from the fast model come as one paragraph (plain text, as P-DISPATCH asks); readable but dense.
- WhatsApp sample (`whatsapp_quote_sunrise_packers.png`): extraction read 1 item ("200x150x100 ₹4,000 /1000"); the dataset notes expect 8 from the chat bubbles. Not tuned (CLAUDE.md cut-list 9: don't tune for the realistic extras).
- Pipeline strip: during Re-run all, later stages keep showing last run's "done" until their turn comes (they should read pending). Cosmetic; fix with the P3 UI pass.
- ~~Browser console 404s for prefetched `/rfx/new` and overview links~~ — both pages exist since P5.
- P1 visual check vs prototype (Responses, 1440 px): layout, rows, pipeline strip, Files/Terms cards and items table match. Differences, all waiting on later tasks: header status stays "Issued" (flags P2-T7 moves it to receiving/reviewing); "30/30 priced", questionnaire "cleared" chip and the Mapped column need P2; Overview/Review/Comparison/Award tabs and Sync inbox/Ask buttons arrive with their phases; "Add response" needs the Inbox sheet (P5). One deliberate deviation: response detail is its own page (TRD §3 route) instead of an expanded row (DESIGN §3.5) — see DECISIONS.
- Realistic Balaji "Revised from 13200" comment: extraction still sets `references_prior_pricing = true`, but the terms decision reads it as p ≈ 0 and flags writes `false` back (fixed in P2-T5/T7).
- Realistic OrientPack (blurry rotated photo): Q10 "Payment 45 days" sometimes comes out `missing` instead of *yes* (1 run in 2) — fails safe (review card, no false "no").
- Gemini emulation gives mapping p = 1.00 on every seed item, so the 0.60–0.85 "Mapping needs a look" path is only exercised by unit logic, not by the seed data.
- Extraction wording varies run to run: one Kohinoor run copied the page-1 footnote into every item's `notes` and folded the Size/Ply columns into `vendor_description`. Numbers unaffected; revisit in P2 if mapping suffers.
- P0 checkpoint: sign-in and RFx list compared with the prototype at 1440 px (match; prototype renders in quirks mode, see DECISIONS) and checked at 375/768/1440/2560 px on production — no sideways page scroll, tables scroll inside their card on phones.
